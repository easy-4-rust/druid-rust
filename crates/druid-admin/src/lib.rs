//! `druid-admin` — axum-based HTTP service that exposes datasource, SQL,
//! Wall, active-connection and `/metrics` endpoints. Modelled after Druid
//! Java's `/druid/index.html` and `/druid/api/*` paths but limited to a JSON
//! surface (no bundled HTML SPA).
//!
//! **Status: design-stage.** See `druid-rust-Architecture.zh_CN.md` §19 and
//! `doc/9、druid-rust-视觉与交互DNA规范.md` for the planned routes, response
//! shape and visual DNA.

#![doc = "druid-admin: design-stage placeholder."]