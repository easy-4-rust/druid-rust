[package]
name         = "druid-sqlx"
version      = "0.0.0-design"
edition      = "2021"
rust-version = "1.95"
license      = "Apache-2.0"
description  = "Direct SQLx PhysicalConnection adapter for druid-rust"
publish      = false

[lints]
workspace = true

[dependencies]
druid-core = { path = "../druid-core" }
async-trait = { workspace = true }
sqlx = { workspace = true }

[dev-dependencies]
druid-pool = { path = "../druid-pool" }
tokio = { workspace = true, features = ["full"] }
