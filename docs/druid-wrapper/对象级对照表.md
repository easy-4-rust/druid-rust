# Java `druid-wrapper` → Rust `druid-wrapper` 对象级对照表

## 1. Java 13 个对象

| Java 对象 | Java 职责 | Rust 迁移落点 | 决策 | 当前 |
| :--- | :--- | :--- | :--- | :--- |
| `com.mchange.v2.c3p0.PooledDataSource` | c3p0 DataSource 类型兼容 | `Pool` provider contract | ADAPTER | PARTIAL |
| `com.mchange.v2.c3p0.ComboPooledDataSource` | Druid c3p0 facade | `SqlxBb8Pool`/`SqlxDeadpoolPool` + config facade | ADAPTER | PARTIAL |
| `org.apache.commons.dbcp.BasicDataSource` | DBCP facade | `DruidPool`/external provider facade | ADAPTER | PARTIAL |
| `org.apache.commons.dbcp.BasicDataSourceFactory` | properties 创建 datasource | `WrapperDataSourceFactory` | DIRECT/ADAPTER | MISSING |
| `org.apache.commons.dbcp.BasicDataSourceMBean` | 管理属性 | `WrapperPoolState`/metrics | PROTOCOL | MISSING |
| `org.apache.commons.dbcp.ManagedBasicDataSource` | managed facade | `ManagedWrapperPool` | ADAPTER | MISSING |
| `org.apache.commons.dbcp.ManagedBasicDataSourceFactory` | managed factory | `ManagedWrapperPoolFactory` | ADAPTER | MISSING |
| `org.apache.commons.dbcp.ManagedBasicDataSourceMBean` | managed MBean | metrics/admin protocol | PROTOCOL | MISSING |
| `org.apache.commons.dbcp2.BasicDataSource` | DBCP2 facade | provider/config facade | ADAPTER | PARTIAL |
| `org.apache.commons.dbcp2.BasicDataSourceFactory` | DBCP2 properties factory | `WrapperDataSourceFactory` | MERGE/ADAPTER | MISSING |
| `org.apache.commons.dbcp2.BasicDataSourceMBean` | DBCP2 management | metrics/admin protocol | PROTOCOL | MISSING |
| `org.logicalcobwebs.proxool.ProxoolConstants` | Proxool property names/defaults | `ProxoolConfigKey`/`WrapperPoolConfig` | DIRECT/MERGE | MISSING |
| `org.logicalcobwebs.proxool.ProxoolDataSource` | Proxool properties/DataSource/JNDI | `ProxoolDataSourceAdapter` | ADAPTER | MISSING |

## 2. Rust Adapter 对象

Toasty 是 `druid` 内置标准实现，不在 wrapper 重导出；下表只登记扩展。

| Rust 对象 | 当前文件 | 归并来源（历史） | 当前 |
| :--- | :--- | :--- | :--- |
| `SqlxConnectionAdapter` | `druid-wrapper/src/sqlx/sqlx_connection_adapter.rs` | `druid-sqlx` | PARTIAL：真实 SQLite 的 warning/clear/关闭状态，以及 Prepared stream/reader/URL/RowId、query/update/generic execute、有序 batch 和完整 Filter 描述符已验证；Any/PostgreSQL/MySQL 与 vendor 资源矩阵待补 |
| `SqlxConnectionFactory` | `druid-wrapper/src/sqlx/sqlx_connection_factory.rs` | `druid-sqlx` | PARTIAL |
| `SqlxPreparedStatement` | `druid-wrapper/src/sqlx/sqlx_prepared_statement.rs` | `druid-sqlx` | PARTIAL：真实 SQLx statement、继承属性、1-based 物理参数槽、资源 setter 时点物化、clear 与 addBatch 值快照已落；Blob/Clob/SQLXML 对象错误矩阵及 Any 后端待补 |
| `RbdcConnectionAdapter` | `druid-wrapper/src/rbdc/rbdc_connection_adapter.rs` | `druid-rbdc` | PARTIAL：公开 SPI 的 warning/clear/丢弃、Prepared 参数化 query/update/batch 与部分计数已验证；真实驱动端到端待补 |
| `RbdcConnectionFactory` | `druid-wrapper/src/rbdc/rbdc_connection_factory.rs` | `druid-rbdc` | PARTIAL |
| `RbdcPreparedStatement` | `druid-wrapper/src/rbdc/rbdc_prepared_statement.rs` | `druid-rbdc` | PARTIAL：具名物理对象、继承 Statement 状态、1-based 参数槽、Blob/Clob/NClob/SQLXML/Object setter 时点物化及 batch 值快照已落 |
| `PreparedParameterMaterializer` | `druid-wrapper/src/prepared_parameter_materializer.rs` | SQLx/RBDC 重复资源转换 | RUST_ONLY：共享 setter 边界转换策略；Ref/Array/vendor custom 明确 unsupported；Functions 100%，不承载连接所有权 |
| `PreparedParameterState` | `druid-wrapper/src/prepared_parameter_state.rs` | SQLx/RBDC 重复参数状态 | RUST_ONLY：共享参数槽/batch 快照；对象级测试通过 |
| `SqlxBb8ConnectionManager` | `druid-wrapper/src/sqlx_bb8/sqlx_bb8_connection_manager.rs` | `druid-sqlx-bb8` | PARTIAL |
| `SqlxBb8Pool` | `druid-wrapper/src/sqlx_bb8/sqlx_bb8_pool.rs` | `druid-sqlx-bb8` | PARTIAL |
| `SqlxDeadpoolConnectionManager` | `druid-wrapper/src/sqlx_deadpool/sqlx_deadpool_connection_manager.rs` | `druid-sqlx-deadpool` | PARTIAL |
| `SqlxDeadpoolPool` | `druid-wrapper/src/sqlx_deadpool/sqlx_deadpool_pool.rs` | `druid-sqlx-deadpool` | PARTIAL |

## 3. 归口关系

```text
druid-wrapper
└── src
    ├── rbdc
    ├── sqlx
    ├── sqlx_bb8
    └── sqlx_deadpool
```

原四个子 crate 已归并并删除；测试已移动到 `crates/druid-wrapper/tests`。
这些内部目录不具有独立发布、独立版本或独立产品完成率。

内置链路在 wrapper 之外：

```text
druid
└── src/toasty
    ├── ToastyConnectionFactory
    └── ToastyConnectionAdapter
```

## 4. 平台能力

| Java 平台/生态 | Rust 映射 |
| :--- | :--- |
| `javax.sql.DataSource` | `Pool` provider |
| JDBC raw connection | `PhysicalConnection` |
| c3p0/DBCP/Proxool pool | bb8/deadpool 或 native DruidPool Adapter |
| JNDI `ObjectFactory` | typed config/factory registry |
| MBean | metrics + admin API |
| Java Properties | serde config + aliases |

| Druid 数据源层级 | Rust 对象 |
| :--- | :--- |
| 内置标准实现 | Toasty（不属于 wrapper） |
| direct 扩展 | SQLx、RBDC |
| external pool 扩展 | bb8、deadpool |

<!-- GLOBAL_OBJECT_LEDGER_MERGE_START -->
## 归并自原全局对象总账的扩展文件条目

> 原全局账本中的 SQLx、RBDC、bb8 与 deadpool 文件条目已归入本模块；Java wrapper 13 个对象仍以前文 13/13 表为 canonical 账本。

#### 13. Rust 侧新增文件清单（保留原表并校正）

| Rust 文件/对象 | 承载的 Java 语义 | 当前 | 处置 |
| :--- | :--- | :--- | :--- |
| `sqlx_connection_adapter.rs` | SQLx raw connection | 已存在 | Native direct adapter；SQLite 执行、warning/clear 和关闭状态真实契约通过，数据库矩阵待补 |
| `rbdc_connection_adapter.rs` | RBDC raw connection | 已存在 | Native direct adapter；真实 RBDC trait 的 warning/clear、丢弃、Prepared 参数/批处理契约通过；隔离真实 SQLite 值合同通过，组合端到端待补 |
| `rbdc_prepared_statement.rs` | JDBC PreparedStatement 物理句柄边界 | 已存在 | RBDC 公开 SPI 无独立 prepared handle，以具名对象保存 SQL、继承属性、参数槽和 batch 快照 |
| `prepared_parameter_materializer.rs` | JDBC 资源 setter 的驱动边界物化 | 已存在 | SQLx/RBDC 共用 RUST_ONLY 策略；Ref/Array/vendor custom 不字符串降级 |
| `prepared_parameter_state.rs` | JDBC 1-based 参数状态与 addBatch 快照 | 已存在 | SQLx/RBDC 共用 RUST_ONLY 状态对象；不替代各 Adapter 的物理 statement |
| `sqlx_bb8_pool.rs` | bb8 pool lease | 已存在 | External bridge；直接实现 `Pool`，不实现 `PhysicalConnectionFactory` |
| `sqlx_deadpool_pool.rs` | deadpool Object | 已存在 | External bridge；直接实现 `Pool`，不实现 `PhysicalConnectionFactory` |

原表中的 `crates/droid-admin` 路径拼写已修正为 `crates/druid-admin`。
<!-- GLOBAL_OBJECT_LEDGER_MERGE_END -->
