# Java Druid core → Rust `druid` 对象级对照表

> Java baseline：`33824c3dec1612711f9bb4e409319bcab2e4cd0e`
> Rust baseline：`2c7bf3bea6f9e94f4583a85794df519c4435a425` + 当前 dirty migration worktree
> 最后静态核对：2026-07-30；文档状态：`PARTIAL`
>
> 本表冻结 Java `/core` 的 1,644 个对象分母。当前先列出主干对象与对象族；
> 每个对象未达到完整方法契约前只能标 `PARTIAL`。
>
> 这里的 1,644 是“来源审计分母”，不是要求 Rust 生成 1,644 个同名类型。
> JVM 专属对象必须入账后标为 `NOT_APPLICABLE/EXCLUDED`，并从 Rust
> “应实现分母”中排除。典型排除项包括 SLF4J、Log4j、Log4j2、Commons
> Logging、Logback、JMX MBean 与 ClassLoader 桥接对象。

## 1. 状态

| 状态 | 含义 |
| :--- | :--- |
| `DONE` | 当前源码存在，完整方法/字段及 Java oracle、真实依赖证据闭合 |
| `PARTIAL` | 当前源码存在，但字段、路径、分支、驱动或测试不完整 |
| `BROKEN` | 当前源码存在，但已确认关键路径错误 |
| `MISSING` | 尚无可运行的真实 Rust 落点 |
| `RENAME` | 有近似实现，但 canonical 对象或文件名不一致 |
| `ADAPTER` | Java 平台能力由 Rust 生态 Adapter 承载，仍需 Druid 契约验收 |
| `PROTOCOL` | Java 容器或平台协议由 Rust 协议实现，仍需逐项验证可观察语义 |
| `RUST_ONLY` | Rust 内部新增对象，必须注明承载的 Java 语义 |
| `MERGE` | 多个 Java 对象合并承载，必须逐个登记源对象和语义落点 |
| `SPLIT` | Java 对象拆到多个 Rust 组件，canonical facade 必须存在 |
| `NOT_APPLICABLE/EXCLUDED` | JVM/Java 平台专属对象，仅保留来源审计记录；不生成 Rust 类型，也不进入应实现分母 |

状态只表达当前证据，不表达计划。对象同名、文件存在或浅层单元测试通过，都
不能单独得到 `DONE`。

### 1.1 映射规则

1. `DIRECT` 对象保留 Java 语义名称：类型使用 PascalCase，文件使用 snake_case。
2. `MERGE` 必须逐个列出所有源对象，不能用“其他对象”替代对象账本。
3. `SPLIT` 必须指定 canonical facade，避免调用方依赖内部拼装。
4. `ADAPTER/PROTOCOL` 必须列出外部类型、兼容层和验收契约，不能只写 crate 名。
5. 一个 `.rs` 文件只定义一个迁移对象；Builder 或内部类型可与主对象同文件。
6. `mod.rs`、`lib.rs` 只做模块声明和重导出，不堆放迁移对象。
7. Java 日志实现类不做 `DIRECT/MERGE`：只迁移事件时机、级别、字段、脱敏和
   开关到 `tracing`；不得创建 SLF4J/Log4j 风格 Rust 类型或依赖。

## 2. 模块落点

| Java 域 | Java 数量 | Rust 落点 | 当前 |
| :--- | ---: | :--- | :--- |
| `sql` | 1,268 | `druid/src/sql` | PARTIAL |
| `support` | 105 | `druid`；管理协议部分归 `druid-admin` | PARTIAL |
| `pool` | 72 | `druid/src/core` + `druid/src/pool` | PARTIAL |
| `wall` | 46 | `druid/src/sql/wall` | PARTIAL |
| `proxy` | 36 | `druid/src/core/proxy` | PARTIAL |
| `util` | 35 | `druid/src/util`，按对象迁移 | PARTIAL |
| `filter` | 29 | `druid/src/core/filter` + `druid/src/stats` + `druid/src/sql/wall` | PARTIAL |
| `stat` | 24 | `druid/src/stats` | PARTIAL |
| `mock` | 22 | test fixtures，不计生产完成 | PARTIAL |
| core 根对象 | 7 | `druid` 根门面 + `druid/src/core` | PARTIAL |
| Java/JDBC 数据库平台 | 不计入 1,644 | `druid/src/toasty` 内置默认实现；`druid-wrapper` 扩展 | ADAPTER |

表中落点是当前物理目录。原 `druid-core`、`druid-pool`、`druid-sql`、
`druid-stats`、`druid-dynamic`、`druid-toasty` 已删除。

## 3. Pool/连接主对象

| Java 对象 | 映射 | Rust 对象/文件 | 当前职责与缺口 |
| :--- | :--- | :--- | :--- |
| `DruidDataSource` | SPLIT | `druid_data_source.rs` / `DruidDataSource` + `druid_pool.rs` / `DruidPool` engine | canonical facade、配置工厂、维护与管理协议已存在；fatal 阈值/借用门禁/即时 discard/shrink 重验生产链已接，完整 Java 配置、XA、驱动矩阵与统一差分仍为 PARTIAL |
| `DruidAbstractDataSource` | SPLIT | `config.rs` / `PoolConfig`、`PoolInnerConfig`、`DruidPoolBuilder` | 池主配置、默认值、属性绑定、边界校验与运行读取已迁入；全字段和动态 setter 仍为 PARTIAL |
| `DruidConnectionHolder` | DIRECT | `druid_connection_holder.rs` | 所有权/reset/schema/PS cache、线程安全 ConnectionEventListener/StatementEventListener 列表、普通/Prepared/Callable statement trace 及 PhysicalConnectionInfo 会话/全局变量持有已接；MySQL `show variables/show global variables` 实际采集已进入创建链，真实 MySQL 与差分待验 |
| `DruidPooledConnection` | DIRECT | `druid_pooled_connection.rs` | recycle、prepared/callable 子集、三个 createStatement 重载、Wrapper 解包、结构化 SQLWarning/Filter around-chain、ConnectionEventListener 添加/移除/关闭一次通知/错误通知、所有 Statement 创建/显式关闭/回收 trace，以及 PhysicalConnection/SQL Filter 错误的 sorter→数据源 fatal 状态→即时 holder/物理连接 discard 主链已迁移；完整统计与统一差分仍缺失 |
| `PreparedStatementPool` | DIRECT | `prepared_statement_pool.rs` | LRU/inUse/share/clear；Oracle implicit cache 缺失 |
| `PreparedStatementHolder` | DIRECT | `prepared_statement_holder.rs` | key/计数/peak；驱动属性矩阵缺失 |
| `DruidPooledPreparedStatement` | DIRECT | `druid_pooled_prepared_statement.rs` | execute/cache/lease、self/PreparedStatement/raw concrete unwrap、clearParameters/clearBatch fatal-discard/cache 淘汰、48 个 JDBC setter 重载与持久参数槽、绑定 query/update/generic execute、有序描述符 batch、顺序多结果/generated keys/getter sorter/ResultSet trace/继承 SQLWarning 与 Statement 属性已验证；五个缓存敏感属性按 Java 顺序初始化、更新并在回收前恢复，首错停止且禁止异常句柄重入缓存；canonical `executeQuery` 直接持有物理 ResultSet，共享生命周期与 handle 使 ResultSet 恢复同一 Prepared 身份；Toasty/SQLx SQLite 资源主链及 RBDC 严格 SPI 已接，原生多数据库 generic execute/ResultSet 仍缺 |
| `PreparedInputParameter` | RUST_SUPPORT | `prepared_input_parameter.rs` | 一个 Rust 对象承载 Java PreparedStatement 各 setter 的输入联合类型；保留 null/typeName、Calendar、Object 元数据、stream/reader 长度和 JDBC 资源身份，并作为 Filter/物理参数批次协议；`RustValue` 仅标识原 Rust 显式 batch 扩展，不冒充 Java setter；不是 Java 新对象计数项 |
| `ToastyPreparedStatement` | PLATFORM_ADAPTER | `toasty/toasty_prepared_statement.rs` | Toasty raw SQL 缺少独立 PreparedStatement 槽位时的物理句柄；setter 阶段物化资源并保存参数，负长度/提前 EOF 当场返回，addBatch 保存值快照，execute 不二次读取；继承 Statement 属性委托真实句柄状态 |
| `DruidPooledCallableStatement` | DIRECT/ADAPTER | `druid_pooled_callable_statement.rs` | Java 本类构造器之外 115 个公开声明均有 Rust 同语义入口；继承的 execute/result-set/generated-keys/more-results 路径复用 Prepared 基类，并由 `DruidPooledCallableStatementHandle` 保留 callable 动态身份、Prepared 继承能力和关闭级联。支持存储过程的真实 driver、错误和结果资源证据仍未闭合 |
| `DruidPooledStatement` | DIRECT | `druid_pooled_statement.rs` | PARTIAL：独立对象、同租约校验、query/update、JDBC `int[]` batch/部分失败、四种 `execute` 入口、结果类型判定、顺序多结果、`getResultSet/getUpdateCount/getMoreResults/getGeneratedKeys`、结构化 SQLWarning、属性、关闭、Wrapper 与 ResultSet trace 已验证；SQLx/RBDC Connection warning Adapter 已闭合，原生多结果及其余 Filter statement 事件仍缺失 |
| `DruidPooledResultSet` | DIRECT/ADAPTER | `druid_pooled_result_set.rs` + `PhysicalResultSet`/`RowSetResultSet` + `ResultSetStatement` | PARTIAL：既有完整 getter/update/metadata/Wrapper 基础上，next/close/warning、9 族标量 18 getter、BigDecimal/Date/Time/Timestamp 16 个强类型重载、plain/map/typed object 六重载、13 族 stream/resource 26 重载、navigation/property 26 调用、NString 两重载、getMetaData、getStatement、七个 row-mutation、14 族 index/label 共 28 个基础列 update setter、`updateObject` plain/scaleOrLength × index/label 四重载、七类资源对象 index/label 14 个 setter、Blob InputStream 与 Clob/NClob Reader 的 12 个 LOB setter、ASCII/Binary/Character/NCharacter 的 22 个 stream setter，以及 `updateNString` 的 index/label 两个 setter 均同步进入 Filter around-chain；`ResultSetStatement` 保留普通/Prepared/Callable 三种动态身份与同一逻辑生命周期；row-mutation、基础/object/resource/LOB/stream/NString setter 已验证默认穿透、全方法短路、参数身份、共享游标、错误分类及真实 SQLite capability error，vendor custom object 可下转回原类型，资源链内拥有型 Clone 保持共享句柄；其余精确参数、共享资源与 metadata 平台身份由探针和真实 Toasty SQLite 验证；`is_closed_with_connection` 保留 Java 可失败入口；StatFilter、typed custom object 单次物理读取和最小 SPI 错误优先级已闭合；185 个 ResultSet 调用保留精确委托，ResultSet update setter Filter 子域已闭合；Statement 与 PreparedStatement canonical query 不再经 `Vec<Row>` 重建，SQLx 真实 SQLite 与 RBDC SPI driver label 可贯穿 metadata/findColumn/label getter；getString/getBytes/stream 读取量及直接/间接 LOB open 已接 SQL 统计；Toasty eager/raw SQL alias 缺失、RBDC 空结果 descriptor、嵌套 ResultSet/Clob object 自动代理、streaming、driver custom conversion 与多数据库仍待补 |
| `ConnectionEventListener` | ADAPTER | `connection_event_listener.rs` + holder/pooled connection 生产接线 | IMPLEMENTED_UNVERIFIED：Rust 最小 SPI 已承载关闭和错误通知；listener 列表、对象身份移除、关闭一次通知及回收清理已接，最终统一测试待做 |
| `StatementEventListener` | PLATFORM_ADAPTER | `statement_event_listener.rs` + holder/pooled connection 生命周期 | IMPLEMENTED_UNVERIFIED：Rust 最小 SPI 保留两个 JDBC 回调签名；按 Java Druid 现状仅迁移 listener 增删与 reset/recycle 清理，不擅自发布 Java 源码未发布的事件；最终统一测试待做 |
| `ExceptionSorter` | DIRECT | `exception_sorter.rs` + 11 个独立 vendor/abstract 文件 | `SqlException` 保留 code/SQLState/recoverable/class/message/cause/`instanceof` 类型链；Java 全部 11 个 vendor/abstract 对象均已独立迁移，池化主链仍按独立账目追踪 |
| `ValidConnectionChecker` | DIRECT | `valid_connection_checker.rs` | 真实驱动超时矩阵不完整 |

## 4. Proxy/Filter 对象族

| Java 对象/对象族 | Rust 落点 | 当前 |
| :--- | :--- | :--- |
| `Filter` / `FilterAdapter` / `FilterEventAdapter` / `FilterManager` / `FilterChain` / `FilterChainImpl` | `filter.rs` + `filter_adapter.rs` + `filter_event_adapter.rs` + `filter_manager.rs` + `filter_chain.rs` + `filter_chain_impl.rs` + `result_set_filter*.rs` | PARTIAL：canonical `FilterChainImpl` 已存在并由 `FilterChain` 接口兼容名直接指向，承载统一实例注册、生命周期、dataSource get/release、物理 connect、SQL/batch/连接事件、warning 有位置链及已迁移 ResultSet 精确 around-chain；DataSource get 可修改 maxWait/短路，release 可包围/短路唯一 holder 回收，ResultSet update setter 子域已闭合。Java 498 个 public/protected 方法中，完整 Connection/Statement/Prepared/Callable/LOB/metadata hook、具体 proxy 身份和 nested downstream-before 展开仍未闭合，不能因 canonical 文件存在而标 DONE |
| `ConnectionProxy/Impl` | `DruidPooledConnection` + `ProxyAttributes` + `TransactionInfo` | MERGED/PARTIAL/IMPLEMENTED_UNVERIFIED：连接 ID、raw/unwrap、惰性 attributes、物理创建 epoch、显式关闭次数、最近成功验证时间、活动事务信息、连接逻辑驱动 properties 及 before/commit/rollback/close Filter 事件身份已进入 canonical 池化连接；各 Adapter 对 vendor properties 的真实应用和统一差分仍缺 |
| `StatementProxy/Impl` | `DruidPooledStatement` + `ExecContext` | MERGED/PARTIAL/IMPLEMENTED_UNVERIFIED：数据源级 20000 起始 ID、attributes、raw/unwrap、最近 SQL/耗时/update/result/batch 状态及 Filter 执行/创建/关闭身份已接；ExecContext 拥有可改写 SQL，普通 Statement 的 Filter 输出已进入物理驱动并回写 lastSql，addBatch 在保存前逐条改写；完整 Java getter 仍开放 |
| `PreparedStatementProxy/Impl` | `DruidPooledPreparedStatement` + `PreparedInputParameter` | MERGED/PARTIAL/IMPLEMENTED_UNVERIFIED：继承同一 Statement ID/attributes/执行状态，参数槽、setter 描述符、batch 快照、JdbcParameter 合同及 0-based Proxy 参数视图已接；剩余 Filter hook 待补 |
| `CallableStatementProxy/Impl` | `DruidPooledCallableStatement` | MERGED/PARTIAL/IMPLEMENTED_UNVERIFIED：继承 Prepared/Statement 身份、attributes、参数与结果状态；真实 callable driver 及剩余 hook 待补 |
| `ResultSetProxy/Impl` | `DruidPooledResultSet` + `ResultSetFilterContext` + `ResultSetOpenContext` | MERGED/PARTIAL/IMPLEMENTED_UNVERIFIED：数据源级 50000 起始 ID、attributes、raw/unwrap、SQL、Statement 身份、construct elapsed、游标/fetch/close/read/open、logic↔physical column map、nullable hidden columns 与三层 Filter ID 已接；Wall tenant metadata 已自动生产并绑定到每个 ResultSet，完整日志列值和统一差分待补 |
| `ClobProxy` / `NClobProxy` / `BlobProxy` | `JdbcBlob/JdbcClob/JdbcNClob` 仅承载 Java 平台资源；Druid Proxy/Filter 事件对象仍待建 | PARTIAL/MISSING |
| `StatFilter` | `druid::stats::StatFilter` | PARTIAL：SQL query/update/generic execute、普通与 PreparedStatement batch、活动事务 commit/rollback、ResultSet fetch/hold/read/stream、数据源/SQL 双层 LOB open、pool get/release 及真实物理 connect/close around-chain 已接生产链；beforeConnect/connectError/afterConnected/Entry 注册与 close count/Entry 移除只由启用的 StatFilter 产生，不由池无条件伪造；完整字段及统一差分待补 |
| `WallFilter` | `druid::sql::WallFilter` | PARTIAL/IMPLEMENTED_UNVERIFIED：独立对象已实现 SQL before/after、拒绝错误、SELECT/UPDATE/INSERT tenant AST 改写、普通 Statement 物理透传与 addBatch 保存前改写、Prepared/Callable prepare 前改写、raw metadata 隐藏列映射与 tenant next 回调、Connection metadata Wall 开关及 privileged 绕过，并由内置 FilterManager 工厂装配；完整 Java hook、配置项与多方言规则仍开放 |
| `LogFilter` | tracing Adapter | PARTIAL：canonical `LogFilter` 只迁移 Druid 的管理配置、开关和事件语义；`log` 仅为 Druid Filter 配置短名，运行时后端统一为 `tracing`；SQL/batch、Statement 创建/关闭、connection commit/rollback、pool get/release、真实物理 connect/close 与 ResultSet open/next/close/error 已输出相应 Proxy ID，物理 connect/close 均遵守嵌套链时机；Java Log4j/SLF4J/Commons Logging 类和 MBean明确 `NOT_APPLICABLE`，不以对象数量为目标；完整 ResultSet 列值、动态 tracing level 和 executable SQL 格式仍缺 |
| `ConfigFilter` / `ConfigTools` | `core/filter/config/{config_filter,config_tools}.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：canonical 对象已保留配置优先级、properties/XML、本地/classpath/HTTP(S)、旧 RSA 密文、错误前缀和工厂前置装配；Java/Rust 密文向量、HTTP/XML 与真实数据源统一验证待最终阶段 |

## 5. SQL/Wall/Stat 对象族

| Java 对象/对象族 | Rust 落点 | 当前 |
| :--- | :--- | :--- |
| `SQLObject` / statement / expression AST | `druid::sql` compatibility AST | MISSING/PARTIAL |
| parser/lexer/dialect parsers | sqlparser-rs + Druid dialect extension | PARTIAL |
| Visitor/OutputVisitor | compatibility visitors | MISSING |
| `SQLUtils` / `JdbcUtils` / `DbType` | canonical facade objects | PARTIAL：`DbType` 严格名称、两别名、mask/hash/equals/PostgreSQL-style 已落；`JdbcUtils` URL/driver 子集已有；`SQLUtils` 与 JdbcUtils 全方法仍缺 |
| `WallConfig` | `wall_config.rs` | PARTIAL：tenantColumn、tenantTablePattern、doPrivilegedAllow、运行期线程安全 TenantCallBack 与 update-check 配置已接；完整 Java 字段和规则生效矩阵仍开放 |
| `WallConfig.TenantCallBack` / `TenantCallBack.StatementType` | `tenant_call_back.rs` / `TenantCallBack` + `TenantStatementType` | DIRECT/IMPLEMENTED_UNVERIFIED：四个回调方法、四种语句类型与 ResultSet tenant 值回调已接；SQL AST tenant 注入和统一差分待补 |
| `WallProvider` | `sql/wall_provider.rs` provider/cache facade | PARTIAL：规则检查、缓存、白/黑名单、统计 API、同步 privileged 嵌套恢复与 async task-local scope 已有；完整方言 provider/visitor、tenant/mustParameterized 仍缺 |
| violation 对象族 | `WallViolation` variants | PARTIAL |
| `JdbcSqlStat` | `stats/jdbc_sql_stat.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：成功/错误、最近错误、running/concurrent、事务、执行/hold 时间与 histogram、batch、update/fetch、lastSlowParameters、读取量/流/LOB 打开数已接生产链；执行开始/最大耗时发生时点已接，管理 ID 独立递增、HASH 精确使用 Java UTF-16 FNV-1a；JVM stack trace、LOB 内容读取统计及统一差分未闭合 |
| `JdbcDataSourceStat` | `stats/jdbc_data_source_stat.rs` | PARTIAL：canonical 对象聚合 SQL merger、Connection/Statement/ResultSet、慢 SQL 与独立 resetStatEnable gate；`StatsCollector` 仅为兼容 type alias；分层完整字段和统一差分未闭合 |
| `DruidStatManagerFacade` / `DruidStatService` | `stats/druid_stat_manager_facade.rs` + `druid_stat_service.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：datasource/sql/wall/connectionInfo/active trace/reset JSON 协议已接；resetAll 按 Jdbc manager→datasource manager→facade count 分层执行，log-and-reset 独立发布区间快照；basic 输出真实 StartTime/Rust MSRV/target，并从已注册数据源枚举去重 driver label，Java runtime 兼容键保持 null；SQL zero/non-running 过滤及 Wall 递归聚合已接；未实例化 Adapter 的全局 Drivers、Java web/spring 分支和逐字段 golden 未闭合 |

## 6. Java 平台对象分账

| Java 平台对象 | Rust 落点 | 规则 |
| :--- | :--- | :--- |
| `java.sql.Connection` | `PhysicalConnection` | 内部最小 SPI |
| JDBC driver factory/connection | `ToastyConnectionFactory` + `ToastyConnectionAdapter` | `druid` 内置标准实现；直接使用 Toasty Driver SPI，不使用 `toasty::Db` pool |
| `java.sql.PreparedStatement` | `PhysicalPreparedStatement` | 真实 driver handle |
| `java.sql.CallableStatement` | `PhysicalCallableStatement` + `CallableInputParameter`/`JdbcObject`/`CallableParameter`/`CallableOutParameter` | setter 身份及 sqlType/typeName/scale 必须保留；`CallableOutputValue` 仅为 `JdbcObject` 兼容 alias；不支持时明确报错 |
| `java.lang.Object`（JDBC 值域） | `JdbcObject` + `JdbcOpaqueObject`/`PhysicalJdbcOpaqueObject` | 标准标量、资源和 vendor custom 对象统一平台表示；自定义对象保留引用身份、类名与受控 downcast |
| `java.lang.Class<T>`（JDBC typed getter） | `JdbcTargetType` | 保留 typed `getObject` 的标准目标和 vendor 类名；`CallableTargetType` 仅为兼容 alias |
| `Map<String, Class<?>>` | `JdbcTypeMap` | ResultSet/Callable/Array/Ref 共用；`CallableTypeMap` 仅为兼容 alias |
| `java.sql.ResultSetMetaData` | `ResultSetMetaData` + `PhysicalResultSetMetaData` + `ResultSetColumnMeta`/`ResultSetColumnType`/`ResultSetNullability` | 双后端句柄：eager descriptor 与 physical driver metadata；Java 标准 getter、逐调用错误时机、self/SPI/concrete Wrapper 身份、三态可空性、Types/class/origin/shape/读写标志完整表达；真实字段只由 driver descriptor 提供，不猜测 |
| `java.sql.ResultSet#getStatement()` 返回对象 | `ResultSetStatement` | ADAPTER：Rust 拥有型枚举保留普通、Prepared、Callable 三种动态身份；各分支 Clone 共享同一逻辑内核、关闭状态和缓存所有权，不是字段快照，也不把三种对象错误合并为单一最小接口 |
| `java.sql.Blob` | `JdbcBlob` + `PhysicalBlob` | 完整 11 方法资源 SPI；对象身份比较，不隐式读取内容；驱动负责实际存储和错误 |
| `java.sql.Clob` | `JdbcClob` + `PhysicalClob` | 完整 13 方法资源 SPI；位置/长度为 Java 有符号类型；禁止静默物化为 Rust `String` |
| `java.sql.NClob` | `JdbcNClob` + `PhysicalNClob` | 保留 `NClob extends Clob` 类型身份和完整继承操作 |
| `java.lang.String` | `JavaString` | UTF-16 code unit 值对象；保留未配对 surrogate，严格转 UTF-8 时显式报错 |
| `java.io.Reader` / `Writer` | `JdbcReader` / `JdbcWriter` | Clone 共享游标/关闭状态；字符以 UTF-16 code unit 传递；Callable setter 不预读 |
| `java.io.InputStream` / `OutputStream` | `JdbcInputStream` / `JdbcOutputStream` | Clone 共享游标与关闭状态；Callable setter 不得提前物化 |
| `java.util.Calendar` | `JdbcCalendar` + `JdbcCalendarArgument`（`CallableCalendar*` 为兼容别名） | 保留时区标识，并区分无 Calendar 重载、显式 null 与实际 Calendar；Callable/ResultSet 共用 |
| `java.math.BigDecimal`、`java.sql.Date/Time/Timestamp` | `bigdecimal::BigDecimal` + `chrono::NaiveDate/NaiveTime/NaiveDateTime` | 使用生态强类型；禁止压缩成 String/Bytes |
| `java.sql.ResultSet` | `PhysicalResultSet` + `RowSetResultSet` + `ResultSetUpdate` | 当前内置 SQLite 为可滚动只读 eager Adapter；1-based 索引、NULL、游标、fetch/type、基础/流/资源/Decimal/日期时间 getter、全部标量与流 update 重载身份、Map/typed getObject SPI 与完整 metadata getter 对象契约已落；通用对象及 String/Boolean/Long/Int/Short/Byte/Double/Float/Bytes 的 index/label 物理 SPI 可被 driver 独立覆盖，默认 `Value` Adapter 的 NULL/转换/错误合同已测；最小 SPI 的精确 operation、错误优先级及 custom typed object 单次读取已锁定；平台已支持 physical metadata，但 Toasty 尚未供给真实 label/origin descriptor；streaming、真实 custom conversion 与多数据库实现待补 |
| `java.sql.SQLException` | `DruidError` + 后续结构化物理错误 | 保留错误分类 |
| `java.sql.BatchUpdateException` | `DruidError::BatchUpdateException { update_counts, cause }` | 保留 `int[]` 部分更新计数、`-2/-3` 表达空间和原始 SQL cause；fatal sorter 递归检查 cause |

### 6.1 内置与扩展对象分账

| 层级 | 对象 | 当前模块/目录 | 归并来源（历史） | 是否内置 | 所有权 |
| :--- | :--- | :--- | :--- | :---: | :--- |
| Druid pool | `DruidPooledConnection` | `druid/src/core` | `druid-core` | 是 | 对外唯一池化连接 |
| 稳定 SPI | `PhysicalConnection` | `druid/src/core` | `druid-core` | 是 | 只表示一条 raw connection |
| 标准 factory | `ToastyConnectionFactory` | `druid/src/toasty` | `druid-toasty` | 是 | 共享无状态 Driver |
| 标准 adapter | `ToastyConnectionAdapter` | `druid/src/toasty` | `druid-toasty` | 是 | 独占一条 Toasty raw connection |
| direct 扩展 | `SqlxConnectionAdapter`/`RbdcConnectionAdapter` | `druid-wrapper/src/{sqlx,rbdc}` | `druid-sqlx`/`druid-rbdc` | 否 | 各独占一条 raw connection |
| external pool | `SqlxBb8Pool`/`SqlxDeadpoolPool` | `druid-wrapper/src/sqlx/{bb8,deadpool}` | 原两个 SQLx pool crate | 否 | 直接实现 `Pool`，不得进入 native idle queue |

本文件已经承接原仓库级对象总账中属于 Java core 的全部明细；本模块完成率只以
1,644 个 Java core 对象逐项关闭为准。

<!-- GLOBAL_OBJECT_LEDGER_MERGE_START -->
## 归并自原全局对象总账的明细

> 以下明细归并自原全局对象总账中属于 Java `/core` 与 Rust `druid` 的条目。
> 归并后本文件是这些对象的唯一迁移账本。

##### 2.1 Druid 对象与平台依赖分账

`java.sql.*`、`javax.sql.*` 是 Java/JDBC 平台标准，不是 Druid 仓库生产对象。它们必须有 Rust 能力映射，但不计入 1,719 个 Druid 对象的迁移分母。

| Java 平台对象 | Rust 能力落点 | 分类 | 边界 |
| :--- | :--- | :--- | :--- |
| `java.sql.Connection` | `PhysicalConnection` + typed request/result | PLATFORM_ADAPTER | druid-rust 内部最小 SPI，不直接对应用公开 |
| `java.sql.Driver` | `PhysicalConnectionFactory` + driver registry | PLATFORM_ADAPTER | 仅创建 raw physical connection，不管理池租约；`create_info_with_properties` 承接单次 Filter 最终 Properties，默认适配预配置 factory，各驱动 Adapter 可覆盖消费，不模拟 JDBC DriverManager |
| `javax.sql.DataSource` | `DruidDataSource` + `ConnectionProvider` | PLATFORM_ADAPTER | facade 对外；Provider 决定 native/bridge 来源 |
| `javax.sql.StatementEventListener` | `StatementEventListener` + holder listener 集合 | PLATFORM_ADAPTER | Rust 标准库无 JDBC 监听器；Druid core 仅保存/移除/reset 清空，故当前不增加回调发布副作用 |
| `java.sql.Statement` / `PreparedStatement` / `ResultSet` | `PhysicalPreparedStatement`、`PreparedInputParameter`、`ToastyPreparedStatement`、执行请求/结果和后续借用行流 | PLATFORM_ADAPTER | PreparedStatement setter 状态已进入物理 SPI；标量与 stream/reader/LOB/URL/RowId/null 由 Toasty SQLite 真实验证，资源在物理 setter 时点物化；SQLx/RBDC 资源参数、流关闭和完整 ResultSet 仍待迁移 |
| `java.sql.CallableStatement` | `PhysicalCallableStatement` + `CallableInputParameter` + `JdbcObject` + `CallableParameter` + `CallableOutParameter` | PLATFORM_ADAPTER | `CallableOutputValue` 仅为兼容 alias；输入 setter 身份及 sqlType/typeName/scale/Calendar/LOB/Reader 精确长度重载必须无损；SQLx/RBDC 当前无统一标准，必须显式 unsupported，不得用普通 SQL 句柄伪装 |
| `java.sql.Blob` | `JdbcBlob` + `PhysicalBlob` | PLATFORM_ADAPTER | 完整 11 方法资源 SPI；对象身份比较，禁止静默物化为 Vec |
| `java.sql.Clob` / `NClob` | `JdbcClob`/`PhysicalClob` + `JdbcNClob`/`PhysicalNClob` | PLATFORM_ADAPTER | Clob 完整 13 方法；NClob 保留继承类型身份；禁止静默物化为 Rust String |
| `java.lang.String` | `JavaString` | PLATFORM_ADAPTER | UTF-16 code unit 值对象，可无损表达未配对 surrogate |
| `java.io.Reader/Writer` | `JdbcReader/JdbcWriter` | PLATFORM_ADAPTER | Clone 共享游标/关闭状态；池化层不得提前消费 |
| `java.io.InputStream/OutputStream` | `JdbcInputStream/JdbcOutputStream` | PLATFORM_ADAPTER | Clone 共享游标与关闭状态；池化层不得提前消费 |
| `java.util.Calendar` | `CallableCalendar` + `CallableCalendarArgument` | PLATFORM_ADAPTER | 保留时区标识与 Calendar 重载/null 身份，不缩成瞬时 offset |
| `java.math.BigDecimal`、`java.sql.Date/Time/Timestamp` | `bigdecimal` + `chrono` 强类型 | PLATFORM_ADAPTER | 保留任意精度/scale、日期时间类型和 Timestamp 纳秒 |
| Toasty `Driver` / `Connection` | `ToastyConnectionFactory` / `ToastyConnectionAdapter` | RUST_ECOSYSTEM | `druid` 内置标准实现；每次创建 raw connection，不使用 Toasty pool |
| `sqlx::Connection` | `SqlxConnectionAdapter` | RUST_ECOSYSTEM | direct adapter，不携带 SQLx pool |
| `rbdc::db::Connection` | `RbdcConnectionAdapter` | RUST_ECOSYSTEM | direct adapter |
| bb8/deadpool lease | `SqlxBb8Pool` / `SqlxDeadpoolPool` + `PhysicalConnectionLease` | RUST_ECOSYSTEM | bridge 直接实现 core `Pool`，不进入 native idle queue |

Rust 标准库没有与 JDBC `java.sql.Connection` 对等的统一数据库连接标准。SQLx、RBDC、Diesel 驱动等属于生态 crate，抽象层级和能力集合并不相同；因此这里迁移的是 JDBC 作为 Druid 底层平台所提供的**能力契约**，由内部 `PhysicalConnection` 统一，再由各驱动 Adapter 实现，而不是虚构一个不存在的 Rust 标准库类型。

#### 3. Pool 与连接生命周期

| Java 对象 | 映射 | Rust 落点 | 当前 | 必须保留的职责 |
| :--- | :--- | :--- | :--- | :--- |
| `DruidDataSource` | SPLIT | `druid::pool::DruidDataSource` facade + `DruidPool` engine | PARTIAL/IMPLEMENTED_UNVERIFIED | init 幂等、asyncInit、initExceptionThrow 两分支、canonical `get_connection/get_connection_with_max_wait/get_connection_direct`、dataSource get/release 有位置 Filter、get/tryGet/isFull/recycle/discard、三个 shrink 重载、public fill 到 maxActive/fill(toCount)、enable/disable、removeAbandoned、维护、close-before-init/幂等 close、restart/generation、resetStatEnable/resetCount/resetStat 与管理快照已接；reset 禁用时完全无副作用，执行后 activePeak 回到当前 activeCount；discard/creator 及 onFatalError 主链接线已闭合；具体 DataSource 全 API Filter 身份、`restart(Properties)`、XA、全配置与统一差分仍开放。Java 1.2.28 无公开 purge 方法，不制造 Rust API |
| `DruidAbstractDataSource` | SPLIT | `PoolConfig` + `PoolInnerConfig`/`DruidPoolBuilder` | PARTIAL | 默认连接属性、dbType、容量/等待/校验/维护/重试/泄漏属性加载与运行读取已接；公平锁和剩余字段待迁移 |
| `DruidConnectionHolder` | DIRECT/SPLIT | `druid/src/core/druid_connection_holder.rs`，内部复用 `ConnectionDefaults` | PARTIAL | canonical holder 已真实持有物理连接、默认状态、时间/useCount/schema/version/discard、惰性 statement pool、ConnectionEventListener、StatementEventListener、statement trace 与会话/全局变量；trace 按 Java reset 顺序关闭普通/Prepared/Callable 并保持 PS cache 状态机；创建前凭据版本快照、旧空闲替换及旧活跃归还淘汰已接；初始化 SQL与 MySQL session/global variables 采集进入 raw connection 创建链；最终差分仍待迁移 |
| `DruidPooledConnection` | DIRECT | `druid/src/core/druid_pooled_connection.rs`（旧 `PooledConnection` 仅兼容重导出） | PARTIAL/IMPLEMENTED_UNVERIFIED | 唯一公开连接并独占一个 holder；回收、监听器、schema、prepare/call、statement trace、Filter SQL 及 Wrapper 已迁移；fatal 命中现回写数据源状态并在错误返回前 take-once discard，SQL 执行/Filter 错误保留当前 SQL。剩余 callable、多驱动与统一差分待迁移 |
| `DruidDataSource#handleFatalError` 内部协作边界 | PROTOCOL/MERGED | `core/fatal_error_handler.rs` / crate-private `FatalErrorHandler` + `PoolInner` 同锁状态 | IMPLEMENTED_UNVERIFIED | Rust 支撑协议，不冒充额外 Java 对象；承载 fatal 记录、一次 creator 唤醒和 validation 清门禁，真实状态仍属于 canonical `DruidDataSource` engine |
| `DruidDataSource#recycle/discardConnection` 最终处置 | SPLIT/RUST_ONLY | `ConnectionRecycleDisposition` + boolean native return callback + `ConnectionCreateWorker` | PARTIAL/IMPLEMENTED_UNVERIFIED | 区分 reusable、正常 discard、recycle-error discard；统计、最大使用次数、closed/disabled 归还销毁、nullable discard、一次性回调、boolean refill signal 及 minIdle 后台补池已接；统一差分仍待迁移 |
| `PhysicalConnectionInfo` | DIRECT | `druid/src/core/physical_connection_info.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | raw connection、connect/init/validate 阶段时序、createTaskId、会话/全局变量及 take-once holder 交接已接；`druid.initVariants/initGlobalVariants/initConnectionSqls` 进入 factory/builder，MySQL 协议族通过 raw SPI 实际采集；真实 MySQL 与统一差分待补 |
| `DruidDataSourceFactory` | DIRECT/ADAPTER | `druid/src/pool/druid_data_source_factory.rs` / `DruidDataSourceFactory` | PARTIAL | Toasty 默认 factory 与外部 PhysicalConnectionFactory 注入、池/Wall 主要属性解析和边界校验已接；完整 Java property/JNDI 面待补 |
| `DruidDataSourceC3P0Adapter` | ADAPTER | `DruidDataSourceC3p0Adapter` facade + `C3p0ConnectionProvider` | MISSING | 外部池配置、租约、unwrap、统计兼容结果；不得形成池中池 |
| `DruidDataSourceStatValue` | DIRECT | `druid/src/stats/druid_data_source_stat_value.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | 独立不可变区间快照已接；池/执行/事务/PS/histogram/SQL list 来自生产计数并 reset；query/login timeout 已进入运行路径，SQL 插入顺序淘汰产生 skip，ResultSet/Callable 直接及 Java 对应的 Map/typed/普通 `getObject` Blob/Clob/NClob 路径产生 LOB open；统一差分待补 |
| `DruidDataSourceStatLogger` | PROTOCOL/MERGED | `pool/data_source_stat_sink.rs` / `DataSourceStatSink` | IMPLEMENTED_UNVERIFIED | 已迁移“区间快照发布”的可替换协议；不照搬 Java logger SPI 名称，输出端可接 tracing、metrics、OpenTelemetry 或用户 sink |
| `DruidDataSourceStatLoggerAdapter` | NOT_APPLICABLE | Rust trait 默认方法/组合 | NOT_APPLICABLE | Java 便利基类不形成 Rust 对象 |
| `DruidDataSourceStatLoggerImpl` | PROTOCOL/MERGED | `pool/tracing_data_source_stat_sink.rs` / `TracingDataSourceStatSink` | IMPLEMENTED_UNVERIFIED | 默认输出结构化 tracing event + 完整 JSON snapshot；周期、立即首发、单轮错误隔离、close/restart 启停已接。不迁移 SLF4J 实现类、logger name/class |
| `PreparedStatementHolder` | DIRECT | `druid/src/core/prepared_statement_holder.rs` | PARTIAL | key、物理 statement、hit、fetch peak、prefetch、Oracle/inUse/pooling 字段已迁移；Oracle 真实驱动动作待补 |
| `PreparedStatementPool` | DIRECT | `druid/src/core/prepared_statement_pool.rs` | PARTIAL | access-order LRU、容量、share/inUse、命中/淘汰/clear/统计已迁移；Oracle implicit cache、listener/trace 待补 |
| `DruidPooledStatement` | DIRECT/ADAPTER | `druid/src/core/druid_pooled_statement.rs` / `DruidPooledStatement` | PARTIAL | canonical 对象已建立；createStatement 三重载、close、JDBC `int[]` batch/部分失败、超时/属性、query/update、四种 execute、结果游标推进、generated keys、SQLWarning、租约隔离和 `PhysicalStatement` unwrap 已测；SQLx/RBDC Connection warning Adapter 已闭合，原生多结果与其余 Filter statement 事件待补 |
| `DruidPooledPreparedStatement` | DIRECT/ADAPTER | `druid/src/core/druid_pooled_prepared_statement.rs` / `DruidPooledPreparedStatement` + `DruidPooledPreparedStatementHandle` | PARTIAL | 独立逻辑句柄、缓存归还/异常删除、exec/fetch/filter/fetch peak、统一 Wrapper、48 个 JDBC setter 重载、持久参数槽、绑定 query/update/generic execute、有序描述符 batch、顺序多结果、generated keys、getter fatal sorter、继承 SQLWarning/Statement 属性、五属性关闭恢复、共享 ResultSet trace 及 `getStatement()` Prepared 动态身份已迁移；Toasty SQLite 资源执行已接，SQLx/RBDC 资源和原生多数据库 generic/batch 待补 |
| `DruidPooledCallableStatement` | DIRECT/ADAPTER | `druid/src/core/druid_pooled_callable_statement.rs` / `DruidPooledCallableStatement` + 同文件共享身份 `DruidPooledCallableStatementHandle` | PARTIAL | Java 116 个本类公开声明为构造器 + 115 个方法；Rust 本类声明入口及继承结果路径已闭合；统一 Wrapper 和 ResultSet 句柄支持 self、Callable/Prepared 接口、raw concrete、强生命周期与关闭级联；真实存储过程 Adapter 和 vendor 错误矩阵待补 |
| `DruidPooledResultSet` | DIRECT/ADAPTER | `druid/src/core/druid_pooled_result_set.rs` / `DruidPooledResultSet` + `PhysicalResultSet` + `ResultSetStatement` | PARTIAL | fetch/close/峰值、Statement trace、完整 getter/update/typed object、next/close/warning、18 个标量 getter、16 个 decimal/temporal getter、六个 object getter、26 个 stream/resource getter、26 个 navigation/property 调用、两条 NString、getMetaData、getStatement、七个 row-mutation、28 个基础列 update setter、四个 `updateObject` 重载、14 个资源对象 setter、12 个 LOB stream/Reader setter、22 个 ASCII/Binary/Character/NCharacter stream setter 及两个 NString setter 的 FilterChain 已闭合；getStatement 保留三动态身份，row-mutation、基础/object/resource/LOB/stream/NString setter 保留默认末端、短路、参数身份、共享游标与 capability error，vendor custom object 运行时身份可恢复；custom typed object 单次物理读取、默认 SPI 精确能力错误和底层错误优先级已闭合；185 个 ResultSet 调用保留精确委托，ResultSet update setter Filter 子域已闭合；SQL 级 read length/stream/LOB open 已接；嵌套 ResultSet/Clob object 自动代理、streaming、真实 metadata/custom conversion 与多数据库待补 |
| `PoolableWrapper` | DIRECT | `druid/src/core/poolable_wrapper.rs` / `PoolableWrapper` | DONE | Java 空 wrapper/空 iface、自身、底层对象、statement→connection、`WrapperProxy` 委托顺序均有 Java oracle 与 Rust 差分；生产 WrapperProxy 状态合并到各池化门面并由其独立账目追踪 |
| `WrapperAdapter` | DIRECT | `druid/src/core/wrapper_adapter.rs` / `WrapperAdapter` | DONE | Java 空 iface、自身识别/解包、未知类型返回空均已差分验证 |
| `ManagedDataSource` | ADAPTER | `pool/managed_data_source.rs` + `DruidDataSource` | IMPLEMENTED_UNVERIFIED | Java 接口自身的 enable 与 ObjectName 管理合同已迁移；`DataSource` 能力由 Rust `Pool`/facade 提供，JMX `ObjectName` 只在边界表示为可选字符串 |
| `ValidConnectionChecker` | DIRECT | `druid::core::ValidConnectionChecker` | PARTIAL | timeout、driver 特例、错误传播 |
| `ValidConnectionCheckerAdapter` | ADAPTER | `core/valid_connection_checker_adapter.rs` | IMPLEMENTED_UNVERIFIED | 空 query 直接有效、raw `PhysicalConnection` 验证 SQL、第一行判定及超时返回 false 已接；Java `DruidStatementConnection` statement 复用属于 JDBC 宿主细节 |
| `JDBC4ValidConnectionChecker` | ADAPTER | `core/jdbc4_valid_connection_checker.rs` / `Jdbc4ValidConnectionChecker` | IMPLEMENTED_UNVERIFIED | driver `isValid(timeout)` 映射为 `PhysicalConnection::ping` + Tokio timeout |
| `ExceptionSorter` | DIRECT | `druid_core::ExceptionSorter` + `SqlException` | BEHAVIOR_VERIFIED | Java 11 个 vendor/abstract 对象全部独立实现；Java oracle 23/23、Rust 全分支 10/10，真实 SQLite 验证结构化错误进入 fatal discard 主链 |
| `GetConnectionTimeoutException` | MERGE | `DruidError::GetConnectionTimeout { wait_millis, active_count, max_active, creating_count, create_elapsed_millis, create_error_count, running_sql, cause }` | IMPLEMENTED_UNVERIFIED | Druid native timeout 已与 bb8/deadpool 通用 `AcquireTimeout` 分离，Java message 顺序、current createError source、running SQL 及 notFull 完整轮次重试已接；timeout 不误增逻辑 connectErrorCount；统一差分待补 |
| `DataSourceClosedException` | MERGE | `DruidError::DataSourceClosed { close_time_millis }` | IMPLEMENTED_UNVERIFIED | Druid 原生数据源关闭后获取/fill 返回独立错误分类并保留实际关闭时刻；Rust 外部池的通用 `PoolClosed` 不冒充该对象 |
| `DataSourceDisableException` | MERGE | `DruidError::DataSourceDisabled` | IMPLEMENTED_UNVERIFIED | `DruidDataSource` 的 get 门禁与已进入等待后被 disable/close 唤醒的路径已接；Rust 不复制 SQLException 继承层 |
| `DataSourceNotAvailableException` | MERGE | `DruidError::DataSourceNotAvailable { cause }` | IMPLEMENTED_UNVERIFIED | 独立错误类别未混同 `DataSourceNotFound`；pool failFast 主链已按 failContinuous 门禁触发并保留 current createError/source，HA 选择链触发仍待补 |

##### Vendor checker/sorter 对象族

| Java 对象 | 映射 | 目标 | 当前 |
| :--- | :--- | :--- | :--- |
| `MySqlExceptionSorter` | DIRECT | `my_sql_exception_sorter.rs` / `MySqlExceptionSorter` | DONE：recoverable、SQLState 08、19 个 error code、OceanBase -9000..-8000、类名、精确消息和 5 层 cause 已逐项测试 |
| `PGExceptionSorter` | DIRECT/IDIOMATIC | `pg_exception_sorter.rs` / `PgExceptionSorter` | DONE：recoverable 与可空 SQLState 08 规则已逐项测试；Rust 类型按 acronym 惯例命名 |
| `OracleExceptionSorter`、`AbstractOracleExceptionSorter` | DIRECT/COMPOSITION | `oracle_exception_sorter.rs`、`abstract_oracle_exception_sorter.rs` | DONE：抽象父类 Set 状态以组合迁移；构造配置、追加/替换、正负码、49 个固定码、TNS 区间、用户自定义区间和消息矩阵已测试 |
| `DB2ExceptionSorter` | DIRECT/IDIOMATIC | `db2_exception_sorter.rs` / `Db2ExceptionSorter` | DONE：recoverable、SQLState 08 与 8 个 error code 已逐项测试 |
| `InformixExceptionSorter` | DIRECT | `informix_exception_sorter.rs` / `InformixExceptionSorter` | DONE：recoverable 与 17 个 error code 已逐项测试 |
| `SybaseExceptionSorter` | DIRECT | `sybase_exception_sorter.rs` / `SybaseExceptionSorter` | DONE：recoverable、可空 message、JZ0C0/JZ0C1 已逐项测试 |
| `PhoenixExceptionSorter` | DIRECT | `phoenix_exception_sorter.rs` / `PhoenixExceptionSorter` | DONE：大小写敏感精确消息与可空消息安全边界已测试 |
| `NullExceptionSorter` | DIRECT | `null_exception_sorter.rs` / `NullExceptionSorter` | DONE：共享实例、永不 fatal、可空 Properties 均已测试 |
| `OceanBaseOracleExceptionSorter` | DIRECT/COMPOSITION | `ocean_base_oracle_exception_sorter.rs` / `OceanBaseOracleExceptionSorter` | DONE：Oracle 全矩阵 + SQLState 08 + OceanBase 五类消息扩展已测试 |
| `MockExceptionSorter` | DIRECT | `mock_exception_sorter.rs` / `MockExceptionSorter` | DONE：共享实例与 Java `instanceof`（含子类可赋值类型链）已测试 |
| `MySqlValidConnectionChecker`、`OracleValidConnectionChecker`、`PGValidConnectionChecker` | DIRECT/ADAPTER | 各自 checker 文件 | PARTIAL/IMPLEMENTED_UNVERIFIED：默认 SQL、关闭门禁、超时与 MySQL/Oracle properties 已接并由 pool 自动选择；原生 driver ping/reflection 特例和真实数据库验证待最终阶段 |
| `MSSQLValidConnectionChecker`、`OceanBaseValidConnectionChecker` | DIRECT/ADAPTER | 各自 checker 文件 | PARTIAL/IMPLEMENTED_UNVERIFIED：默认 SQL、OceanBase 双模式和 pool 自动选择已接；真实数据库与 vendor 原生特例待验证 |

这些对象不能合并成一个按数据库名称分支的文件后宣称完成；每个数据库的错误码、SQLState、ping SQL 和 timeout 都要独立验收。

#### 4. HA 与 XA

| Java 对象 | 映射 | Rust 落点 | 当前 | 迁移语义 |
| :--- | :--- | :--- | :--- | :--- |
| `HighAvailableDataSource` | DIRECT/SPLIT | `dynamic/high_available_data_source.rs` / `HighAvailableDataSource`；旧 `DynamicDataSource` 为 Rust 读写分离扩展 | PARTIAL/IMPLEMENTED_UNVERIFIED | canonical facade、并发 init、命名 map、外层 blacklist、selector 替换、Java `Ok(None)` 获取、Pool typed unavailable、父配置复制、默认 FileNodeListener/PoolUpdater 启停及原生子池 destroy 已接；ZooKeeper 与统一差分仍缺 |
| `DataSourceCreator` | DIRECT/ADAPTER | `dynamic/data_source_creator.rs` / `DataSourceCreator` | PARTIAL/IMPLEMENTED_UNVERIFIED | 根据 NodeEvent 复制父池容量、超时、校验、驱逐、泄漏、PS cache、Filter 与连接属性；JDBC URL 先归一化，再由内置 Toasty 未池化 Driver SPI 创建 native Druid 子池；非 Toasty vendor 仍需 wrapper factory 注入协议 |
| `PropertiesUtils` | DIRECT/ADAPTER | `dynamic/properties_utils.rs` / `PropertiesUtils` | IMPLEMENTED_UNVERIFIED | `java-properties` 保留续行、Unicode 和转义分隔符；load failure→空集合、`.url` 名称发现、prefix filter 已接；Java classpath fallback 按平台边界不适用 |
| `DataSourceSelector` | DIRECT | `dynamic/selector/data_source_selector.rs` / `DataSourceSelector` | PARTIAL/IMPLEMENTED_UNVERIFIED | get/setTarget/name/init/destroy 契约已独立 |
| `DataSourceSelectorEnum` | DIRECT | `dynamic/selector/data_source_selector_enum.rs` / 同名 enum | IMPLEMENTED_UNVERIFIED | byName/random/stickyRandom 精确名称及 Factory 忽略大小写解析 |
| `NamedDataSourceSelector` | DIRECT/ADAPTER | `dynamic/selector/named_data_source_selector.rs` / 同名对象 | PARTIAL/IMPLEMENTED_UNVERIFIED | 单节点、target/default/null、reset 已接；Java ThreadLocal 映射为 Tokio task ID/非 runtime thread ID，任务终结自动清理待完善 |
| `RandomDataSourceSelector` | DIRECT | `dynamic/selector/random_data_source_selector.rs` / 同名对象 | PARTIAL/IMPLEMENTED_UNVERIFIED | HA/验证双 blacklist、全部被拉黑 fallback、busy 节点仅非全忙时剔除、随机选择与四项配置已接；validate/recover task 待补 |
| `StickyRandomDataSourceSelector` | DIRECT/ADAPTER | `dynamic/selector/sticky_random_data_source_selector.rs` / 同名对象 | PARTIAL/IMPLEMENTED_UNVERIFIED | task 上下文 holder、5 秒过期、blacklist、idle/active 容量检查与随机回退已接；自动上下文清理待补 |
| `StickyDataSourceHolder` | DIRECT/ADAPTER | `dynamic/selector/sticky_data_source_holder.rs` / 同名对象 | IMPLEMENTED_UNVERIFIED | retrievingTime、nullable datasource、isValid 与 setter/getter 已接 |
| `RandomDataSourceRecoverThread` | PROTOCOL/ADAPTER | `dynamic/selector/random_data_source_recover_task.rs` / `RandomDataSourceRecoverTask` | PARTIAL/IMPLEMENTED_UNVERIFIED | 受监管 Tokio 恢复循环、blacklist 快照、pool acquire→sleep→ping→移除、shutdown 已接；Java DruidDataSource validateConnection 具体路径与统一故障测试待补 |
| `RandomDataSourceValidateThread` | PROTOCOL/ADAPTER | `dynamic/selector/random_data_source_validate_task.rs` / `RandomDataSourceValidateTask` | PARTIAL/IMPLEMENTED_UNVERIFIED | 五并发验证、static successTimes、skip 时间窗、wrapping errorCount、动态休眠、threshold blacklist、name cleanup 与 shutdown 已接；raw driver 直连映射为 Pool acquire→ping，properties/raw URL 待补 |
| `RandomDataSourceValidateFilter` | DIRECT | `dynamic/selector/random_data_source_validate_filter.rs` / 同名对象 | IMPLEMENTED_UNVERIFIED | execute/query/update/batch 成功后按 datasource 名记录 successTime，`haRandomValidator` 内置 alias/factory 已接；统一 Filter/HA 时间线待测 |
| `NodeListener`、`NodeEvent`、`NodeEventTypeEnum` | DIRECT/PROTOCOL | `dynamic/node/node_listener.rs`、`node_event.rs`、`node_event_type_enum.rs` | IMPLEMENTED_UNVERIFIED | typed PoolUpdater observer、非空事件更新时间、ADD 先于 DELETE、仅节点集合变化产生事件、密码只显示长度 |
| `ZookeeperNodeListener` | DIRECT/ADAPTER | `dynamic/node/zookeeper_node_listener.rs` / `ZookeeperNodeListener` | PARTIAL/IMPLEMENTED_UNVERIFIED | `zookeeper-client` persistent recursive watch + 直接子节点 cache 映射 Curator PathChildrenCache；ADD/DELETE、忽略 UPDATE、断连后重连全量 rebuild、URL 三种占位符与 private/external client 生命周期已接；真实 ZooKeeper 集群差分待最终阶段 |
| `ZookeeperNodeInfo` | DIRECT | `dynamic/node/zookeeper_node_info.rs` / `ZookeeperNodeInfo` | IMPLEMENTED_UNVERIFIED | prefix 非空时补点号，host/port/database/username/password nullable 字段完整 |
| `ZookeeperNodeRegister` | DIRECT/ADAPTER | `dynamic/node/zookeeper_node_register.rs` / `ZookeeperNodeRegister` | PARTIAL/IMPLEMENTED_UNVERIFIED | persistent parent mkdir、单 member 门禁、Java properties payload、ephemeral member、deregister 与 private/external client 所有权已接；Curator GroupMember 的连接恢复由原生 client session 代替，真实集群证据待补 |
| `FileNodeListener` | DIRECT/ADAPTER | `dynamic/node/file_node_listener.rs` / `FileNodeListener` | IMPLEMENTED_UNVERIFIED | properties 过滤、空 URL 忽略、非空差分才替换快照、启动同步刷新、定时 try-lock 跳过与受监管 shutdown 已接 |
| `PoolUpdater` | DIRECT/ADAPTER | `dynamic/node/pool_updater.rs` / `PoolUpdater` | IMPLEMENTED_UNVERIFIED | ADD 创建/重复 ADD 取消摘除、DELETE 两阶段 blacklist、禁止删除最后节点、active>0 延迟、关闭失败仍摘除、周期任务和锁串行化已接 |
| `DruidXADataSource` | ADAPTER | `DruidXaDataSource` | MISSING | XA 数据源 |
| `DruidPooledXAConnection` | ADAPTER | `DruidPooledXaConnection` | MISSING | XA 连接生命周期 |
| `JtdsXAConnection`、`JtdsXAResource` | ADAPTER | JTDS capability adapter | MISSING | XAResource 状态机 |

#### 5. Filter 与 Proxy

| Java 对象 | 映射 | Rust 落点 | 当前 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| `Filter` | SPLIT | `BeforeFilter` + `AfterFilter` + `ExtendedFilter` + `ResultSetFilter` | PARTIAL | ResultSet 已闭合 185 个精确调用（含 `updateObject` 四重载、资源对象 setter 14 重载、LOB stream/Reader setter 12 重载、ASCII/Binary/Character/NCharacter stream setter 22 重载及 NString setter 两重载），ResultSet update setter Filter 子域闭合；其余源 hook仍须继续逐项映射 |
| `FilterChain` | DIRECT | `core/filter_chain.rs` 的 `FilterChain` 接口兼容名 | PARTIAL | 直接指向 canonical `FilterChainImpl`，不复制第二套状态；SQL before/after、connection commit/rollback、Statement create/prepare/call 成功事件与 ResultSet 已迁移调用族接入生产链 |
| `FilterChainImpl` | DIRECT | `core/filter_chain_impl.rs` / `FilterChainImpl` | PARTIAL/IMPLEMENTED | canonical 对象、注册顺序/类名去重、init/destroy、before/逆序 after、connection/statement warning position chain 和已迁移 ResultSet 185 方法族已落位；Java 全部 498 个 public/protected 方法及单一 position 对所有对象族的动态分派尚未闭合 |
| `FilterAdapter` | DIRECT | `druid/src/core/filter_adapter.rs` / `FilterAdapter` | PARTIAL | Java 491 个公开方法；独立对象已承载 init/destroy/config 空语义、精确自身 Wrapper、SQL before/after 及当前 185 个 ResultSet hook 的默认继续链，并通过真实 Toasty SQLite；未迁移的 Callable/Connection/Statement/Clob/DataSource/metadata 精确 hook 随 Filter/FilterChain 账目保持开放 |
| `FilterEventAdapter` | DIRECT | `druid/src/core/filter_event_adapter.rs` / `FilterEventAdapter` | PARTIAL | Java 541 行，含 32 个公开入口与 13 个模板 hook；物理 connect 已覆盖直接/后台 creator 的 before→factory→真实 ID→reverse after，dataSource get 已为可修改 maxWait/短路的有位置链；独立对象及组合式 `FilterEventListener` 已覆盖 create/prepare/call 成功反向事件、execute/query/update/batch before/after、错误后置替换和 ResultSet open 顺序，并由真实 Toasty SQLite 验证。Rust `ExecContext` 尚不能传递 Java `StatementProxy` 实例身份，dataSource 仅暴露名称/状态协议，later/downstream before 失败也未按 Java 已进入 adapter 范围逐层展开；Java 45 分支错误测试矩阵尚未完整镜像，因此不得标 DONE |
| `FilterManager` | DIRECT | `druid/src/core/filter_manager.rs` / `FilterManager` + `resources/META-INF/druid-filter.properties` | PARTIAL | bundled alias、显式 property sources、UTF-16 回退、逗号展开、类名去重、缺失类继续及构造失败错误已迁移；`DruidPoolBuilder#setFilters`、Factory properties、canonical Filter 工厂和 inventory 自动 provider 均已接线。JVM 三 ClassLoader 自动资源扫描及全部 alias 对象行为仍开放 |
| `AutoLoad` | PROTOCOL/ADAPTER | `core/auto_load.rs` / `AutoLoad` + inventory | IMPLEMENTED_UNVERIFIED | 保留 `value`、稳定类名和 provider 注册；Rust 显式 order 映射 Java provider-file 顺序，同 order 按类名确定化；显式 Filter init 后追加 auto provider 及 `druid.loadSpifilterSkip` 已接生产 builder，跨 crate provider 统一验证待做 |
| `StatFilter` | DIRECT | `druid_stats::StatFilter` | PARTIAL | SQL execute/batch、逐项 update count、活动事务、ResultSet、pool get/release 和真实物理 connect/close 已接；建连沿有位置链包围 raw driver 并负责 before/error/after/Entry，关闭在 raw close 前记录；连接属性及 Stat system properties 配置生命周期进入 Factory，merge/slow threshold/log level/connection stack 管理面已落位；慢参数文本和 SQL dialect parameterize 仍缺 |
| `StatFilterMBean` | PROTOCOL/MERGED | `stats/stat_filter.rs` 的线程安全管理方法 | IMPLEMENTED_UNVERIFIED | Rust 无 JMX；mergeSql、logSlowSql、slowSqlMillis 的 getter/setter 与 mergeSql 操作合并进 canonical `StatFilter`，不制造空 MBean trait；动态配置与慢 SQL tracing 统一验证待做 |
| `MergeStatFilter` | DIRECT | `stats/merge_stat_filter.rs` | IMPLEMENTED_UNVERIFIED | 独立对象强制 `merge_sql=true`，完整复用 `StatFilter` 的 execute/batch/connection/result-set 行为；普通 `StatFilter` 默认保留原 SQL，`mergeStat` alias 已装配，统一验证待最终阶段 |
| `StatFilterContext` | DIRECT | `druid/src/stats/stat_filter_context.rs` | PASS/PARTIAL | 进程单例、COW 写入、下标动态分发、重复注册/首次删除与异常短路已迁移；SQL execute/update、活动事务 commit/rollback 及 ResultSet 事件族已接生产链 |
| `StatFilterContextListener` | DIRECT | `druid/src/stats/stat_filter_context_listener.rs` | PASS | 14 个方法、可空 error 与 listener 异常传播完整保留 |
| `StatFilterContextListenerAdapter` | DIRECT | `druid/src/stats/stat_filter_context_listener_adapter.rs` | PASS/BEHAVIOR_VERIFIED | 独立具体对象显式实现 14 个无操作回调；`new/default/copy/clone/debug` 及所有回调已测，不以 Listener trait 默认方法冒充 |
| `StatFilterContextListener/Adapter` | SPLIT/DIRECT | 上述 `StatFilterContextListener` + `StatFilterContextListenerAdapter` 两个独立对象 | PASS | 承接原迁移总账的合并名称；Rust 按 Java 的接口与 Adapter 两个对象拆分，未合并实现或遗漏语义 |
| `LogFilter`、`LogFilterMBean` | DIRECT/MERGED | `core/log_filter.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | Java 默认开关、七个 properties 键、四个事件分类标签、连接/Statement/ResultSet 开关、SQL format option、管理方法和 tracing 事件已落位；SQL/batch/Statement 创建关闭/connection 事务、pool 获取归还、真实物理建连关闭及 ResultSet proxy ID 已接；physical connect/close 沿 before Filter 有位置链执行，成功事件只在下游成功后记录；Rust 无 JMX，Java 日志后端对象不迁移；完整列值和参数插值格式尚缺 |
| `Slf4jLogFilter`、`Log4jFilter`、`Log4j2Filter`、`CommonsLogFilter` | NOT_APPLICABLE | 无 Rust 运行时对象 | NOT_APPLICABLE | 这些是 Java 日志后端选择类；只在配置迁移文档中说明 `log4j/log4j2/slf4j/commonLogging → log`，不注册同名类型或 alias |
| `Log4jFilterMBean`、`Log4j2FilterMBean`、`CommonsLogFilterMBean` | NOT_APPLICABLE | 无独立 Rust 对象 | NOT_APPLICABLE | Java 日志实现及 JMX 空扩展接口都不属于 Rust 产品模型 |
| `ConfigFilter` | ADAPTER | `core/filter/config/config_filter.rs` / `ConfigFilter` | PARTIAL/IMPLEMENTED_UNVERIFIED | connection→配置文件→system 解密开关、配置文件→connection→system 公钥、配置文件→connection→数据源密码的优先级已落位；支持 Java properties/XML、文件/classpath/HTTP(S)、原错误前缀、显式 classpath roots 与 rustls-backed client，并在 `DruidDataSourceFactory` 构造 driver 前解析；HTTP/XML/系统属性与外部 driver 完整行为验证待统一阶段 |
| `ConfigTools` | DIRECT | `core/filter/config/config_tools.rs` / `ConfigTools` | IMPLEMENTED_UNVERIFIED | 默认 512-bit 历史密钥、PKCS#8 私钥、X.509/SPKI 公钥、Base64、keypair、文件/证书公钥读取及 Java 非典型 private-encrypt/public-recover PKCS#1 v1.5 type-1 兼容已实现；仅作旧密文兼容并标 deprecated，Java/Rust 向量尚待统一差分 |
| `EncodingConvertFilter`、`CharsetConvert`、`CharsetParameter` | ADAPTER | `core/filter/encoding/{encoding_convert_filter,charset_convert,charset_parameter}.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | 三个 canonical 对象已独立落位，保留 Java replacement 转码以及 ResultSet 字符串/object/type-map/Reader 结果转换；SQL、Prepared/Callable 参数改写尚未进入可变生产链 |
| `MySQL8DateTimeSqlTypeFilter`、`MySQL8DateTimeResultSetMetaData` | ADAPTER | `core/filter/mysql8datetime/{my_sql8_date_time_sql_type_filter,my_sql8_date_time_result_set_meta_data}.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | metadata 仅将 `LocalDateTime` 映射为 `Timestamp`，其余字段委托；Rust 已以 `Value::Timestamp` 表达旧 JDBC 值语义，`mysql8DateTime` alias 已进入生产装配，真实 MySQL 8 fixture 待补 |
| `ConnectionProxy/Impl` | MERGED/ADAPTER | `DruidPooledConnection` + `proxy_attributes.rs` + `transaction_info.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | ID、attributes、raw/unwrap、connectedTime、closeCount、lastValidateTime、transactionInfo、逻辑驱动 properties 和连接事件身份已接；Adapter vendor properties 实际应用仍缺 |
| `StatementProxy/Impl` | MERGED/ADAPTER | `DruidPooledStatement` | PARTIAL/IMPLEMENTED_UNVERIFIED | ID、attributes、执行状态、Filter 执行/创建/关闭身份已接；完整 getter 与统一差分仍缺 |
| `PreparedStatementProxy/Impl` | MERGED/ADAPTER | `DruidPooledPreparedStatement` | PARTIAL/IMPLEMENTED_UNVERIFIED | 参数记录、JdbcParameter 0-based 视图、继承身份和状态已接 |
| `CallableStatementProxy/Impl` | MERGED/ADAPTER | `DruidPooledCallableStatement` | PARTIAL/IMPLEMENTED_UNVERIFIED | callable 参数、继承身份和状态已接；真实 driver 待补 |
| `ResultSetProxy/Impl` | MERGED/ADAPTER | `DruidPooledResultSet` + `ResultSetFilterContext` | PARTIAL/IMPLEMENTED_UNVERIFIED | ID、attributes、construct/fetch/close/read/open、双向列映射、hidden columns 与 Filter 三层身份已接；Wall 自动生产待补 |
| `DatabaseMetaDataProxyImpl` | ADAPTER | `core/database_meta_data_proxy_impl.rs` + `physical_database_meta_data.rs` + pooled connection/Filter 生产入口 | PARTIAL/IMPLEMENTED_UNVERIFIED | Java 176 个公开声明中 173 个直接方法已形成完整 SPI/Deref 委托，getConnection 映射稳定 connection ID，三个源实现历史错误委托原样保留；Toasty/SQLx/RBDC 已接；SQLx SQLite 已覆盖 160/173 并真实实现 table/column/PK/index/FK/type-info；Connection metadata around-chain 和 Wall 开关已接。13 个平台/driver 边界、Wall privileged scope、Any vendor 覆盖与统一差分仍缺；`raw/raw_mut` 替代借用型 SPI unwrap，具体 Java driver class downcast 为 JAVA_ONLY_EXEMPT；六字段 MetaData DTO 不计入本对象 |
| `ResultSetMetaDataProxy` | DIRECT/PROTOCOL | `core/result_set_meta_data_proxy.rs` | IMPLEMENTED_UNVERIFIED | metadata ID/raw/所属 ResultSet 合同；Rust 用 ResultSet ID 替代自引用强指针 |
| `ResultSetMetaDataProxyImpl` | DIRECT | `core/result_set_meta_data_proxy_impl.rs` + `DruidPooledResultSet#meta_data_proxy` | PARTIAL/IMPLEMENTED_UNVERIFIED | 80000 起 metadata ID、ResultSet ID、attributes、raw unwrap 和已有 21 列描述方法已接；metadata 专用 Filter around-chain 与统一差分待补 |
| `ClobProxy/Impl`、`NClobProxy/Impl` | ADAPTER | LOB wrappers | MISSING | LOB 生命周期与事件 |
| `DataSourceProxy` | MERGED/PROTOCOL | `pool/data_source_proxy.rs` + `DruidDataSource` | IMPLEMENTED_UNVERIFIED | canonical 数据源实现统计、管理 ID、name/dbType/raw factory、URL/Filter/properties 与五组 ID 合同，不新建第二个池 |
| `DataSourceProxyImpl` | MERGED | `DruidDataSource` + `DruidPool` + Filter/Stats | PARTIAL/IMPLEMENTED_UNVERIFIED | connect/统计/ID 主状态合并；driver version、脱敏 properties 文本与完整 composite 管理字段待补 |
| `DataSourceProxyConfig` | ADAPTER | `DruidPoolBuilder` / `PoolInnerConfig` | PARTIAL | name/raw URL/URL/driver/Filter 已分布接入；JMX、Name/Password callback 可变配置仍缺 |
| `WrapperProxy/Impl` | MERGED | `Wrapper` + `ProxyAttributes` + 各池化门面 | PARTIAL/IMPLEMENTED_UNVERIFIED | id/raw/unwrap/惰性 attributes 已接；各具体 Proxy 剩余字段按对象行追踪 |
| `JdbcParameter` / 内部 `TYPE` | DIRECT | `core/jdbc_parameter.rs` | IMPLEMENTED_UNVERIFIED | trait 保留 value/length/calendar/sqlType；内部常量同文件 |
| `JdbcParameterImpl` | DIRECT | `core/jdbc_parameter_impl.rs` | IMPLEMENTED_UNVERIFIED | 完整构造、默认 -1、Calendar 与 scaleOrLength |
| `JdbcParameterInt` | DIRECT | `core/jdbc_parameter_int.rs` | IMPLEMENTED_UNVERIFIED | INTEGER 值语义；Java 0..126 identity cache 映射为 Rust 值类型 |
| `JdbcParameterLong` | DIRECT | `core/jdbc_parameter_long.rs` | IMPLEMENTED_UNVERIFIED | BIGINT 值语义；Java 0..126 identity cache 映射为 Rust 值类型 |
| `JdbcParameterString` | DIRECT | `core/jdbc_parameter_string.rs` | IMPLEMENTED_UNVERIFIED | nullable/empty/VARCHAR |
| `JdbcParameterDecimal` | DIRECT | `core/jdbc_parameter_decimal.rs` | IMPLEMENTED_UNVERIFIED | nullable/0/10/DECIMAL 值语义 |
| `JdbcParameterDate` | DIRECT | `core/jdbc_parameter_date.rs` | IMPLEMENTED_UNVERIFIED | nullable DATE |
| `JdbcParameterTimestamp` | DIRECT | `core/jdbc_parameter_timestamp.rs` | IMPLEMENTED_UNVERIFIED | nullable TIMESTAMP |
| `JdbcParameterNull` | DIRECT | `core/jdbc_parameter_null.rs` | IMPLEMENTED_UNVERIFIED | typed NULL 与 TINYINT→INTEGER 历史分支 |
| `TransactionInfo` | DIRECT | `core/transaction_info.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | 数据源级 60000 起始 ID、起止 epoch、首次结束、显式覆盖和最多十条 SQL 已接活动事务；Java proxy 兼容子类合并，不创建空子类型；统一差分待补 |
| `StatementExecuteType` | DIRECT | `core/statement_execute_type.rs` / `StatementExecuteType` | IMPLEMENTED_UNVERIFIED | 独立保留 execute/query/update/batch 最近入口类型；`StatementType` 继续只表达普通/Prepared/Callable 对象种类，二者不再混淆 |

#### 6. SQL 对象域

SQL 的 1,268 个对象必须迁移语义，不能整体标记为“由 sqlparser-rs 替代”。对象登记按以下层级展开：公共 facade 保留 Druid 名称；大量 AST 节点允许映射到 Rust enum variant，但每个 Java 节点仍是一条账目。

| Java 对象/对象族 | 映射 | Rust 目标 | 当前 | 验收焦点 |
| :--- | :--- | :--- | :--- | :--- |
| `DbType` | DIRECT | `sql/db_type.rs` / `DbType` | PARTIAL/IMPLEMENTED_UNVERIFIED | Java 严格 `of`、两项忽略大小写别名、全部真实数据库名称、deprecated AntSpark、0..58 mask/合并、hashCode64、equals(String) 与 PostgreSQL-style 已迁移；JVM 专属 log4jdbc variant 按平台边界 NOT_APPLICABLE，URL 迁移器仍归一化其真实 vendor；全枚举差分待最终阶段 |
| `SQLUtils` | DIRECT | `sql/sql_utils.rs` / `SqlUtils` | PARTIAL/IMPLEMENTED_UNVERIFIED | parseStatements、single statement、format、toSQLString、parameterize 与 DbType→sqlparser dialect 已接；Java 全方法、feature/comment、visitor 输出和方言精确差分待补 |
| `JdbcUtils` | SPLIT | `sql/jdbc_utils.rs` / `JdbcUtils` + driver adapters | PARTIAL/IMPLEMENTED_UNVERIFIED | Java case-sensitive getDbTypeRaw/getDbType、URL 分支顺序、getTypeName、四类 db-family 与 MySQL class 判定已落；Rust DSN/driver identity 由独立 infer_db_type 承载；RAII 替代 close、adapter 替代 ClassLoader。print/execute/showTables/DDL script 与统一差分仍缺 |
| Lexer、`Token`、`Keywords` | ADAPTER | `DruidLexerAdapter` + token compatibility | MISSING | token、位置、注释、错误 |
| `SQLParserFeature` | DIRECT | `SqlParserFeature` | MISSING | feature 开关 |
| `SQLParserUtils` | DIRECT | `SqlParserUtils` | MISSING | 方言 parser 创建 |
| `SQLStatementParser` 及方言 parser | ADAPTER | parser facade + sqlparser 扩展/fork | MISSING | 全输入消费、多语句、方言 |
| `SQLExprParser` 及方言 expr parser | ADAPTER | expr parser facade | MISSING | 类型、优先级、方言表达式 |
| `SQLObject`、`SQLObjectImpl` | ADAPTER | `SqlObject` trait + metadata store | MISSING | parent、attributes、comments、clone |
| `SQLStatement`、`SQLExpr` | ADAPTER | compatibility traits/enums | MISSING | Visitor、类型判断、序列化 |
| `sql/ast/*` 67 个对象 | MERGE/DIRECT | compatibility AST | MISSING | 每对象登记 |
| `sql/ast/expr/*` 72 个对象 | MERGE | `DruidExpr` variants | MISSING | 每 variant 对应源对象 |
| `sql/ast/statement/*` 283 个对象 | MERGE | `DruidStatement` variants | MISSING | 每 variant 对应源对象 |
| `SQLASTVisitor`、`SQLASTVisitorAdapter` | DIRECT | Visitor trait/adapter | MISSING | visit/endVisit 顺序 |
| 各方言 Visitor | DIRECT/ADAPTER | 方言 visitor 模块 | MISSING | 方言节点覆盖 |
| `SQLASTOutputVisitor` 及方言输出 visitor | ADAPTER | formatter compatibility layer | MISSING | 大小写、空白、hint、参数 |
| `SQLASTParameterizedVisitor` | ADAPTER | parameterizer visitor | MISSING | 参数列表、merge、replacement count |
| `SchemaRepository` 与 repository 对象 | DIRECT | schema repository 模块 | MISSING | resolve、DDL、表列谱系 |
| `SQLTransformUtils`、builder 对象 | DIRECT | transform/builder 模块 | MISSING | AST rewrite |
| `PagerUtils` | DIRECT | `PagerUtils` | MISSING | count/limit 方言输出 |
| dialect/mysql 214 个文件 | MIXED | MySQL compatibility module | MISSING | 全量对象账本 + fixture |
| dialect/oracle 116 个文件 | MIXED | Oracle compatibility module | MISSING | 同上 |
| dialect/postgresql 50 个文件 | MIXED | PostgreSQL compatibility module | MISSING | 同上 |
| dialect/sqlserver 25 个文件 | MIXED | SQL Server compatibility module | MISSING | 同上 |
| dialect/db2、h2、clickhouse、bigquery 等 | MIXED | 各方言模块 | MISSING | 禁止 GenericDialect 静默降级 |

对象级展开规则：P4 开始前从 CodeGraph 导出每个 Java 类型，分配稳定 `OBJ-SQL-*` ID，并逐条绑定 Rust enum variant/type、fixture 和状态。对象族完成率只有在族内全部对象关闭后才可标 `DONE`。

#### 7. Wall

| Java 对象 | 映射 | Rust 落点 | 当前 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| `WallConfig` | DIRECT | `druid_sql::WallConfig` | PARTIAL/IMPLEMENTED_UNVERIFIED | updateCheckColumns 的 table.column 解析、规范化、有序首列与运行期线程安全 handler 替换已接；其余字段存在不等于全部规则生效 |
| `WallProvider` | DIRECT | canonical `WallProvider`；现 `Wall` | RENAME/PARTIAL | cache、white/black list、stats |
| `WallFilter` | DIRECT | `sql/wall_filter.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | 独立对象实现 Before/After/ResultSet Filter，并由内置 FilterManager 工厂装配；SELECT/UPDATE/INSERT AST 改写已进入普通 Statement 物理调用/addBatch 与 Prepared/Callable prepare/cache key，且按 raw metadata 生产隐藏列、成功 next 后回调 tenant 值；完整 Java hook/配置和方言规则未闭合 |
| `WallVisitor`、`WallVisitorBase` | ADAPTER | visitor compatibility | MISSING | 完整规则遍历 |
| `WallVisitorUtils` | DIRECT | `wall_visitor_utils.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | 独立对象已迁移 active tenant SELECT 投影、UPDATE 赋值、INSERT values/select/union 改写和 generateTenantValue；其余公共检查算法仍缺 |
| `WallCheckResult` | DIRECT | `sql/wall_check_result.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | SQL、violation 与表/函数统计快照已接；sqlStat 为 Option，精确保留 privileged Java null；Java statement AST 身份和完整统计对象仍缺 |
| `Violation`、`IllegalSQLObjectViolation`、`SyntaxErrorViolation` | MERGE | `WallViolation` enum | PARTIAL | variant/错误码/消息需对齐 |
| `WallContext` | ADAPTER | `sql/wall_context.rs` 的同步 thread-local context | PARTIAL/IMPLEMENTED_UNVERIFIED | Java `ThreadLocal` 生命周期、DbType、SQL/表/函数统计、comment/update-none/delete-none/like-number warning 与 update 检查项容器已接生产；union/其余 visitor warning 和并发差分待补 |
| `WallUpdateCheckHandler` | DIRECT | `sql/wall_update_check_handler.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | 独立线程安全 trait、运行期替换、字面量即时检查与 Prepared 参数执行前检查已接；真实 SQLite PreparedStatement 与 Java live diff 待统一验证 |
| `WallUpdateCheckItem` | DIRECT | `sql/wall_update_check_item.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | table/column/value/filterValues、Java 1-based 占位符身份、字面量去重和绑定值求值已迁移并由 UPDATE AST 生产；复杂表达式/方言差分待补 |
| `WallProviderCreator` | ADAPTER | inventory provider registry | MISSING | order 与动态发现 |
| 各数据库 `*WallProvider` | DIRECT | 各自 provider 文件 | MISSING | 默认 config 目录和方言 |
| 各数据库 `*WallVisitor` | DIRECT | 各自 visitor 文件 | MISSING | 方言规则 |
| `WallSqlStat`、`WallTableStat`、`WallFunctionStat`、`WallDenyStat` 及 Value 对象 | DIRECT | `druid/src/sql` 内各独立对象文件 | PARTIAL/IMPLEMENTED_UNVERIFIED | 核心原子计数、快照、直方图与 reset 已接；完整 Java 字段和并发差分待最终验证 |
| `WallProviderStatLogger/Adapter/Impl` | PROTOCOL/MERGED | tracing/metrics/用户 sink 协议 | PARTIAL | 只迁移周期发布、结构化字段和错误隔离语义；Java logger SPI、便利基类和 SLF4J 实现类不形成 Rust 对象 |

#### 8. Stat、Support 与管理面

| Java 对象/对象族 | 映射 | Rust 落点 | 当前 | 迁移结果 |
| :--- | :--- | :--- | :--- | :--- |
| `JdbcSqlStat` | DIRECT | `stats/jdbc_sql_stat.rs`；`MergedSqlStat` 仅为旧别名 | MATCH/PARTIAL/IMPLEMENTED_UNVERIFIED | update/fetch 行数生产接线已完成；完整字段与并发差分待补 |
| `JdbcSqlStatValue` | DIRECT | `stats/jdbc_sql_stat_value.rs` | IMPLEMENTED_UNVERIFIED | Java `getData()` 41 键已全部建模并接入可用生产值；JVM Throwable stack、JdbcStatContext file 与 Java 固定 null 字段保持 nullable；逐字段 Java JSON golden/live diff 待统一验证 |
| `JdbcDataSourceStat` | DIRECT | `stats/jdbc_data_source_stat.rs` / `JdbcDataSourceStat`；`StatsCollector` 仅兼容 alias | PARTIAL | 数据源统计、分层对象、独立 reset gate 已接；完整字段待补 |
| `JdbcConnectionStat`、`JdbcStatementStat` | DIRECT | `stats/jdbc_connection_stat.rs`、`stats/jdbc_statement_stat.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | 两个独立对象及生产计数已接；JMX 类型不迁移，连接/语句隐式清理和最终并发差分待补 |
| `JdbcResultSetStat` | DIRECT | `druid/src/stats/jdbc_result_set_stat.rs` | PARTIAL | 对象字段、原子更新、错误/时间/reset 及 StatFilter→ResultSet open/close/fetch/hold 已迁移；SQL 级与管理面待补 |
| `JdbcStatManager`、`DruidDataSourceStatManager`、`DruidStatManagerFacade` | DIRECT/SPLIT | `stats/jdbc_stat_manager.rs` + `druid_data_source_stat_manager.rs` + `druid_stat_manager_facade.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | Jdbc 全局 Connection/Statement/ResultSet、1001 起 SQL ID、分层 reset 顺序；数据源显式 Arc 注册/注销/查询/reset/logStats；三个 resetCount 独立保存且空注册表调用仍计数。Java ThreadLocal Context、Spring/Web 分支、JMX/OpenType 和完整聚合仍缺 |
| `JdbcStatContext` | DIRECT | `stats/jdbc_stat_context.rs` / `JdbcStatContext` | IMPLEMENTED_UNVERIFIED | name/file/sql/requestId/traceEnable nullable bean 已独立；JdbcStatManager 以 Tokio task ID、非 runtime thread ID 映射 Java ThreadLocal，并接入 StatFilter SQL override、name/file 和 statement close 清理 |
| `JdbcTraceManager`、`JdbcTraceManagerMBean` | DIRECT/NOT_APPLICABLE | `stats/jdbc_trace_manager.rs` / deprecated `JdbcTraceManager`；无 MBean | IMPLEMENTED_UNVERIFIED/NOT_APPLICABLE | Java 类唯一行为是稳定 singleton；空 MBean/JMX 宿主不形成 Rust 对象，也不引入日志框架 |
| 所有 `*MBean` | NOT_APPLICABLE/PROTOCOL | OpenMetrics descriptor + admin operation | NOT_APPLICABLE | Java JMX 对象本身不迁移；只对仍有产品意义的属性、操作、权限建立管理协议台账 |
| `DruidStatService` | PROTOCOL | `stats/druid_stat_service.rs`，由 admin Axum 边界承载 HTTP | PARTIAL/IMPLEMENTED_UNVERIFIED | 核心 JSON 路由、Java 风格 query 解析、MapComparator/分页、SQL detail、reset-all 与独立 log-and-reset 已接；locale Collator、parsed schema、非法分页异常、web/spring 分支、鉴权宿主和逐字段 golden 未闭合 |
| `StatViewServlet`、`ResourceServlet` | PROTOCOL | Axum Router/static service | MISSING | 页面、资源、认证、IP |
| `WebStatFilter` | PROTOCOL | Tower middleware | MISSING | URI/session/request 统计 |
| Spring/Jakarta starter 对象 | PROTOCOL | config loader + Tower/Axum integration | MISSING | 配置绑定和自动装配结果 |
| iBatis/MyBatis support 对象 | ADAPTER | ORM instrumentation adapter | MISSING | SQL 识别和统计 |
| logging support 对象 | NOT_APPLICABLE/PROTOCOL | tracing subscriber/layer | NOT_APPLICABLE | Java 日志门面/实现对象不迁移；只迁移 Druid 事件级别、字段、过滤与脱敏语义；原生字段使用 category/target，Java logger name 仅作 deprecated 配置入口 |
| 三代 starter 共 26 个 Java 对象 | PROTOCOL | features/config presets | MISSING | 版本化配置兼容 |

#### 9. Rust 当前对象与来源

| Rust 对象 | 来源语义 | 分类 | 处置 |
| :--- | :--- | :--- | :--- |
| `DruidPool`、`PoolInner`、`PoolInnerConfig` | `DruidDataSource` 内部池引擎 | RUST_ONLY/SPLIT | 保留为内部实现，增加 canonical facade |
| `ConnectionCloseWorker` | Java recycle/discard 同步物理 close + Rust async Drop 义务 | RUST_ONLY/SPLIT | 每池唯一受监管 worker；Drop 只入队，pool 保存 JoinHandle，closed 且 active=0 后 FIFO 退出 |
| `PoolConfig`、`PoolConfigBuilder`、`DruidPoolBuilder` | `DruidAbstractDataSource` 配置 | RUST_ONLY/SPLIT | 去重并绑定配置字段契约 |
| `PhysicalConnection`（旧 `Connection` 仅兼容重导出） | JDBC Connection 平台能力 | RUST_ONLY/ADAPTER | 已收窄为内部最小 SPI；不得作为第二个公开池化连接 |
| `PhysicalCallableStatement` | JDBC CallableStatement 平台能力 | RUST_ONLY/ADAPTER | 独立于 pooled wrapper 的最小驱动句柄 SPI；不支持的 Adapter 明确报错 |
| `CallableInputParameter`、`JdbcObject`（兼容 alias `CallableOutputValue`）、`JdbcCalendar`（兼容 alias `CallableCalendar`）、`CallableParameter`、`CallableOutParameter` | CallableStatement setter/返回值、Calendar、索引/名称与 OUT 注册描述 | RUST_ONLY/ADAPTER | 通用 JDBC 值与 Calendar 使用 canonical `Jdbc*` 名称；保留 setter variant、返回类型、Calendar 重载、index/name、sqlType、scale、typeName、LOB/Reader/null/精确长度重载，不计入 Druid Java 对象分母 |
| `JdbcBlob`、`PhysicalBlob`、`JdbcInputStream`、`JdbcOutputStream` | JDBC Blob 与 Java IO 二进制资源 | RUST_ONLY/ADAPTER | 完整资源句柄边界；不冒充 Druid `BlobProxy` 对象 |
| `JdbcClob`、`PhysicalClob`、`JdbcNClob`、`PhysicalNClob`、`JdbcReader`、`JdbcWriter`、`JavaString` | JDBC Clob/NClob、Java 字符 IO 与 String 平台资源 | RUST_ONLY/ADAPTER | Clob 13 方法、NClob 类型身份、UTF-16 与资源生命周期完整分账；不冒充 Druid Proxy |
| 当前 `Driver`、`ConnectionFactory` | JDBC Driver/driver manager 平台能力 | RUST_ONLY/ADAPTER | 收敛为 `PhysicalConnectionFactory` + registry，仅用于 native direct adapter |
| `ConnectionProvider` | `DruidDataSource.getConnection*` 的连接来源选择 | RUST_ONLY/SPLIT | native/bridge 统一入口；一次 acquire 只能选择一个 Provider |
| `DruidPooledConnection` 内部 returner、`PhysicalConnectionLease` | `DruidPooledConnection.close` + `DruidDataSource.recycle` | RUST_ONLY/SPLIT | 唯一所有权与 exactly-once return；Drop 失败进入 `ConnectionCloseWorker` 受监管回收 |
| `DruidConnectionHolder` | Java 同名 holder | DIRECT/SPLIT | native idle queue 与 pooled facade 之间移动同一个对象；物理连接与生命周期元数据不再拆成 `IdleConn` 两份状态 |
| `ConnectionDefaults` | `DruidConnectionHolder` 默认状态字段与 `reset()` | RUST_ONLY/SPLIT | 首次入池捕获；按 Java 顺序复位；已作为 canonical holder 的内部值对象 |
| `ConnectionRecycleDisposition` | `DruidDataSource.recycle` 的 putLast/discard/recycle-error 分支 | RUST_ONLY/SPLIT | 只表达最终处置，不替代 datasource 回收状态机 |
| `NativePoolProvider` | Druid native holder/idle queue | RUST_ONLY/SPLIT | 使用 `PhysicalConnectionFactory` 创建，完整迁移池生命周期 |
| `SqlxBb8Pool`、`SqlxDeadpoolPool`、`PhysicalConnectionLease` | 外部池 wrapper/兼容结果 | RUST_ONLY/ADAPTER | 借用外部 lease，归还外部池，不进入 Druid idle queue |
| `BeforeFilter`、`AfterFilter`、`ExtendedFilter` | `Filter` | SPLIT | 用 hook 清单证明无语义丢失 |
| `ExecContext` 与事件枚举 | Proxy/Filter 调用上下文 | RUST_ONLY | 补齐身份、参数、结果、错误和 parent span |
| `Wall` | `WallProvider` 简化实现 | RENAME/PARTIAL | canonical `WallProvider` 已存在；继续补完整方言 provider/visitor 与规则 |
| `ParameterizedSql`、`SqlMerger` | SQL 参数化 + `JdbcSqlStat` key | RUST_ONLY/MERGE | P4 后基于兼容 AST 重做 |
| `StatsCollector` | 多个 `Jdbc*Stat` | RENAME/MERGE | 拆出 canonical 统计对象 |
| `DynamicDataSource`、`DataSourceGroup`、`LoadBalancer` | HA 数据源 | RUST_ONLY/SPLIT | 增加 canonical HA facade/selector |

#### 10. Wall 对象明细（恢复原表粒度）

##### 10.1 Wall 统计对象

| Java 对象 | 映射 | 目标 Rust 文件 | 当前 | 修订说明 |
| :--- | :--- | :--- | :--- | :--- |
| `WallSqlStat` | DIRECT | `sql/wall_sql_stat.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | SQL、violation、表/函数解析结果与执行/错误/fetch/update 原子统计已接；完整 Java 字段待补 |
| `WallSqlStatValue` | DIRECT | `sql/wall_sql_stat_value.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | 独立不可变快照和稀疏管理 map 已接；完整字段待补 |
| `WallSqlFunctionStat` | DIRECT | `sql/wall_sql_function_stat.rs` | IMPLEMENTED_UNVERIFIED | SQL 内函数调用次数值对象已接 |
| `WallSqlTableStat` | DIRECT | `sql/wall_sql_table_stat.rs` | IMPLEMENTED_UNVERIFIED | SQL 内表操作次数值对象已接 |
| `WallTableStat` | DIRECT | `sql/wall_table_stat.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | 表级操作/影响行数/六档直方图与 reset 快照已接 |
| `WallTableStatValue` | DIRECT | `sql/wall_table_stat_value.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED | 独立表级快照已接；最终逐字段对照待做 |
| `WallFunctionStat` | DIRECT | `sql/wall_function_stat.rs` | IMPLEMENTED_UNVERIFIED | 函数级原子聚合与 reset 快照已接 |
| `WallFunctionStatValue` | DIRECT | `sql/wall_function_stat_value.rs` | IMPLEMENTED_UNVERIFIED | 独立函数快照已接 |
| `WallDenyStat` | DIRECT | `sql/wall_deny_stat.rs` | IMPLEMENTED_UNVERIFIED | deny 次数、最后时间与 resetCount 已接 |
| `WallProviderStatValue` | DIRECT | `sql/wall_provider_stat_value.rs` | IMPLEMENTED_UNVERIFIED | canonical provider 快照、Java `toMap` 字段、零执行项过滤和 reset 原子取值已接；多数据源 facade 复用 Java 递归/name/sql 去重聚合，统一 golden/live diff 待补 |
| `WallProviderStatLogger` | PROTOCOL/MERGED | 统计快照发布 sink | PARTIAL | 迁移周期发布能力，不照搬 Java logger SPI 名称 |
| `WallProviderStatLoggerAdapter` | NOT_APPLICABLE | Rust trait 默认方法/组合 | NOT_APPLICABLE | Java 便利基类不形成 Rust 对象 |
| `WallProviderStatLoggerImpl` | NOT_APPLICABLE/PROTOCOL | tracing/metrics event sink | NOT_APPLICABLE | 不迁移 Java 日志实现对象；只迁移结构化字段、周期与错误隔离 |
| `WallProviderCreator` | ADAPTER | `wall_provider_creator.rs` + inventory | MISSING | order 和动态注册 |

原表把这些对象合并到单个 `wall_stat.rs` 并标记完成；修订后按“一对象一文件”恢复，只有明确的 Value/Builder 内部类型可随主对象同文件。

##### 10.2 各数据库 Wall Provider/Visitor

| Java 对象 | 映射 | Rust 目标 | 当前 |
| :--- | :--- | :--- | :--- |
| `MySqlWallProvider` | DIRECT | `mysql/my_sql_wall_provider.rs` | MISSING |
| `MySqlWallVisitor` | DIRECT | `mysql/my_sql_wall_visitor.rs` | MISSING |
| `OracleWallProvider` | DIRECT | `oracle/oracle_wall_provider.rs` | MISSING |
| `OracleWallVisitor` | DIRECT | `oracle/oracle_wall_visitor.rs` | MISSING |
| `PGWallProvider` | DIRECT | `postgresql/pg_wall_provider.rs` | MISSING |
| `PGWallVisitor` | DIRECT | `postgresql/pg_wall_visitor.rs` | MISSING |
| `DB2WallProvider` | DIRECT | `db2/db2_wall_provider.rs` | MISSING |
| `DB2WallVisitor` | DIRECT | `db2/db2_wall_visitor.rs` | MISSING |
| `SQLServerWallProvider` | DIRECT | `sqlserver/sql_server_wall_provider.rs` | MISSING |
| `SQLServerWallVisitor` | DIRECT | `sqlserver/sql_server_wall_visitor.rs` | MISSING |
| `SQLiteWallProvider` | DIRECT | `sqlite/sqlite_wall_provider.rs` | MISSING |
| `SQLiteWallVisitor` | DIRECT | `sqlite/sqlite_wall_visitor.rs` | MISSING |
| `CKWallProvider` | DIRECT | `clickhouse/ck_wall_provider.rs` | MISSING |
| `ClickhouseWallVisitor` | DIRECT | `clickhouse/clickhouse_wall_visitor.rs` | MISSING |
| `WallVisitorUtils` | DIRECT | `wall_visitor_utils.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：tenant active path 已迁移；其余 visitor 公共算法仍缺 |

#### 11. Stat 对象逐项明细

| Java 对象 | 映射 | Rust 目标 | 当前 |
| :--- | :--- | :--- | :--- |
| `JdbcSqlStat` | DIRECT | `stats/jdbc_sql_stat.rs` | MATCH/PARTIAL/IMPLEMENTED_UNVERIFIED |
| `JdbcSqlStatValue` | DIRECT | `stats/jdbc_sql_stat_value.rs` | MATCH/PARTIAL/IMPLEMENTED_UNVERIFIED |
| `JdbcSqlStatMBean` | NOT_APPLICABLE/PROTOCOL | metric descriptor + admin schema | NOT_APPLICABLE：JMX 类型不迁移 |
| `JdbcDataSourceStat` | DIRECT | `jdbc_data_source_stat.rs` / `JdbcDataSourceStat` | MATCH/PARTIAL |
| `JdbcDataSourceStatMBean` | NOT_APPLICABLE/PROTOCOL | metric descriptor + admin schema | NOT_APPLICABLE：JMX 类型不迁移 |
| `JdbcConnectionStat` | DIRECT | `stats/jdbc_connection_stat.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：active/connecting、connect/error/close/transaction、纳秒统计、七档 histogram、Entry map/快照已接 |
| `JdbcConnectionStatMBean` | NOT_APPLICABLE/PROTOCOL | metric descriptor + admin schema | NOT_APPLICABLE：JMX 类型不迁移 |
| `JdbcStatementStat` | DIRECT | `stats/jdbc_statement_stat.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：create/prepare/call/close、running/concurrent、execute/error/time、五档 histogram 已接 |
| `JdbcStatementStatMBean` | NOT_APPLICABLE/PROTOCOL | metric descriptor + admin schema | NOT_APPLICABLE：JMX 类型不迁移 |
| `JdbcResultSetStat` | DIRECT | `jdbc_result_set_stat.rs` | PARTIAL：同名对象、Java 状态序列/并发规则与 ResultSet StatFilter 纵向链已验证；管理面待接 |
| `JdbcResultSetStatMBean` | NOT_APPLICABLE/PROTOCOL | metric descriptor + admin schema | NOT_APPLICABLE：JMX 类型不迁移 |
| `JdbcStatManager` | DIRECT | `stats/jdbc_stat_manager.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：独立单例、SQL ID、全局 Connection/Statement/ResultSet 与已注册 datasource JDBC reset 已接；ThreadLocal Context、JMX list 与 DruidDriver proxy registry 待补 |
| `JdbcStatManagerMBean` | NOT_APPLICABLE/PROTOCOL | admin operation | NOT_APPLICABLE：JMX 对象不迁移，业务操作进入管理协议 |
| `JdbcStatManagerFactoryBean` | NOT_APPLICABLE | 无 Rust 对象 | NOT_APPLICABLE：Spring FactoryBean/JMX 注册宿主不进入核心；Axum 管理装配由 druid-admin 承载 |
| `JdbcStatContext` | ADAPTER | `jdbc_stat_context.rs` + task/thread execution map | IMPLEMENTED_UNVERIFIED |
| `JdbcTraceManager` | DIRECT | `jdbc_trace_manager.rs` deprecated singleton | IMPLEMENTED_UNVERIFIED |
| `JdbcTraceManagerMBean` | NOT_APPLICABLE/PROTOCOL | trace admin schema | NOT_APPLICABLE：JMX 类型不迁移 |
| `DruidStatService` | PROTOCOL | `stats/druid_stat_service.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：核心路由、exact/startsWith 差异、非 URL-decode 参数、空列表 null、分页/typed 排序、SQL detail、reset-all 与 log-and-reset 分支已建立；locale/schema/异常与统一 golden 待补 |
| `DruidStatServiceMBean` | NOT_APPLICABLE/PROTOCOL | admin schema | NOT_APPLICABLE：有效操作由 service/admin API 承载 |
| `DruidDataSourceStatManager` | DIRECT/ADAPTER | `stats/druid_data_source_stat_manager.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：显式 registry、register/unregister/get/instances、逐 datasource reset 与独立 resetCount 已接；Java identity weak/JMX 注册边界不迁移，完整管理结果待补 |
| `DruidDataSourceStatManagerMBean` | NOT_APPLICABLE/PROTOCOL | admin schema | NOT_APPLICABLE：JMX 注册与 OpenType 不进入 Rust 核心 |
| `DruidStatManagerFacade` | DIRECT | `stats/druid_stat_manager_facade.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：name/id 查询、数据源/SQL/Wall/连接、Drivers Adapter、三层 reset 与 logStats 已接 |
| `DataSourceMonitorable` | DIRECT | `stats/data_source_monitorable.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：核心统计、driver label、removeAbandoned、JDBC reset 与 logStats 合同已显式化 |
| `TableStat` | DIRECT | `stats/table_stat.rs` / `TableStat` 及同文件 inner 类型 | IMPLEMENTED_UNVERIFIED | 14 个 wrapping counter、Java 顺序 Display、Name/Column FNV identity、Relationship/Condition equality 与格式、Column flags/attributes/dataType、12 项 Mode mark 已接 |

#### 12. Support、SQL、Util 与 Mock 原始清单的修订落点

##### 12.1 Support 子包

| Java 子包/对象 | 原表文件数 | 映射 | Rust 目标 | 当前 |
| :--- | :---: | :--- | :--- | :--- |
| `support.http.StatViewServlet` | 5 | PROTOCOL | `stat_view_router.rs` | MISSING |
| `support.http.WebStatFilter` | 3 | PROTOCOL | `web_stat_layer.rs` | MISSING |
| `support.http.ResourceServlet` | 2 | PROTOCOL | `resource_service.rs` | MISSING |
| `support.spring.*` | 20 | PROTOCOL | config/DI/instrumentation 模块 | MISSING |
| `support.ibatis.*` | 10 | ADAPTER | ORM instrumentation | MISSING |
| `support.logging.*` | 15 | NOT_APPLICABLE/PROTOCOL | `tracing` + `tracing-subscriber` | NOT_APPLICABLE | Java 日志门面、实现桥和包装类不做对象级迁移；仅迁移被业务代码消费的级别、事件字段、过滤与动态配置语义 |
| `support.jakarta.*` | 5 | PROTOCOL | Axum/Tower adapters | MISSING |
| `support.druid.*` | 5 | DIRECT/MERGE | 按对象登记 | MISSING |

##### 12.2 SQL 子包

| Java 子包 | 原表文件数 | 原处置 | 修订迁移落点 |
| :--- | :---: | :--- | :--- |
| `sql/ast/` | 67 | `sqlparser::ast::*` | compatibility AST，对象逐条登记 |
| `sql/ast/expr/` | 72 | `sqlparser::ast::Expr` | `DruidExpr` variant/type 逐条登记 |
| `sql/ast/statement/` | 283 | `sqlparser::ast::Statement` | `DruidStatement` variant/type 逐条登记 |
| `sql/parser/` | 23 | `sqlparser::Parser` | parser facade + adapter/extension |
| `sql/visitor/` | 21 | 遍历 enum | Visitor/output/parameterize 兼容层 |
| `sql/dialect/mysql/` | 214 | `MySqlDialect` | MySQL 方言扩展与差分语料 |
| `sql/dialect/postgresql/` | 50 | `PostgreSqlDialect` | PostgreSQL 方言扩展 |
| `sql/dialect/oracle/` | 116 | `OracleDialect` | Oracle 方言扩展/fork |
| `sql/dialect/sqlserver/` | 25 | `MsSqlDialect` | SQL Server 方言扩展 |
| `sql/dialect/db2/` | 21 | `Db2Dialect` | DB2 方言扩展 |
| `sql/dialect/h2/` | 10 | 近似 Hive | 禁止近似替代，迁移 H2 契约 |
| `sql/dialect/clickhouse/` | 18 | `ClickHouseDialect` | ClickHouse 扩展 |
| `sql/dialect/bigquery/` | 17 | `BigQueryDialect` | BigQuery 扩展 |
| 其他方言 | 369 | Generic/不支持 | 逐方言 L1～L4 处置 |
| `sql/builder/` | 8 | 不需要 | AST builder/transform |
| `sql/repository/` | 7 | 不需要 | schema repository |
| `sql/template/` | 1 | 不需要 | template 语义迁移 |

##### 12.3 Util 代表对象

| Java 对象 | 映射 | Rust 目标 | 当前 |
| :--- | :--- | :--- | :--- |
| `JdbcUtils` | SPLIT | `jdbc_utils.rs` + driver adapters | PARTIAL/IMPLEMENTED_UNVERIFIED：JDBC URL/db-family/type-name 主语义与 Rust DSN 分层已落；便捷执行/DDL/完整差分仍缺 |
| `DruidDataSourceUtils` | DIRECT | `druid_data_source_utils.rs` | MISSING |
| `SQLUtils` | DIRECT | `sql/sql_utils.rs` / `SqlUtils` | PARTIAL/IMPLEMENTED_UNVERIFIED：parse/format/toSQLString/parameterize canonical facade 已存在；Java 全方法与差分待补 |
| `DbType` | DIRECT | `db_type.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED：严格 Java 名称/别名、mask/hash/equals/style 已落；全枚举差分待统一阶段 |
| `TransactionInfo` | DIRECT | `core/transaction_info.rs` | PARTIAL/IMPLEMENTED_UNVERIFIED |
| 其余 util 对象 | DIRECT/MERGE | P0 CodeGraph 导出后逐条登记 | MISSING |

##### 12.4 Mock 22 个对象

Java mock 对象不作为生产 API 直接发布，但它们承载的驱动行为必须迁移为 Rust oracle fixture。每个 Java mock 对象仍登记为 `TEST_ADAPTER`，绑定 Rust test fixture；使用 `mockall` 不能自动视为完成。

#### 13. Rust 侧新增文件清单（保留原表并校正）

| Rust 文件/对象 | 承载的 Java 语义 | 当前 | 处置 |
| :--- | :--- | :--- | :--- |
| `physical_connection.rs` | `java.sql.Connection` 平台能力 | 已存在；`Connection` 仅兼容重导出 | 内部最小 SPI；Proxy/Filter 语义由 `DruidPooledConnection` 承载 |
| `physical_callable_statement.rs` | `java.sql.CallableStatement` 平台能力 | 已存在；标量/Decimal/时间/Blob/Clob/NClob/Reader SPI | Adapter 只能在真实支持时实现，不得伪造 |
| `callable_input_parameter.rs` / `callable_parameter.rs` / `callable_out_parameter.rs` | CallableStatement IN setter、参数定位与 OUT 描述 | 已存在 | 保留 setter 身份、index/name、sqlType、scale、typeName |
| `druid_pooled_callable_statement.rs` | Java `DruidPooledCallableStatement` | PARTIAL | Java 本类 116 个公开声明与继承结果入口均可追踪；同文件 handle 保留 ResultSet 返回的具体 callable 身份；支持存储过程的真实 driver 与完整结果资源证据待补 |
| `driver.rs` | `java.sql.Driver`/`DruidDriver` | PARTIAL | ADAPTER |
| `pool.rs` | `javax.sql.DataSource` | BROKEN/PARTIAL | 内部 SPI |
| `physical_connection_factory.rs` | `DruidDataSource.init()` 创建物理连接路径 | 已存在；`ConnectionFactory` 仅兼容重导出 | 仅服务 native pool，不承载外部池 lease |
| `connection_provider.rs` | `DruidDataSource.getConnection*` 来源选择 | MISSING | native/bridge 统一抽象 |
| `physical_connection_lease.rs` | 外部 pooled connection 所有权 | 已存在 | 一个透明租约只含一个 physical connection；析构归还原外部池 |
| `druid_connection_holder.rs` / `DruidConnectionHolder` | Java `DruidConnectionHolder` | 已存在；holder 生命周期契约通过 | native pool 创建、借出、回收始终移动同一个 holder；旧 `ConnectionHolder` 仅为兼容 alias |
| `connection_defaults.rs` / `ConnectionDefaults` | `DruidConnectionHolder` 默认状态与 `reset()` | 已存在；回收契约测试通过 | canonical holder 的内部值对象，不独立冒充 Java holder |
| `connection_recycle_disposition.rs` / `ConnectionRecycleDisposition` | `DruidDataSource.recycle` 的最终处置分支 | 已存在；native/bb8/deadpool callback 已接线 | 区分 reusable、discard 和 recycle error |
| `DruidPooledConnection` 内部三参数 `FnOnce` returner | `close/recycle/discard` | 已存在 | 接收 physical/id/disposition；显式 close 完整复位，脏 Drop 直接淘汰 |
| `toasty_connection_factory.rs` / `toasty_connection_adapter.rs` | Toasty 0.9 Driver SPI | 已存在 | 内置标准实现；SQLite 实库通过；不创建 `toasty::Db`，避免 pool-in-pool |
| `DruidPoolBuilder`/`PoolConfigBuilder` | properties/setter | PARTIAL | 去重 |
| `idle_queue.rs` | holder 数组 | 尚未独立 | RUST_ONLY |
| `leak_detector.rs` | removeAbandoned | MISSING | RUST_ONLY |
| `parameterize.rs` | parameterized visitor | 尚未独立；当前在 stats | RUST_ONLY/ADAPTER |
| `fingerprint.rs` | SqlStat key | 尚未独立；当前在 stats | RUST_ONLY |
| `sql_hint.rs` | selector route context | 已存在 | RUST_ONLY |
| `datasource_group.rs` | HA 节点组 | 已存在 | RUST_ONLY |
| `prom_exporter.rs` | StatService/MBean | MISSING | PROTOCOL |

原表中的 `crates/droid-admin` 路径拼写已修正为 `crates/druid-admin`。

#### 14. 对象关闭条件

一个对象只有同时满足以下条件才可标 `DONE`：

- Java FQCN、源提交、Rust 类型和文件均已登记；
- 映射形态及必要 ADR 已批准；
- 字段默认值、公开方法、错误、副作用和并发语义有契约；
- Java fixture 与 Rust 测试差分通过；
- 中文 doc 注释注明 Java 来源；
- 名称检查、无 stub 检查、CodeGraph 调用链检查通过；
- 若由 enum 承载，源对象对应的 variant 不与其他对象混账。
<!-- GLOBAL_OBJECT_LEDGER_MERGE_END -->
