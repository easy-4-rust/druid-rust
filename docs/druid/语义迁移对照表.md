# Java Druid core → Rust `druid` 语义迁移对照表

> Java baseline：`33824c3dec1612711f9bb4e409319bcab2e4cd0e`
> Rust baseline：`2c7bf3bea6f9e94f4583a85794df519c4435a425` + 当前 dirty migration worktree
> 最后静态核对：2026-07-30；文档状态：`PARTIAL`（`V0_STATIC`，尚未统一验收）

## 1. 状态规则

| 状态 | 定义 |
| :--- | :--- |
| `PASS` | Java oracle 与 Rust 真实依赖路径均通过 |
| `PARTIAL` | 子集已通过，但方法/配置/驱动矩阵不完整 |
| `FAIL` | 已有实现与 Java 可观察结果不同 |
| `TODO` | 尚无实现或证据 |
| `NOT_APPLICABLE/EXCLUDED` | JVM/Java 平台实现细节；只做来源审计，不生成 Rust 对象、不进入实现完成率 |

日志只对齐可观察语义，不对齐 Java 框架对象：Rust 统一使用 `tracing`。
SLF4J、Log4j、Log4j2、Commons Logging、Logback 及其 MBean/桥接类统一为
`NOT_APPLICABLE/EXCLUDED`。允许保留的是 Druid 自身的日志事件合同，包括事件
时机、级别、结构化字段、过滤、脱敏与错误关联。

## 2. 连接池

| ID | Java 语义 | Rust 当前 | 状态/缺口 |
| :--- | :--- | :--- | :--- |
| CORE-POOL-001 | maxActive 原子容量 | 原子预留与压力测试 | PARTIAL |
| CORE-POOL-002 | maxWait、公平等待、超时 | `-1` 无限等待、0/正数截止时间、取消安全 wait 计数、`maxWaitThreadCount` 已接；公平锁切换未闭合 | PARTIAL |
| CORE-POOL-003 | initialSize/minIdle/fill | init 预建、asyncInit 受监管后台目标、initExceptionThrow=true 的“已初始化后返回首错”、false 的固定 3s 重试、显式 `fill()` 到 maxActive、`fill(toCount)` 截断 maxActive、后台 minIdle fill、keepAlive 失败补池和配置边界已接；enable=false 只禁止借用/回收，不阻止 init/fill | PARTIAL：待初始化错误/异步任务统一差分 |
| CORE-POOL-004 | testOnBorrow/testOnReturn/testWhileIdle | 三开关、统一 ValidConnectionChecker、borrow/return/idle 主链已接 | PARTIAL：待多驱动与差分 |
| CORE-POOL-005 | rollback/reset/schema restore | 主顺序与 SQLite 契约 | PARTIAL |
| CORE-POOL-006 | shrink/phyTimeout/phyMaxUseCount/keepAlive | Java `checkTime` 分支、顺序保活、失败销毁、minIdle 回填、fatal 前连接强制重验及取消安全批次已接 | PARTIAL/IMPLEMENTED_UNVERIFIED：fatal 与普通 shrink 组合差分待补 |
| CORE-POOL-007 | removeAbandoned/async close | active lease 追踪、运行中跳过、超时失效、backtrace/脱敏日志已接；Rust Drop/异步关闭映射待统一验收 | PARTIAL |
| CORE-POOL-008 | exactly-once recycle/discard | native/bb8/deadpool 已测 | PASS |
| CORE-POOL-009 | shutdown 排空任务和租约 | close 先封池、取消安全排空 idle、等待维护任务；活跃租约的强制终止仍开放 | PARTIAL |

## 3. Connection/Statement

| ID | Java 语义 | Rust 当前 | 状态/缺口 |
| :--- | :--- | :--- | :--- |
| CORE-CONN-001 | exec/query 与参数类型 | Toasty SQLite NULL/BOOL/I64/F64/DECIMAL/DATE/TIME/TIMESTAMP/TEXT/BLOB 真实绑定；声明列类型恢复强类型；raw BOOLEAN 按 SQLite storage class 返回 INTEGER | PARTIAL：PostgreSQL/MySQL/Turso 未闭合 |
| CORE-CONN-002 | transaction/savepoint | Toasty SQLite begin/commit/rollback/setAutoCommit/named savepoint 真实测试 | PARTIAL：多数据库/XA 缺失 |
| CORE-CONN-003 | PreparedStatement 六重载 key/cache | 主语义及 self/PreparedStatement/raw concrete unwrap 已测；`close_with_connection` 先按 Java 固定顺序恢复五个 Statement 属性，再处理 clearParameters/clearBatch 和 cache；首个恢复错误立即停止并增加 exceptionCount；完整 JDBC `setXxx` 重载由 `PreparedInputParameter` 保留类型、nullable、Calendar、长度与资源身份；绑定可驱动 query/update/generic execute 与有序 batch；Toasty/SQLx SQLite 及 RBDC 严格 SPI 的物理句柄在 setter 阶段物化资源，Filter 单次/批次仍收到原描述符；canonical query 直接包装 `PhysicalResultSet`，不再经 `Vec<Row>` 重建；继承属性、SQLWarning、共享 ResultSet trace 及 Prepared 动态身份已接 | PARTIAL：无连接参数 close 无法运行 ExceptionSorter；RBDC 组合实库及 SQLx Any/PostgreSQL/MySQL、Toasty PostgreSQL/MySQL/Turso generic/batch/ResultSet 矩阵缺失 |
| CORE-CONN-004 | CallableStatement 三重载/IN/OUT | Java 构造器之外 115 个公开方法均有 Rust 同语义入口；另有 5 个 Rust 生命周期/扩展方法；self/Callable/Prepared/raw concrete unwrap 已闭合 | PARTIAL：API 面已闭合，真实存储过程 driver、结果资源与 vendor 错误矩阵未闭合 |
| CORE-CONN-005 | ResultSet 生命周期/metadata | canonical `DruidPooledResultSet` + `PhysicalResultSet`/`RowSetResultSet`；只读游标、基础/流及强类型 getter、资源对象 16 getter、对象型 14 update、LOB Reader/InputStream 12 update、标量/字符/二进制流 56 update、Map/typed getObject 各 2 重载、Calendar 三态、NULL/wasNull、SQLWarning、完整 metadata 与 Wrapper；next/close/warning、九族标量 18 getter、BigDecimal/Date/Time/Timestamp 16 个强类型重载、plain/map/typed object 六重载、stream/resource 26 重载、navigation/property 26 调用、NString 两重载、getMetaData、getStatement、七个 row-mutation 无参调用、updateNull 与 13 个 typed setter 的 index/label 共 28 个基础列更新调用、updateObject 的 plain/scaleOrLength × index/label 四重载、Ref/Blob/Clob/Array/RowId/NClob/SQLXML 的 index/label 14 个资源对象 setter、Blob InputStream 与 Clob/NClob Reader 的 12 个 LOB setter、ASCII/Binary/Character/NCharacter 的 22 个 stream setter，以及 `updateNString` 的 index/label 两个 setter，均由位置化 Filter around-chain 同步转发；`ResultSetStatement` 保留普通/Prepared/Callable 三种动态身份、同一逻辑生命周期和 Filter 替换能力，标签、Calendar 三态、nullable Map、目标类型、资源与 metadata 平台身份保持可辨；`is_closed_with_connection` 保留 Java 可失败 Filter 入口，内部 `is_closed` 仅作无失败生命周期观察器；StatFilter open/close/fetch/hold、错误路由、同租约与 Statement trace 已验证；`JdbcOpaqueObject` 保留 vendor object 身份并可下转回原类型；custom typed object 精确一次物理列读取，最小 SPI 的导航/属性/update 默认方法逐项保留 operation 名称，并优先传播列索引或底层读取错误；185 个 ResultSet 调用逐方法精确委托，ResultSet update setter Filter 子域已闭合；Statement/PreparedStatement canonical query 均直接持有物理 ResultSet，SQLx 真实 SQLite（含普通 Statement 零行 query）与 RBDC SPI 的 driver label 可贯穿 metadata/findColumn/label getter，物理或 Filter 错误在同一 Java 调用点分类并计入 Statement | PARTIAL：Toasty 仍为 eager RowSet 且 raw SQL alias 不保留；RBDC 空结果 metadata、SQL 级 read length、streaming、多数据库、嵌套 ResultSet/Clob 自动代理与 vendor custom typed conversion 仍待补 |
| CORE-CONN-006 | LOB/Array/Ref/SQLXML | Blob 11 方法、Clob 13 方法、NClob 继承契约及 Array/Ref/RowId/SQLXML/URL 独立平台资源已实现；额外恢复 Druid `ClobProxy/Impl`、`NClobProxy/Impl` 四对象，13 个操作进入有位置 FilterChain，ResultSet 可产生 index/label Proxy；EncodingConvertFilter 的 Clob 字符/Reader 分支已接；`PhysicalConnection` 与 `DruidPooledConnection` 已补齐 `createBlob/createClob/createNClob`，三入口逐次从 position=0 进入 FilterChain，Clob/NClob 返回 Druid Proxy，Blob 按 Java 保持 raw 句柄 | PARTIAL/IMPLEMENTED_UNVERIFIED：真实 LOB Adapter 与 Java/SQLite capability 差分尚未闭合；Java baseline 无 BlobProxy，不制造伪对象 |
| CORE-CONN-007 | fatal error/discard | `SqlException` 可承载 code/SQLState/recoverable/class/message/cause/可赋值类型链；11 个 Java vendor/abstract sorter 对象均为独立文件；23 条 `PhysicalConnection` 错误路径统一进入 sorter；SQLx 保留公开 database fields，Toasty/RBDC 保留各自上游公开信息上限 | PARTIAL：Java vendor oracle 23/23 + Oracle 连接/语句全族 53/53、Rust sorter 10/10、Rust 全路径 1/1、真实 SQLite Toasty structured-error 与 SQLx fatal-discard 均通过；Statement 全操作、上游未公开的 vendor 字段、listener/错误统计待闭合 |
| CORE-CONN-008 | SQLite 表达式列 runtime type | `COUNT(*)` 回归已修复 | PASS |
| CORE-CONN-009 | `WrapperAdapter`/`PoolableWrapper`/池化连接与语句 unwrap | `Any/TypeId` 类型令牌 + `Unwrapped` 区分具体对象与 Connection/Prepared/Callable 接口；Java connection 34/34 + statement 28/28、Rust wrapper 3/3 + prepared/callable、真实 SQLite wrapper 3/3 | PASS（Wrapper 对象语义；metadata 与生产 Proxy 对象另行验收） |
| CORE-CONN-010 | `DruidPooledStatement` 与 `createStatement` 三重载 | 独立 `PhysicalStatement` SPI 和 canonical `DruidPooledStatement` 已承接默认/类型并发/完整保持性三入口、query/update、JDBC `int[]` batch、四种 `execute`、结果类型判定、顺序多结果、`getResultSet/getUpdateCount/getMoreResults/getGeneratedKeys`、SQLWarning、属性、关闭、跨租约拒绝和 Wrapper；Toasty 与 SQLx SQLite warning/clear 已实测，RBDC Connection warning SPI 已测 | PARTIAL：普通 Statement 主链已闭合；SQLx/RBDC 原生多结果/生成键矩阵、其余 Filter statement 事件和完整统计尚未闭合 |
| CORE-CONN-011 | `DruidPooledPreparedStatement#addBatch/clearBatch/executeBatch` | wrapper 在 add 时保存独立 `Vec<Value>` 快照；`clearParameters` 不动已入批参数；before Filter 短路保留批次，物理调用开始后成功/失败均消费；默认 SPI 顺序复用同一 PreparedStatement 并保留部分计数；真实 Toasty/SQLx SQLite 与 RBDC 严格 SPI 已测 | PARTIAL：Druid/Filter/Stat 主语义已闭合；RBDC 组合实库、PostgreSQL/MySQL 原生 batch、`-2/-3` 与驱动专属异常矩阵待补 |
| CORE-CONN-012 | `DruidPooledPreparedStatement#execute/getResultSet/getUpdateCount/getMoreResults/getGeneratedKeys` | `PhysicalConnection::execute_prepared` 保留参数快照和 prepare 重载键；组合 `DruidPooledStatement` 基类状态承接结果类型、顺序多结果、generated keys、继承 SQLWarning、wrapper 关闭与 ResultSet trace；`DruidPooledPreparedStatementHandle` 让 query/current/generated-keys ResultSet 恢复同一 Prepared 身份，`DruidPooledCallableStatementHandle` 再保留 Java callable 子类型身份；两者共享 key、物理 unwrap 和关闭级联；物理 getter 错误按发生时点进入 sorter | PARTIAL：Druid/Toasty SQLite Prepared 主链及 callable 测试 SPI 已闭合；SQLx/RBDC 原生 generic execute、多数据库多结果、真实存储过程与 vendor generated-key 描述待补 |
| CORE-CONN-013 | `PreparedStatement#setXxx/clearParameters` 参数状态 | 48 个 setter 重载入口覆盖 null/typeName、标量、Decimal、日期时间/Calendar、Object 目标类型/scale、Stream/Reader int/long/无长度、LOB/Array/Ref/URL/RowId/SQLXML；1-based 稀疏参数在执行前拒绝；重复 setter 覆盖槽位；clear 仅在物理 clear 成功后清空；fatal setter 立即 discard；`JdbcParameter` 九对象合同及 0-based Proxy 参数视图已接；真实 Toasty/SQLx SQLite 与 RBDC 严格 SPI 已验证资源 setter 时点物化、query/update 和有序 batch | PARTIAL：JdbcParameter Java diff、RBDC 组合实库、SQLx Any 与 PostgreSQL/MySQL/Turso 资源及 vendor 类型矩阵待补 |
| CORE-CONN-014 | Statement/PreparedStatement query 保留驱动 ResultSet 身份 | `fetch_result_set/fetch_prepared_result_set/fetch_prepared_parameters_result_set` 贯穿物理 SPI、外部 lease、Filter、共享 Statement 状态和池化 wrapper；SQLx 真实 SQLite 普通/零行/prepared alias、RBDC driver label、默认 SPI 与 lease 均已测；Filter after 对未 eager 消费的结果集保持 `row_count=None` | PARTIAL：Toasty 明确为 eager RowSet；RBDC 零行 descriptor、streaming、PostgreSQL/MySQL/Turso 与 vendor ResultSet 尚未闭合 |

### 3.1 CallableStatement 当前重载账本

| Java 签名族 | Rust API/对象 | 输入/输出与副作用 | 证据 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| `registerOutParameter(int/String, int[, int/String])` 六重载 | 六个 `register_*_out_parameter*` | index/name、sqlType、scale/typeName 原样进入物理 SPI；错误使缓存条目失效 | Java 69 个 Callable 测试 + Rust callable contract | `BEHAVIOR_VERIFIED`（测试驱动） |
| `setNull(String,int[,String])` 两重载 | `set_named_null*` + `CallableInputParameter::Null` | 保留 `sqlType/typeName`，不再压缩成无类型 `Value::Null` | Rust descriptor 精确断言 | `IMPLEMENTED_UNVERIFIED` |
| `setObject(String,Object[,int[,int]])` 三重载 | `set_named_object*` + `CallableInputParameter::Object` | 保留 `targetSqlType/scale` | Rust descriptor 精确断言 | `IMPLEMENTED_UNVERIFIED` |
| `setBoolean/Byte/Short/Int/Long/Float/Double/String/NString/Bytes` | 同语义 `set_named_*` | 每个 Java setter 使用独立 enum variant，驱动可区分调用 | Rust callable contract | `IMPLEMENTED_UNVERIFIED` |
| `getString/Boolean/Byte/Short/Int/Long/Float/Double/Bytes` index/name | 同语义 `get_*` / `get_named_*` | 池化层统一错误与缓存淘汰，类型转换由 `PhysicalCallableStatement` 方法承担 | Rust callable contract | `IMPLEMENTED_UNVERIFIED` |
| `setBigDecimal(String,BigDecimal)`、`getBigDecimal(int[,scale]/String)` | `set_named_big_decimal`、`get_*_big_decimal*` | `bigdecimal::BigDecimal` 保留任意精度和 scale；Java null 用 `Option` 区分 | Rust core 2/2 + pool 2/2 | `IMPLEMENTED_UNVERIFIED` |
| `set/get Date/Time/Timestamp(int/String[,Calendar])` | 同名 snake_case API + `CallableCalendarArgument` | `chrono` 强类型；区分无 Calendar 重载、显式 null Calendar、实际时区标识 | Java 69 个 Callable 测试 + Rust Calendar 调用账本 | `IMPLEMENTED_UNVERIFIED` |
| `getBlob(int/String)`、`setBlob(String,Blob/InputStream[,long])` | `get*_blob` / `set_named_blob*` + `JdbcBlob/RdbcInputStream` | Blob 保留物理对象身份和 11 方法资源 SPI；流不预读；区分 Java null、无长度重载和显式 long | Java Callable 101/101 + Rust Blob 2/2 + pool 2/2 | `IMPLEMENTED_UNVERIFIED`（无 Callable E7） |
| `getClob(int/String)`、`setClob(String,Clob/Reader[,long])` | `get*_clob` / `set_named_clob*` + `JdbcClob/RdbcReader` | Clob 保留对象身份和 13 方法资源 SPI；Reader 不预读；区分对象 null、Reader null、无长度和显式 long | Java Callable 101/101 + Rust Clob 2/2 + pool 2/2 | `IMPLEMENTED_UNVERIFIED`（无 Callable E7） |
| `getNClob(int/String)`、`setNClob(String,NClob/Reader[,long])` | `get*_n_clob` / `set_named_n_clob*` + `JdbcNClob/RdbcReader` | 保留 national-character 类型身份、Java null 与 Reader/long 重载 | 同上 | `IMPLEMENTED_UNVERIFIED`（无 Callable E7） |
| `getCharacterStream/getNCharacterStream(int/String)` | `get_*_character_stream` / `get_named_*` | 返回共享身份的 `RdbcReader`，不转成 String；SQL NULL 为 `None`，类型错误进入异常淘汰 | Rust pool index/name/null/type-error | `IMPLEMENTED_UNVERIFIED` |
| `setCharacterStream(String,Reader[,int/long])`、`setNCharacterStream(String,Reader[,long])` | `set_named_*_character_stream*` + `JdbcCharacterLength` | `Unspecified/Int/Long` 三种 descriptor 保留 Java 精确重载；负长度不在池化层改写；Reader 不预读 | Rust descriptor/错误/关闭契约 | `IMPLEMENTED_UNVERIFIED` |
| `getObject(int/String)` 标量部分 | `JdbcObject`（`CallableOutputValue` 为兼容 alias） | 保留 Decimal/Date/Time/Timestamp 类型身份；ResultSet/LOB 等不得伪装成标量 | Rust typed OUT contract | `PARTIAL` |
| URL、Array/Ref、RowId、SQLXML、二进制流、map/typed `getObject`、ResultSet 包装、unwrap | 已有同语义 API/资源对象 | 不得用 String/Bytes 静默替代；真实 driver 必须逐类型验证 | 115 个 Java 方法入口账本；真实存储过程证据待补 | `IMPLEMENTED_UNVERIFIED/PARTIAL` |

上述测试驱动证据不是支持存储过程数据库的 E7 证据，因此整个对象仍为 `PARTIAL`。

### 3.2 PreparedStatement setter 语义账本

| Java 签名族 | Rust 对象/API | 已保留语义 | 当前边界 |
| :--- | :--- | :--- | :--- |
| `setNull(int,int[,String])` | `PreparedInputParameter::Null` + `PreparedTypeNameArgument` | 区分无 typeName 重载、显式 null 和实际 typeName | 标量执行为 SQL NULL |
| `setBoolean/Byte/Short/Int/Long/Float/Double` | 独立 enum variant + 同名 snake_case setter | 不在池化层抹平成单一 number；重复设置覆盖同一 1-based 槽位 | Toasty SQLite 真实绑定通过 |
| `setBigDecimal/String/NString/Bytes` | nullable 强类型 variant | 保留 Decimal scale、national string 和 bytes/null | Toasty SQLite 真实绑定通过；SQLite 可在读取时规范化 decimal |
| `setDate/Time/Timestamp([,Calendar])` | `JdbcCalendarArgument` | 区分无 Calendar 重载、显式 null Calendar 与实际时区对象 | Toasty SQLite 强类型读回通过 |
| `setObject([,targetSqlType[,scaleOrLength]])` | `JdbcObject` + 可选目标 SQL 类型/scale | 三个 Java 重载不合并，标量可进入统一 Value 路径 | vendor object 仍需原生 Adapter |
| ASCII/Binary/Unicode Stream | `RdbcInputStream` + `JdbcStreamLength` | 无长度/int/long 精确区分，负长度原样保留，不预读 | 执行时显式报原生 Adapter 未实现 |
| Character/NCharacter Stream | `RdbcReader` + `JdbcCharacterLength` | 无长度/int/long 精确区分，Reader 身份与位置不被消费 | 执行时显式报原生 Adapter 未实现 |
| Blob/Clob/NClob 对象及 stream/reader | 对应 JDBC 资源对象 variant | 对象重载与流重载不混淆，nullable 和长度保真 | 原生 LOB bind 待补 |
| Ref/Array/URL/RowId/SQLXML | 独立资源 variant | 不用 String/Bytes 冒充资源；URL 是可无损标量例外 | 资源原生 bind 待补 |

所有 setter 都先验证语句/租约开放状态，再立即调用
`PhysicalPreparedStatement::set_parameter`；只有物理调用成功才更新逻辑参数槽。
因此驱动错误发生在与 Java 相同的 setter 调用点，并立即进入连接异常 sorter。
`clear_parameters` 同样只在物理 clear 成功后清空逻辑状态。执行前将 1-based 槽位
转为有序参数；任何空洞都返回 `InvalidArgument`，不会把未绑定参数伪装成 NULL。

## 4. Filter/Proxy

| ID | Java 语义 | 状态 |
| :--- | :--- | :--- |
| CORE-FLT-001 | before 顺序/短路 | PARTIAL |
| CORE-FLT-002 | after 逆序/错误上下文 | PARTIAL |
| CORE-FLT-003 | connection 全 hook | TODO/PARTIAL |
| CORE-FLT-004 | statement/prepared/callable/resultset 全 hook | TODO |
| CORE-FLT-005 | Stat/Wall/Log/Config Filter | PARTIAL |
| CORE-FLT-006 | proxy attributes/raw/wrapper | TODO |
| CORE-FLT-007 | ResultSet 标量 getter 的精确重载、注册顺序、短路、返回改写与错误展开 | PASS（18 个 index/label 重载；真实 Toasty SQLite + 物理探针） |
| CORE-FLT-008 | ResultSet BigDecimal/Date/Time/Timestamp 重载及 Calendar 三态 Filter | PASS（16 个精确重载；真实 Toasty SQLite + 物理探针） |
| CORE-FLT-009 | ResultSet `getObject` 的 plain/map/typed × index/label 六重载 FilterChain | PASS（六个精确重载；nullable Map、目标类型、错误分类与真实 Toasty SQLite） |
| CORE-FLT-010 | ResultSet Ref/Blob/Clob/Array/URL/RowId/NClob/SQLXML 与五类 stream 的 index/label FilterChain | PASS（13 族 26 个精确重载；标签物理 SPI、共享资源句柄、短路、错误分类与真实 Toasty SQLite 流读取） |
| CORE-FLT-011 | ResultSet navigation/property 方法族 FilterChain | PASS（26 个方法；游标状态、int 参数、findColumn 标签、可失败 isClosed、错误分类与真实 Toasty SQLite 状态机） |
| CORE-FLT-012 | ResultSet `getNString` index/label 独立 FilterChain | PASS（两条精确重载；不再折叠为 getString，Unicode、短路与错误分类经真实 Toasty SQLite 验证） |
| CORE-FLT-013 | ResultSet `getMetaData()` 平台句柄 FilterChain | PASS（拥有物理 SPI 的 metadata 对象；默认穿透、短路、错误分类与真实 Toasty SQLite） |
| CORE-FLT-014 | ResultSet `getStatement()` 动态平台句柄 FilterChain | PASS（普通/Prepared/Callable 三身份；同一逻辑对象、短路替换、错误分类与真实 Toasty SQLite） |
| CORE-FLT-015 | ResultSet row-mutation 无参方法族 FilterChain | PASS（insert/update/delete/refresh/cancel/moveToInsert/moveToCurrent 七调用；默认穿透、短路、错误分类与真实 Toasty SQLite capability error） |
| CORE-FLT-016 | ResultSet 基础列更新 setter FilterChain | PASS（updateNull 及 13 个 typed setter 的 index/label 共 28 个精确入口；默认穿透、全量短路、参数身份、错误分类与真实 Toasty SQLite capability error） |
| CORE-FLT-017 | ResultSet `updateObject` 四重载 FilterChain | PASS（plain/scaleOrLength × index/label 四个精确入口；SQL NULL、vendor custom 对象身份、负 scale、短路、错误分类与真实 Toasty SQLite capability error） |
| CORE-FLT-018 | ResultSet 资源对象 update setter FilterChain | PASS（Ref/Blob/Clob/Array/RowId/NClob/SQLXML × index/label 共 14 个精确入口；nullable、RowId 值、短路、错误分类与真实 Toasty SQLite capability error） |
| CORE-FLT-019 | ResultSet LOB stream/Reader update setter FilterChain | PASS（Blob InputStream、Clob/NClob Reader × index/label × 无长度/long 共 12 个精确入口；nullable、共享游标、原始 long、短路、错误分类与真实 Toasty SQLite capability error） |
| CORE-FLT-020 | ResultSet ASCII/Binary/Character/NCharacter stream update setter FilterChain | PASS（ASCII/Binary/Character × index/label × 无长度/int/long 18 个入口，加 NCharacter × index/label × 无长度/long 四个入口，共 22 个；nullable、共享游标、int/long 边界、短路、错误分类与真实 Toasty SQLite capability error） |
| CORE-FLT-021 | ResultSet `updateNString` index/label 独立 FilterChain | PASS（两条精确重载；nullable、Unicode、短路、错误分类与真实 Toasty SQLite generic update capability error） |
| CORE-FLT-022 | canonical `FilterAdapter` 默认适配对象 | PARTIAL（独立对象已建立；生命周期/配置空语义、精确自身 Wrapper、SQL before/after 与 185 个 ResultSet 默认继续链经真实 Toasty SQLite 验证；Java 491 个公开方法中的其余 hook 随 Filter/FilterChain 迁移继续开放） |
| CORE-FLT-023 | canonical `FilterEventAdapter` 事件模板对象 | PARTIAL（独立对象已建立；物理 connection_connect 已接直接/后台 creator，真实 ID、before→factory→after 反向顺序和错误主链已落；dataSource get/release 均为可短路的有位置 around-chain；当前生产链可承载的 Statement create/prepare/call、execute/query/update/batch、error-after 与 ResultSet open 已验证；StatementProxy/具体 DataSource 身份、downstream-before 局部展开及完整 Java 测试矩阵仍开放） |
| CORE-FLT-024 | canonical `FilterManager` alias/resource/factory 管理对象 | PARTIAL（独立对象与 Java bundled resource 已建立；properties 顺序覆盖、UTF-16 `<128` 回退、逗号展开、类名去重、缺失继续、构造失败及显式工厂加载已验证；自动 classloader 与 DataSource `setFilters` 装配仍开放） |

## 5. SQL/Wall/Stat

| ID | Java 语义 | 状态 |
| :--- | :--- | :--- |
| CORE-SQL-001 | 全 SQL AST 对象 | TODO/PARTIAL |
| CORE-SQL-002 | 方言 parser 与 feature | TODO/PARTIAL |
| CORE-SQL-003 | Visitor、clone、parent、attributes | TODO |
| CORE-SQL-004 | canonical output/format | TODO |
| CORE-SQL-005 | parameterize/merge/fingerprint | PARTIAL |
| CORE-WALL-001 | WallConfig 全字段 | PARTIAL |
| CORE-WALL-002 | rule/violation/cache/provider | PARTIAL |
| CORE-STAT-001 | SQL/dataSource/web/spring 统计 | PARTIAL |
| CORE-STAT-002 | reset/MBean/JSON 结果 | TODO |

## 6. SQLite 真实证据

`crates/druid/tests/sqlite_core_semantics_test.rs` 纵向穿过
`druid facade → DruidPool → DruidPooledConnection → statement cache →
ToastyConnectionAdapter → Toasty Driver → SQLite`，验证：

- 真实 DDL/DML/查询与参数绑定；
- PreparedStatement 真实执行和连接级缓存命中；
- 事务 rollback；
- close 后同一物理连接复用和计数；
- `COUNT(*)` 按 SQLite 运行时 INTEGER 类型解码；
- CallableStatement 明确 `UnsupportedOperation`。

SQLite 不支持存储过程、XA 和 vendor SQLState，相关语义不得标 `PASS`。

`crates/druid/tests/toasty_connection_adapter_test.rs` 直接验证 factory
metadata、六类 `Value` 参数、`last_insert_rowid()`、prepared close、完整保存点、
SQLite 只支持 Serializable、只读明确 unsupported、discard 和未知 URL scheme。
该合同已由 `druid` 默认构建执行。

## 7. Toasty 标准实现与扩展边界

| ID | 语义 | 当前证据 | 状态 |
| :--- | :--- | :--- | :--- |
| CORE-DRV-001 | 内置 factory 每次创建 raw connection | 直接 `Driver::connect`，代码禁止 `toasty::Db` | PASS |
| CORE-DRV-002 | native pool 唯一所有者 | core SQLite 纵向测试回收同一连接 | PASS |
| CORE-DRV-003 | Toasty SQL driver capability | SQLite 实测；PG/MySQL/Turso feature 已定义 | PARTIAL |
| CORE-DRV-004 | 非 SQL driver 不冒充 JDBC | factory 拒绝 `Capability.sql=false` | PASS |
| CORE-DRV-005 | SQLx/RBDC 仍可通过唯一 `druid-wrapper` 扩展 | 已归入 wrapper 内部模块，回归通过 | PASS（架构边界） |
| CORE-DRV-006 | vendor error/generated keys/type metadata | SQLite 子集；多驱动未闭合 | PARTIAL |

<!-- GLOBAL_SEMANTIC_LEDGER_MERGE_START -->
## 原全局语义 ID 全量归并

> 原全局语义总账中属于 core/pool/filter/sql/wall/stat/HA 的 ID 全部保留在此。
> 现有 `CORE-*` 是可执行合同编号，`SEM-*` 是历史全量基线编号；后续应逐条
> 建立一对一或一对多映射，不得静默删除。

### `SEM-CFG-*`

保留原全局 ID，便于历史引用和自动审计。

| ID | Java 行为 | Rust 目标 | 当前 | 验收 |
| :--- | :--- | :--- | :--- | :--- |
| SEM-CFG-001 | `DruidDataSource.init()` 幂等初始化 | `DruidDataSource::init()` + `DruidPool` lifecycle lock/initialized gate | IMPLEMENTED_UNVERIFIED：并发 init 由单一 Tokio 生命周期锁串行，成功前不发布 initialized，失败可重试；Filter、initialSize、close worker 与维护任务只装配一套；与 Java 一致，enable=false 不阻止显式 init | Java 并发 init、初始化失败重试和取消的统一差分 |
| SEM-CFG-002 | `configFromProperties` 属性名、别名、优先级 | serde/config facade | TODO | 全量属性 golden test |
| SEM-CFG-003 | 默认值与 Druid 1.2.28 一致 | config types | PARTIAL | 字段逐项 snapshot |
| SEM-CFG-004 | 非法 max/min/wait/validation 配置立即失败 | builder/init validation | PARTIAL | 错误类型和时机一致 |
| SEM-CFG-005 | SPI Filter/Checker/Sorter/Driver 自动发现和排序 | inventory registry | TODO | provider 顺序与 fallback |
| SEM-CFG-006 | init 创建 initialSize 连接并填充 holder | native pool 已按 `PhysicalConnectionInfo` 执行 driver connect→默认属性 init→有序 init SQL→MySQL session/global variables 采集→未执行 SQL时 factory validate→holder；保留 connect/init/validate 时序、变量所有权和 Java `checked` 跳过追加验证语义；initialSize fill 已接 | PARTIAL/IMPLEMENTED_UNVERIFIED | 成功/部分失败/全失败、MySQL 变量、阶段错误计数和真实 SQLite/MySQL 统一验证 |
| SEM-CFG-007 | init 启动 creator/destroy/log tasks | 每池唯一受监管 create worker + close worker + maintenance task + stat publisher；普通 get future 仍可直接建连 | PARTIAL/IMPLEMENTED_UNVERIFIED：discard/disabled/recycle-error 后通过 boolean empty-signal 请求 creator；create worker 现按严格 retry 阈值、正退避、failContinuous/failFast/break 状态机重试，退避中合并 target 并响应 shutdown；init/close/restart 保存并排空 task handle；Java logger class/name 不迁移。`useGlobalDataSourceStat` 冲突、超时诊断与取消/live diff 待补 | 启停、creator 多等待者/失败恢复/合并请求、sink panic/error、取消、重启及周期字段差分 |
| SEM-CFG-008 | enable/disable/close/restart 状态门禁 | `PoolInner` enable/closed/generation/creatingCount + `DruidPool` lifecycle lock + canonical facade | PARTIAL/IMPLEMENTED_UNVERIFIED：closed 优先于 disabled；disable 唤醒等待者并使归还连接销毁；close-before-init 无副作用且 close 幂等/可在 future 取消后续跑清理；restart 在 activeCount 非零时返回独立类型错误，成功前等待旧 factory future 退出并排空 close worker，随后 reset、enable=true、inited=false，generation 淘汰旧异步获取 | Java 每状态 live diff；`restart(Properties)` 动态配置仍缺 |
| SEM-CFG-009 | 密码/配置版本变化使旧连接失效 | `notify_credentials_changed` + pool/holder 原子版本；不复制密码 | PARTIAL/IMPLEMENTED_UNVERIFIED：旧空闲替换、创建竞态和旧活跃归还销毁已接；Java 动态 config 差分待补 | 归还时 discard、创建中切换、取消安全 |
| SEM-CFG-010 | MBean 配置读写结果 | admin config API | TODO | 字段、权限、错误对照 |
| SEM-CFG-011 | `initPhysicalConnection` 按 autoCommit → readOnly → isolation → catalog 初始化，ODPS 跳过 autoCommit | `PoolInner::initialize_physical_connection` | PARTIAL（事件序列契约通过；真实 adapter 能力矩阵待补） | 不同初始状态、空 catalog、unsupported |
| SEM-CFG-012 | 物理连接初始化失败时关闭连接、释放容量并记 connect error | native create failure path | PARTIAL（故障注入契约通过） | 每个 setter 失败、close 失败、并发关闭 |
| SEM-CFG-013 | `resetStatEnable` 默认 true；禁用时 `resetStat()` 完全无副作用且 resetCount 不变；启用时重置数据源池计数、activePeak=当前 activeCount、activePeakTime=0、清 lastCreateError，并增加 resetCount；setter 同步传播到 JdbcDataSourceStat；`restart()` 调用同一门禁 | `DruidPool` 原子 gate/count + `PoolInner#reset_stats` + `JdbcDataSourceStat` 独立 gate；facade getter/setter/reset/count；Factory 解析 `druid.resetStatEnable` | IMPLEMENTED_UNVERIFIED | Java reset 前后字段 snapshot、禁用 restart、active 非零 reset、JdbcDataSourceStat 独立 reset 和并发调用统一验证 |

### `SEM-POOL-*`

保留原全局 ID，便于历史引用和自动审计。

CodeGraph 验证 Java 获取连接主链为 `getConnection` → FilterChain
`dataSource_connect` → `getConnectionDirect` → `getConnectionInternal` →
idle/create/wait → validate → `DruidPooledConnection`。`SEM-POOL-*` 的实现和
差分测试必须覆盖这条调用链，不能只验证最终返回对象。

| ID | 语义 | 当前 | 关键差分场景 |
| :--- | :--- | :--- | :--- |
| SEM-POOL-001 | 优先复用 idle holder | PARTIAL | FIFO/LIFO 与时间字段 |
| SEM-POOL-002 | 空闲为空且容量可用时原子预留并创建 | PASS（CAS 预留 + gated concurrency contract） | 高并发不得越过 maxActive |
| SEM-POOL-003 | 达上限后按公平性等待 | PARTIAL | fair/unfair 唤醒顺序 |
| SEM-POOL-004 | maxWait 超时及错误字段 | IMPLEMENTED_UNVERIFIED（严格 `maxWait > 0` 才启用 deadline，0/负数无限等待；Druid 专用 GetConnectionTimeout 保存 wait/active/max/creating/create elapsed/error count/current cause/running SQL；notFullTimeoutRetryCount 在未满时重新执行完整 maxWait；timeout 不误增 connectErrorCount） | Java message/cause、毫秒边界、running SQL 顺序、notFull 并发统一差分 |
| SEM-POOL-005 | 创建失败退避、重试和 failFast | PARTIAL/IMPLEMENTED_UNVERIFIED（direct creator 与受监管 refill creator 分别维护局部 errorCount；严格 `> retryAttempts` 且正退避才发布 datasource 级 failContinuous；failFast 等待者返回带 current createError cause 的 DataSourceNotAvailable，成功清 current/fail 状态并保留 last history；break 终止 creator；逻辑 connectErrorCount 在 failFast 分支增加一次） | Java 多等待者、零/正退避、失败恢复、break+无限/有限 maxWait、取消 |
| SEM-POOL-006 | testOnBorrow | PARTIAL | valid/invalid/validator error |
| SEM-POOL-007 | testWhileIdle | IMPLEMENTED_UNVERIFIED（按 idle 与 eviction interval 阈值校验） | idle 时间阈值 |
| SEM-POOL-008 | keepAlive 探测与补最小空闲 | IMPLEMENTED_UNVERIFIED（到期探测、队列顺序、成功/失败计数、minIdle 回填与后台调度已接） | minIdle 守恒 |
| SEM-POOL-009 | 连接被 disable/closed datasource 拒绝 | IMPLEMENTED_UNVERIFIED（入口 closed 为携带关闭时间的 `DataSourceClosed`；入口 disabled 及已经进入等待后被 disable/close 唤醒为 `DataSourceDisabled`；外部池才使用通用 `PoolClosed`） | Java 错误类型、关闭时间消息、入口与等待中关闭的分支差分 |
| SEM-POOL-010 | dataSource get/release 与物理 connect Filter | PARTIAL/IMPLEMENTED_UNVERIFIED：外层 get 可修改 maxWait/短路返回，release 可包围或短路 holder 回收，两者均为有位置 around-chain；物理 `connection_connect` 独立覆盖直接与后台 creator，before→factory→真实 ID→reverse after 及错误计数已接 | Java 具体 DruidDataSource 引用、双 Filter 短路/错误展开、Properties 与统一差分 |
| SEM-POOL-011 | active/pooling/connect/wait 计数原子变化 | IMPLEMENTED_UNVERIFIED（connectCount 按内部获取轮次递增；init/timeout 不计 connectError，closed/disabled 入口计一次，fatal/maxWaitThread 拒绝计两次，failFast 计一次；等待门限严格 `current > max`） | Java 中断、关闭唤醒、fatal/timeout 交错及统一并发/取消 counter golden |
| SEM-POOL-012 | async 取消不遗失容量 permit | IMPLEMENTED_UNVERIFIED（create reservation、borrow candidate、shrink/close detached holder、wait registration 均有 Drop 守卫） | 在每个 await 点取消 |
| SEM-POOL-013 | 所有获取入口只返回 `DruidPooledConnection` | PASS（native、`dyn Pool`、dynamic、bb8、deadpool） | native、dynamic、bridge 类型一致 |
| SEM-POOL-014 | 单次 acquire 只选择 native 或 external bridge 一个 Provider | PARTIAL（类型边界已互斥，`DruidDataSource` 配置门禁待建） | 配置互斥；禁止 pool-in-pool |
| SEM-POOL-015 | Provider 返回带唯一回收权的连接租约 | PARTIAL（`DruidPooledConnection` FnOnce + external `PhysicalConnectionLease`） | 成功、超时、取消、Provider 关闭 |

### `SEM-REC-*`

保留原全局 ID，便于历史引用和自动审计。

Java `recycle` 的顺序是迁移契约：移除 abandoned trace → 必要时 rollback →
holder reset → discard/寿命/使用次数/密码版本判断 → closed/testOnReturn →
schema 恢复 → datasource enable → phyTimeout → active/close/recycle 计数 →
`putLast` 或物理关闭。

| ID | 语义 | 当前 | 验收 |
| :--- | :--- | :--- | :--- |
| SEM-REC-001 | close/Drop 只归还一次 | PASS（显式 close、重复 close、Drop 契约） | 重复 close、move、panic |
| SEM-REC-002 | `dyn Pool` 路径同样归还 | PASS（无 `into_core` 转换；Dynamic 计数守恒测试） | trait-object route 与 concrete pool 一致 |
| SEM-REC-003 | 非 autoCommit 且非 readOnly 时 rollback；失败由 close 吞掉、连接淘汰并增加 recycleError | PARTIAL（Java/Rust 成功、readOnly、失败事件契约通过；真实驱动待补） | rollback 成功/失败 |
| SEM-REC-004 | holder reset 顺序：readOnly、holdability、isolation、autoCommit、listener/statement trace、warnings；验证后恢复 initSchema | PARTIAL/IMPLEMENTED_UNVERIFIED（连接属性恢复已与 clearWarnings 分段；ConnectionEventListener/StatementEventListener 清理、普通/Prepared/Callable trace 强引用关闭及 PS cache 归还/删除已接；MySQL-family initSchema 已在 testOnReturn 后恢复且 schema 变化清 PS cache；Java 差分仍缺） | 状态逐项恢复、任一步失败 |
| SEM-REC-005 | testOnReturn；invalid 时 destroy 且不计 recycleError | PARTIAL（validator 成功/失败与计数契约通过；真实驱动待补） | invalid、validator error、timeout |
| SEM-REC-006 | fatal/discard/closed 连接不回池 | IMPLEMENTED_UNVERIFIED（nullable 显式 discard、holder 一次性处置、closed/adapter marker、active/discard/close 计数、boolean refill signal 与受监管 creator 已接；sorter fatal 现先记录同锁状态并立即 take-once discard，阈值严格 `>`、借用门禁、错误 cause/时间/UTF-16 SQL、validation 清除及一次 fatal refill 均进入生产链） | Java `OnFatalErrorMaxActiveTest*`、并发 fatal、driver error、补池与计数 |
| SEM-REC-007 | phyMaxUseCount、phyTimeout；Rust 额外 maxLifetime 默认禁用 | PARTIAL（phyMaxUseCount 与 Java MaxPhyTime/shrink/归还 phyTimeout 边界已验证；管理面绑定和后台维护待补） | 边界时刻 |
| SEM-REC-008 | password/config version 淘汰 | PARTIAL/IMPLEMENTED_UNVERIFIED：池级原子 `userPasswordVersion`、factory 调用前版本快照、旧空闲连接批量替换、旧活跃连接归还销毁及借出/新建竞态门禁已接；`notify_credentials_changed()` 要求调用方先原子更新 factory，池不复制密码；Java 动态 config 与并发差分待补 | 版本切换、创建中竞态、活跃归还、取消安全 |
| SEM-REC-009 | 满池返还物理关闭且计数正确 | PARTIAL | 与并发 create 竞争 |
| SEM-REC-010 | removeAbandoned 追踪与关闭 | IMPLEMENTED_UNVERIFIED（weak lease、running 跳过、timeout 失效、backtrace 管理面；物理关闭由 owner 后续操作/Drop 完成） | owner、timeout、stack trace |
| SEM-REC-011 | shrink/idle eviction/minEvictableIdleTime/maxEvictableIdleTime | IMPLEMENTED_UNVERIFIED（显式/后台 shrink、min/max idle、phyTimeout、keepAlive、失败补池与取消安全已接；每轮冻结 fatal count 增量，故障前连接不受 keepAlive 开关影响而强制重验，成功清门禁、失败销毁并触发一次 refill） | minIdle/keepAlive/fatalError 统一组合差分 |
| SEM-REC-012 | `tryGetConnection/isFull/fill()/fill(int)`、私有 `emptySignal(int)` 协调、restart | PARTIAL/IMPLEMENTED_UNVERIFIED（tryGet 零 pooling 不建连、isFull、initialSize、public fill 到 maxActive、fill(toCount) 截断、每池唯一 create worker 接收 refill target、Notify/direct-create 协调与 restart 已接；restart 保留 activeCount 门禁、统计 reset、惰性再 init 与异步 generation 隔离；`restart(Properties)` 未闭合。Java 1.2.28 本类没有公开 purge/empty API，旧账目已纠正） | 维护操作、统计、动态配置 |
| SEM-REC-013 | creator/destroy task 关闭可等待 | IMPLEMENTED_UNVERIFIED（维护与每池唯一 close worker 均保存 JoinHandle；close/restart 可排空无活动租约的 worker，idle close 取消安全） | graceful shutdown、活跃租约归还与取消 |
| SEM-REC-014 | statement cache 清理 | PARTIAL（holder take/discard、pool destroy、显式 clear 与 MySQL schema 变化已清理；recycle 全异常矩阵、listener/result-set trace 待补） | recycle error/discard/schema change/in-use |
| SEM-REC-015 | 一个 lease 只有一个归还策略且至多执行一次 | PASS（native/bb8/deadpool exactly-once contract） | close、Drop、panic、重复 close |
| SEM-REC-016 | async recycle 失败进入受监管回收，不在 Drop 中阻塞 | IMPLEMENTED_UNVERIFIED（脏连接/要求 testOnReturn 的 Drop 只向每池唯一 `ConnectionCloseWorker` 入队；pool 持有任务句柄，最后租约归还后 FIFO shutdown） | runtime 内外 Drop、worker 关闭竞态、shutdown |
| SEM-REC-017 | native 归还 Druid idle queue；bridge 归还原外部池 | PASS（native active/idle 与两种 external reuse 契约） | 两种模式计数和所有权分别守恒 |

2026-07-28 证据批次：

- Java 基线提交 `33824c3dec1612711f9bb4e409319bcab2e4cd0e`：创建/回收目标用例通过；PreparedStatement oracle 另执行 `DruidConnectionHolderTest4`、两组 `PreparedStatementKeyTest`、`PSCacheTest3`、`DruidDataSourceTest_clearCache`，21/21 测试方法通过；Callable oracle 执行 `PoolableCallableStatementTest` 与 `ConnectionTest4`，101/101 通过。
- Rust `crates/druid/tests/recycle_semantics_test.rs` 12/12：验证事件序列、状态快照、canonical holder 往返和计数。
- Rust `crates/druid/tests/druid_connection_holder_test.rs` 6/6：验证 holder 所有权、时间/useCount/版本、默认状态与 schema 恢复。
- Rust `crates/druid/tests/maintenance_semantics_test.rs` 5/5：验证 shrink/phyTimeout/keepAlive 主分支。
- external bridge 的 bb8/deadpool 各有 2 个真实 SQLite 回收用例：显式事务 close 复用原物理连接；脏事务 Drop 标记 broken/discarded 后创建替代连接。
- PreparedStatement 证据为 Rust core 6/6、pool 6/6、SQLx SQLite 5/5（其中 2 个 prepared 新契约）、RBDC 3/3，覆盖完整 key、access-order LRU、share/inUse、异常移除、schema clear、跨租约拒绝、真实 SQLx statement 执行和 RBDC token 委托。
- CallableStatement 的 Java 116 个公开声明冻结为构造器加 115 个方法；Rust 115 个同语义入口已闭合，另有 5 个生命周期/扩展方法。现有合同覆盖 `Precall1/2/3`、缓存、IN/OUT、LOB/流、`wasNull`、异常淘汰和非 callable 句柄拒绝；支持存储过程的真实 Adapter 与 vendor 矩阵仍开放。
- 真实 SQLx/RBDC 数据库矩阵尚未覆盖 readOnly/isolation/holdability 等可选能力，因此相关契约保持 `PARTIAL`，不提前标为 `PASS`。

### `SEM-CONN-*`

保留原全局 ID，便于历史引用和自动审计。

| ID | 语义 | 原总账状态 | 验收条件 |
| :--- | :--- | :--- | :--- |
| SEM-CONN-001 | autoCommit/readOnly/isolation/catalog/schema/holdability | PARTIAL | getter/setter/unsupported |
| SEM-CONN-002 | commit、rollback、savepoint | PARTIAL | 成功、错误、状态 |
| SEM-CONN-003 | network/query timeout 与 cancel | TODO | driver cancellation |
| SEM-CONN-004 | statement execute/query/update/largeUpdate/batch | PARTIAL（普通/Prepared/Callable 共用内核现保存独立 `StatementExecuteType`、最近 SQL、开始单调时点、耗时、首结果类型和 updateCount；普通 Statement batch 的 `int[]`、`-2/-3`、部分失败、单元素 updateCount 和批次保留已接；Prepared batch 同步更新最近状态；largeUpdate 与原生多结果仍缺） | 返回类型、行数、最近状态 Java live diff |
| SEM-CONN-005 | prepared 参数类型、null、LOB、清理和 batch | PARTIAL（48 个 JDBC setter 重载已成为持久 `PreparedInputParameter` 状态，保留 nullable/Calendar/typeName/targetSqlType/scale/长度/资源身份；九个 `JdbcParameter` Java 对象各有真实 Rust 文件，Prepared 描述符实现 value/length/calendar/sqlType 合同及 0-based Proxy 参数视图；重复覆盖、稀疏拒绝、clear 成功时机、setter fatal-discard、绑定 query/update/generic execute 与有序 batch 已接；Toasty/SQLx SQLite 与 RBDC 严格 SPI 在物理 setter 阶段物化资源且执行不二次读取） | JdbcParameter Java live/golden diff、RBDC 组合实库、SQLx Any 与多数据库资源矩阵待补 |
| SEM-CONN-006 | callable IN/OUT 参数 | PARTIAL（三种 prepareCall key、六种 OUT 注册、标量/Decimal/日期时间/Blob/Clob/NClob/Reader 命名 IN、索引/命名 OUT、LOB 资源与精确长度重载、wasNull、缓存与异常删除已迁移；self/Callable/Prepared/raw concrete unwrap、关闭后身份、query/execute/current/generated-keys 继承结果入口及 Callable ResultSet 动态身份已闭合；真实存储过程 Adapter 仍待补） | Java 101 个 callable oracle + wrapper 28/28；Rust typed/LOB/pool/unwrap/result-set identity + SQLite unsupported 能力检测 |
| SEM-CONN-007 | result set next/close/fetch row count/hold time | PARTIAL（next/previous 成功计数、历史峰值、显式 close 与 Statement trace 级联 close已验证；Prepared 与 Callable ResultSet 均保留同一具体 statement 强身份，原变量 Drop 不提前回收且 handle close 级联关闭；eager 行集无伪 streaming；完整 getter/update、Calendar、warning/metadata 已接线） | streaming 提前退出、hold-time/stat/filter、完整/真实 label metadata、多数据库与真实存储过程 ResultSet |
| SEM-CONN-008 | metadata、unwrap 与 Proxy identity/attributes | PARTIAL（`WrapperAdapter`、`PoolableWrapper` 的 null/self/raw/statement/proxy-delegate 顺序已闭合；池化 Connection/Statement/Prepared/Callable/ResultSet 具有数据源级 ID、惰性 attributes 和各自 Physical SPI unwrap；Connection 保留时间/验证/活动事务、逻辑驱动 properties 及连接事件身份；Statement 创建/关闭事件保留真实 ID；ResultSet 保留 construct/读量/资源计数、双向列映射与 nullable hidden columns；Prepared/Callable ResultSet 恢复共享句柄；ResultSetMetaData proxy 及 DatabaseMetaData 完整 SPI/借用 proxy 已建立；DatabaseMetaData raw SPI 由 `raw/raw_mut` 安全暴露） | Adapter vendor properties、完整驱动字段与统一差分仍待闭合；借用型具体 driver `Any` downcast 按平台边界 JAVA_ONLY_EXEMPT |
| SEM-CONN-017 | DataSourceProxy 统计/驱动/URL/Filter/properties 与 ID 序列 | PARTIAL/IMPLEMENTED_UNVERIFIED：canonical DruidDataSource 实现 DataSourceProxy；raw driver 为未池化 PhysicalConnectionFactory；管理 ID 稳定；connection/statement/resultSet/metadata/transaction 首值分别为 10001/20000/50000/80000/60000；配置与 raw URL、Filter 名称、逻辑驱动属性可观察 | DataSourceProxyConfig callbacks/JMX、driver version、composite 字段和 Java live diff |
| SEM-CONN-018 | ResultSetMetaData Proxy 身份、raw、ResultSet 归属与列访问 | PARTIAL/IMPLEMENTED_UNVERIFIED：独立 Proxy/Impl 文件；80000 起始 ID；ResultSet ID 避免自引用环；attributes/raw Wrapper；现有 ResultSetMetaData 21 方法继续精确物理委托 | metadata 21 方法专用 Filter around-chain、错误分类差分 |
| SEM-CONN-019 | DatabaseMetaData 176 声明、raw 委托、连接身份与 Adapter 能力 | PARTIAL/IMPLEMENTED_UNVERIFIED：173 方法完整 Physical SPI；nullable String/ResultSet/RowIdLifetime 无损类型；借用 proxy + pooled connection 生产入口；三个 Java 历史委托分支；Toasty/SQLx/RBDC 接线；SQLx SQLite 已显式覆盖 160/173，并从真实 schema/PRAGMA 实现 table/column/PK/index/FK/type-info；Connection metadata Filter around-chain 与 Wall privileged task-local 已接 | 13 个平台/driver unsupported 边界、SQLx Any vendor 覆盖、Java/真实 SQLite live diff；具体 driver class downcast 为 JAVA_ONLY_EXEMPT |
| SEM-FLT-031 | `Connection#getMetaData` 必须从 position 0 进入 Filter；Wall `metadataAllow/logViolation/throwException/doPrivilegedAllow` 决定阻断、记录、privileged 绕过或继续 | `ConnectionDatabaseMetaDataFilterChain` + `DruidPooledConnection#database_meta_data` + `WallFilter#connection_get_meta_data` | PARTIAL/IMPLEMENTED_UNVERIFIED：默认继续、raw 末端、连接生命周期、事务追踪、Wall 三开关及同步/thread-local + async/task-local privileged 已落 | Java/Rust live differential 待闭合 |
| SEM-WALL-025 | `WallProvider#doPrivileged` 嵌套恢复；privileged SQL 只增加 checkCount，不解析、不缓存、不产生 sqlStat | `do_privileged` + `scope_privileged` + nullable `WallCheckResult.sql_stat` | IMPLEMENTED_UNVERIFIED | Java `DoPrivilegedTest` live/mirrored disposition；Rust panic/cancel/跨 worker obligation |
| SEM-CONN-009 | fatal exception 使 holder discard | PARTIAL（全部 Java vendor sorter 已迁移；23 条已公开 `PhysicalConnection` 错误路径、PreparedStatement setter 与两条缓存清理错误路径统一经 sorter 标记 holder 与物理 Adapter；尚缺 Statement 全操作、listener/统计及多数据库 Adapter 字段矩阵） | Java vendor 23/23 + Oracle 异常全族 53/53；Rust sorter 10/10 + Connection 全路径 1/1 + prepared setter/cleanup；真实 SQLite Toasty structured-error 与 SQLx DatabaseError→SqlException→fatal→Discard |
| SEM-CONN-010 | XA prepare/commit/rollback/recover | TODO | 2PC 故障注入 |
| SEM-CONN-011 | `PhysicalConnection` 只表达物理数据库能力，不含池化/Filter/统计 | PASS（独立 object-safe SPI；旧 `Connection` 仅 alias） | API surface 与依赖检查 |
| SEM-CONN-012 | `SqlxConnectionAdapter`、`RbdcConnectionAdapter` 通过同一 direct contract | PARTIAL（两者已接通 create/exec/fetch/transaction/savepoint/close/warnings/clear_warnings/database_meta_data；Callable 当前都显式 unsupported；SQLx warning 由真实 SQLite 验证） | reset、cancel、metadata 完整字段/ResultSet、RBDC 真实数据库及 callable 能力矩阵 |
| SEM-CONN-013 | bb8/deadpool 通过 Provider contract，而非 `PhysicalConnectionFactory` | PASS（两者直接实现 core `Pool`） | acquire/timeout/return/broken lease |
| SEM-CONN-014 | adapter 统一上报 capability 与结构化 `PhysicalError` | TODO | unsupported、transient、fatal、timeout、cancel |
| SEM-CONN-015 | `PreparedStatement` 继承真实 `Statement` 属性；池化 wrapper 构造时独立读取 maxFieldSize/maxRows/queryTimeout/fetchDirection/fetchSize，getter 失败仅保留零默认值；setter 在物理调用前更新 current；关闭时按上述顺序只恢复变化项，首错即停，成功后才 clear/cache | `PhysicalPreparedStatement` 暴露继承属性；`PreparedStatementPhysicalStatement` 仅关闭逻辑基类视图，避免误关待缓存物理句柄；`DruidPooledPreparedStatement` 保存 default/current 并按 Java 顺序恢复；Toasty/SQLx statement 状态由各自 Adapter 持有 | PASS（属性/恢复生命周期） | Java 源码调用顺序审计；Rust 故障注入验证首错停止、重试与 exceptionCount 淘汰；真实 Toasty SQLite 验证属性转发、恢复和同物理句柄复用 |
| SEM-CONN-016 | Prepared 资源 setter 必须先调用物理句柄；资源读取/长度校验错误在 setter 返回，Filter 与 batch 保留原 setter 描述符，execute 使用物理句柄已物化快照且不二次读取 | `ToastyPreparedStatement`、`SqlxPreparedStatement` 与 `RbdcPreparedStatement` 在 `set_parameter` 物化值并保存 1-based 槽位；`PreparedInputParameter` 贯穿单次/批次 Filter；`add_parameter_batch` 保存有序物理值快照；`PhysicalConnection::*_prepared_parameters` 保持跨 Adapter SPI；Ref/Array/vendor custom 不做字符串降级 | PASS（Toasty/SQLx SQLite、RBDC SPI）；PARTIAL（全 Adapter） | xerial SQLite JShell；真实 Toasty/SQLx SQLite；RBDC 严格 SPI 验证 stream/reader/Blob/Clob/NClob/SQLXML/Object setter/query/update/batch 和错误时序；隔离真实 `rbdc-sqlite` 验证参数值 |

### `SEM-FLT-*`

保留原全局 ID，便于历史引用和自动审计。

| ID | Java 语义 | Rust 当前 | 状态 | 验收 |
| :--- | :--- | :--- | :--- | :--- |
| SEM-FLT-001 | Filter 按配置顺序执行 before | `FilterChain::before_execute` | PARTIAL | 顺序/短路 |
| SEM-FLT-002 | after 在成功和失败均执行，顺序正确 | `after_execute(...).await` | PASS（completion cancellation 另见 SEM-FLT-016） | exactly-once |
| SEM-FLT-003 | `exec` 全上下文透传 | before/after 复用同一 `ExecContext` | PASS | 参数、name、start、fingerprint |
| SEM-FLT-004 | `fetch` 同样经过 chain | 统一 before/after，after 含 row_count | PASS | before/after + row stat |
| SEM-FLT-005 | connection_* hooks | 事件 enum，仅部分调用 | PARTIAL | 每个 Java hook |
| SEM-FLT-006 | statement/prepared/callable hooks | 事件 enum | PARTIAL | 全执行入口 |
| SEM-FLT-007 | resultSet/LOB/metadata hooks | ResultSet next/close 已接同步 around-chain；其余方法族与 LOB/metadata 未接 | PARTIAL | 全访问族与 close |
| SEM-FLT-008 | init/destroy/configFromProperties | canonical `FilterChainImpl` 对显式 Filter 按注册顺序执行 connection properties→system properties→init；Java 自动 provider 在 init 后追加、不回溯配置或初始化；destroy 按顺序且单个错误不阻断后续清理。LogFilter connection 配置和 StatFilter 两阶段配置已进入 Factory | PARTIAL/IMPLEMENTED_UNVERIFIED | 其余 Filter 配置项、配置失败/生命周期时序统一差分 |
| SEM-FLT-009 | dynamic-dispatch chain 游标 | canonical `FilterChainImpl` 已落位；Connection/Statement warning 与 ResultSet 185 方法族每次从 position 0 进入有位置子链，支持 Filter 调用 chain 下一节点、短路和返回改写；SQL/batch/事件当前仍是扁平 before/逆序 after，未复现所有 Java nested downstream-before 局部展开 | PARTIAL | 补齐其余对象族和统一 position 模型 |
| SEM-FLT-010 | Filter alias/SPI/order | canonical `FilterManager` + bundled aliases +显式 factory；`AutoLoad` 以 inventory 映射 `@AutoLoad + ServiceLoader`，保留 value/skip 开关和“显式 Filter init 后再追加自动 provider”的 Java 时序；显式 order 承载 provider-file 顺序 | PARTIAL/IMPLEMENTED_UNVERIFIED | JVM 三 ClassLoader 自动资源扫描、跨 crate inventory provider 与失败/重复/order 统一验证 |
| SEM-FLT-011 | WallFilter 接入 | canonical `WallFilter` 已实现 SQL/batch Before/After、SELECT/UPDATE/INSERT tenant AST 改写、普通 Statement 物理透传与 addBatch 保存前改写、Prepared/Callable prepare/cache-key 改写、ResultSet raw metadata 隐藏列生产和 tenant next 回调，并由内置 FilterManager 工厂装配 | PARTIAL/IMPLEMENTED_UNVERIFIED | 四种 log/throw 组合、完整 Java hook 与方言规则统一验证 |
| SEM-FLT-012 | StatFilter 接入 | SQL before/after、普通与 PreparedStatement generic execute/batch、update count、活动事务、ResultSet、pool get/release、真实物理 connect/close 与全局 context listener 已接；physical connect 自行执行 before/error/after/Entry 注册，physical close 在下游 raw close 前增加计数、按 ID 删除 Entry 并记录寿命；两者都只在启用 StatFilter 时发生；MBean 有效合同合并为管理 API，connection→system 配置顺序、动态有符号 slowSqlMillis、logSlowSql/level 与 mergeSql 已接生产 Factory | PARTIAL | LOB、慢参数文本、方言参数化及统一验证 |
| SEM-FLT-013 | LogFilter 输出配置 | canonical tracing-backed `LogFilter` 只保留 Druid 事件开关、七个兼容 properties 键、管理配置面及事件分支；`log` 仅为 Druid Filter 配置短名；SQL/batch、Statement 创建/关闭、connection commit/rollback、pool get/release、真实 physical connect/close 与 ResultSet open/next/close/error 均携带相应 Proxy ID；connect/close 成功事件只在下游 raw 操作成功后输出；Java 未消费的 `connectionConnectBeforeLogEnable` 只保留配置可观察性，不伪造 before 日志。Log4j/SLF4J/Commons Logging 对象均 `NOT_APPLICABLE` | PARTIAL/IMPLEMENTED_UNVERIFIED | 完整 ResultSet 值、动态 tracing level、SQL 参数插值/pretty 输出、Prepared close hook 次级错误和统一行为验证 |
| SEM-FLT-014 | ConfigFilter 解密/远程配置 | canonical `ConfigFilter` + deprecated `ConfigTools`；精确保留三组属性优先级、Java `connectionProperties` 分号语法、ISO-8859-1 properties/Unicode escape、XML properties、file/classpath/HTTP(S)、默认与自定义 RSA 公钥、Java 错误前缀；`DruidDataSourceFactory::resolve_config_properties` 保证内置 Toasty 和扩展 adapter 可先解析再构造物理 factory，`config` alias 也进入真实 FilterManager | PARTIAL/IMPLEMENTED_UNVERIFIED | Java/Rust 旧密文 live differential、HTTP redirect/status/XML DTD、进程环境桥、外部 driver 凭据和真实 SQLite 统一验证 |
| SEM-FLT-015 | Encoding/DateTime filter 结果 | `CharsetConvert`/`CharsetParameter`/`EncodingConvertFilter` 与 `MySQL8DateTimeSqlTypeFilter`/`MySQL8DateTimeResultSetMetaData` 均为独立 canonical 对象；encoding alias、client/server 属性动态配置、普通 execute/prepare/addBatch SQL、Clob string/Reader、ResultSet string/object/type-map/Reader replacement 转码已接；LocalDateTime→Timestamp metadata 和 mysql8DateTime alias 已接 | PARTIAL/IMPLEMENTED_UNVERIFIED | Prepared/Callable 参数描述符生产链、真实编码 fixture 和 MySQL 8 driver fixture |
| SEM-FLT-016 | execute/query future 被取消后仍产生唯一 completion outcome | 无 completion guard | TODO | 每个 await 点、行流提前 drop、队列关闭 |
| SEM-FLT-017 | `StatFilterContext` 是进程单例；listener 允许重复注册、删除首个相同实例，事件按 Java 下标循环动态分发且首个异常中止 | `OnceLock` + COW `RwLock<Vec<Arc<...>>>`；14 个事件全覆盖；SQL execute/update、活动事务 commit/rollback 与 ResultSet open/fetch/close 已接生产链 | PARTIAL | Java JShell 动态增补/重复/删除及 SQL/事务 oracle；Rust 全事件、异常短路和真实 SQLite；pool/physical/LOB 等生产接线待补 |
| SEM-FLT-018 | execute before 在物理操作前；after 在本地统计后、按逆序传播，listener 异常可覆盖物理结果；commit/rollback listener 仅在对应物理操作成功后触发，保存点 rollback 不发送全局 rollback | `ExecContext.in_transaction/operation` + `FilterChain::add_filter/after_execute/after_connection_event`；池化连接按 Java 时序调用并传播首错 | PASS（已覆盖入口） | Java StatFilter/MockDriver oracle；真实 SQLite 验证 before 失败无副作用、after/commit/rollback 失败时物理副作用已发生及保存点边界 |
| SEM-FLT-019 | 普通 Statement batch 仅进入/退出 Filter 链一次；before SQL 按 `"\n;\n"` 合并，成功逐项发送 `int[]` update count 后以 nullable `lastExecuteSql=null` 发送 after，失败以合并 SQL 和 `BatchUpdateException` 发送 after；after listener 可覆盖已经完成的物理 batch | `BatchExecContext` + `before_batch/after_batch`；`PhysicalConnection::exec_batch` 返回 `Vec<i32>`，`DruidError::BatchUpdateException` 保存部分计数和原始 SQL cause | PASS（普通 Statement） | Java MockDriver JShell `[1,-2]`/partial oracle；真实 SQLite 单次事件、部分副作用、nullable SQL 与 listener 覆盖时序 |
| SEM-FLT-020 | PreparedStatement batch 只进入/退出 Filter 链一次；before/after SQL 都是固定预编译 SQL；继承的 `batchSqlList` 为空，故 Java StatFilter batch size 为 0；成功逐项发送 update count；before 短路不消费，物理调用后成功/失败均消费参数批次 | `BatchExecKind::PreparedStatement` + `BatchExecContext.parameter_sets`；`DruidPooledPreparedStatement` 保存 add-time 快照；`PhysicalConnection::exec_prepared_batch` 提供可覆盖的驱动边界 | PASS（Druid/Toasty SQLite 主链） | Java Druid+xerial SQLite JShell：`[1,1]`、repeat `[]`、stat `0:0`、固定 SQL 回调；Rust 真实 Toasty SQLite：快照、清理独立性、部分副作用、before 重试及相同 Filter/Stat 时序 |
| SEM-FLT-021 | 普通 Statement generic execute 的 before/after 各一次；SELECT 返回结果集且 update count 为 `-1`，DML 返回 update count；`getMoreResults` 成功后逻辑关闭旧 wrapper，失败不关闭；generated keys 由独立结果集 trace；StatFilter 对 generic DML 只累加 SQL 级 update count，不发送全局 update-count listener | `ExecOperation::Execute` + `StatementExecuteResult` 顺序结果游标；`PhysicalConnection::execute` 和 `PhysicalStatement` getter/推进错误钩子；Toasty `RawSqlRet::Infer` 由 SQLite prepared statement 元数据区分 query/update | PASS（普通 Statement core/Toasty SQLite） | Java Druid+xerial SQLite JShell 与 42 个 Maven 用例；Rust 真实 SQLite SELECT/INSERT/生成键/无效 current/列选择不支持，以及 mock 顺序多结果和错误 sorter |
| SEM-FLT-022 | PreparedStatement generic execute 使用固定预编译 SQL 与当次参数快照，before/after 各一次；结果类型、update count、多结果推进、generated keys 与普通 Statement 同状态机；成功推进关闭旧 wrapper，失败保持开放；物理 getter 错误在调用点进入 sorter | `PhysicalConnection::execute_prepared` + `DruidPooledPreparedStatement.statement_base`；`PreparedStatementKey::statement_generated_keys` 保留 M4/M5/M6；Toasty SQLite driver Infer 判定 query/update | PASS（Druid/Toasty SQLite 主链） | Java Druid+xerial SQLite JShell + Maven 46/46；Rust 真实 SQLite 默认/auto/index/name generated keys、参数 Filter、无效 current、getter fatal sorter及 mock query→update→query |
| SEM-FLT-023 | Connection、Statement、PreparedStatement 与 ResultSet 的 `getWarnings/clearWarnings` 每次从 Filter position 0 开始；Filter 可短路、改写警告链，按进入顺序调用并逆序展开；Connection getter 不调用 `handleException`，clear 调用，Statement/PreparedStatement/ResultSet 两者均进入异常分类 | `ConnectionWarningFilterChain`、`StatementWarningFilterChain` 与扩展后的 `ResultSetFilterChain`；`SqlWarning` 保留 message/SQLState/vendor code/next warning；Toasty/SQLx SQLite 返回真实无警告并支持 clear，RBDC 公开 SPI 同合同 | PASS（Druid + 三类 Adapter warning 主链） | CodeGraph Java 六条 FilterChainImpl warning 路径；xerial SQLite 四类对象 null 与两层 Filter oracle；Java Maven 120/120；Rust Toasty/SQLx 真实 SQLite、RBDC SPI、Filter 顺序/改写、fatal sorter 及默认 SPI |
| SEM-FLT-024 | ResultSet 的 String/Boolean/Byte/Short/Int/Long/Float/Double/Bytes 必须按 index/label 精确重载从 position 0 进入 Filter；Filter 可短路或改写返回值，进入顺序与逆序展开保持不变；Filter 错误不触达后续物理 getter，并在池化调用点进入异常分类 | `ResultSetFilter`、`ResultSetFilterChain`、`FilterChain` 和 `DruidPooledResultSet` 的 18 条同名入口 | PASS（标量 getter 子族） | Java `FilterChain`/`FilterChainImpl` 18 条签名逐项对照；Rust 双层 wrapping、全重载 pass-through/short-circuit、DriverError 分类与真实 Toasty SQLite |
| SEM-FLT-025 | ResultSet BigDecimal 的无 scale/有 scale、Date/Time/Timestamp 的无 Calendar/Calendar 重载必须分别进入精确 Filter 方法；Calendar 参数要区分未调用重载、显式 null 与具体时区；Filter 可短路/改写，错误在池化调用点分类 | `ResultSetFilter`、`ResultSetFilterChain`、`FilterChain`、`DruidPooledResultSet` 的 16 条强类型入口；`JdbcCalendarArgument` 保留三态 | PASS（decimal/temporal getter 子族） | Java `FilterChain`/`FilterChainImpl` 16 条签名逐项对照；Rust 全重载 pass-through/short-circuit、scale/Calendar 参数身份、DriverError 分类及真实 Toasty SQLite index 读取 |
| SEM-FLT-026 | ResultSet `getObject(int/String)`、`getObject(int/String, Map)` 与 `getObject(int/String, Class<T>)` 必须分别从 position 0 进入精确 Filter；标签重载不得退化为 `findColumn + index`，Java `null Map`、空 Map、映射内容和目标类型身份必须保留；Filter 可短路/改写并在池化调用点分类错误 | `ResultSetFilter`、`ResultSetFilterChain`、`FilterChain`、`DruidPooledResultSet` 的六条 object 入口；`JdbcTypeMap`、`JdbcTargetType` 原样穿透 | PASS（object Filter 子族；嵌套 ResultSet/Clob 自动代理另列 PARTIAL） | Java `FilterChain`/`FilterChainImpl` 六条签名和末端 raw 重载逐项对照；Rust 默认穿透、全重载短路、null/empty/populated Map、custom target、DriverError 分类与真实 Toasty SQLite plain/typed index 读取 |
| SEM-FLT-027 | ResultSet 的 Ref/Blob/Clob/Array/URL/RowId/NClob/SQLXML，以及 Ascii/Unicode/Binary/Character/NCharacter stream 必须按 index/label 分别从 position 0 进入精确 Filter；标签不得在池化层折叠成 `findColumn + index`，资源 Clone 必须共享游标/关闭状态；Filter 可返回 null、资源句柄或错误 | `ResultSetFilter`、`ResultSetFilterChain`、`FilterChain`、`DruidPooledResultSet` 的 26 条入口；`PhysicalResultSet` 补齐五类流的标签与 NCharacterStream SPI | PASS（stream/resource Filter 子族） | Java 三层 26 条签名及末端 raw 重载逐项对照；Rust 默认穿透全重载、短路、资源共享游标、DriverError 分类；真实 Toasty SQLite binary/NCharacter index 实读（Toasty alias 缺失单列 PARTIAL） |
| SEM-FLT-028 | ResultSet 的 wasNull/findColumn、previous/first/last/beforeFirst/afterLast/absolute/relative/getRow、四个游标位置判断、fetch direction/size、type/concurrency/holdability/cursorName、三种 row 状态和 isClosed 共 26 个调用必须每次从 position 0 进入 Filter；int/label 参数、返回改写与错误分类不可丢失 | 四层 navigation/property 精确入口；`is_closed_with_connection` 承载 Java 可失败调用，`is_closed` 只作 Rust 内部生命周期快照 | PASS（navigation/property Filter 子族） | Java `FilterChain`/`FilterChainImpl` 26 条方法逐项对照；Rust 默认物理穿透、全方法短路、参数身份、findColumn DriverError 计数；真实 Toasty SQLite before/next/last/previous/after/before 状态机 |
| SEM-FLT-029 | ResultSet `getNString(int/String)` 虽与 Rust `String` 共用 Unicode 表示，仍必须进入独立 Filter/物理 SPI，不能伪装成 `getString`；index/label 重载、null、Unicode 与错误分类保持不变 | `PhysicalResultSet::n_string/n_string_by_label` 及四层 `result_set_get_n_string*` 精确入口 | PASS（NString Filter 子族） | Java 三层两条签名与 raw 末端逐项对照；Rust 物理重载日志、短路返回、label DriverError 计数及真实 Toasty SQLite 中文 index 读取 |
| SEM-FLT-030 | ResultSet `getMetaData()` 必须从 position 0 进入 Filter；Filter 可以返回替代 metadata 或错误，默认末端必须保留真实 driver metadata SPI/Wrapper 句柄而非复制字段快照 | 四层 `result_set_get_meta_data` 返回拥有后端的 `ResultSetMetaData` 平台对象 | PASS（metadata Filter 子族） | Java 三层签名与 raw 末端静态对照；Rust 物理穿透、短路空 metadata、DriverError 计数；真实 Toasty SQLite 两列 metadata |
| SEM-FLT-031 | ResultSet `getStatement()` 必须从 position 0 进入 Filter；默认返回创建结果集的同一逻辑 Statement，且运行时普通、Prepared、Callable 身份不可抹平；Filter 可返回替代共享句柄或错误，Clone 不得创建第二个逻辑对象 | `ResultSetStatement` 三分支拥有型共享句柄 + 四层 `result_set_get_statement`；`statement_object` 保留动态身份并在原 Statement 上分类错误 | PASS（statement platform-object Filter 子族） | Java Filter/FilterChain/FilterChainImpl 静态签名；Rust 普通 Statement 默认穿透、替换、DriverError 计数，Prepared/Callable 同一对象与继承身份；真实 Toasty SQLite ResultSet |
| SEM-FLT-032 | ResultSet `insertRow/updateRow/deleteRow/refreshRow/cancelRowUpdates/moveToInsertRow/moveToCurrentRow` 七个无参 mutation 调用必须分别从 position 0 进入 Filter；Filter 可短路，默认末端调用 raw 同名方法，错误在池化调用点归入原 Statement；只读驱动的七种 capability error 不得折叠 | `ResultSetFilter`、`ResultSetFilterChain`、`FilterChain` 与 `DruidPooledResultSet` 七条精确入口 | PASS（row-mutation Filter 子族；基础列 setter 由 `SEM-FLT-033` 关闭，其余 setter 仍 PARTIAL） | Java `Filter`/`FilterChain`/`FilterChainImpl` 与 `ResultSetProxyImpl` 七签名静态对照；Rust 精确物理调用日志、全方法短路、refresh DriverError 计数；真实 Toasty SQLite 七种 UnsupportedOperation |
| SEM-FLT-033 | `updateNull` 及 Boolean/Byte/Short/Int/Long/Float/Double/BigDecimal/String/Bytes/Date/Time/Timestamp 必须按 index/label 形成 28 个独立 Filter 调用；nullable 引用值、数值宽度、日期时间类型和标签身份不得被通用 update 抹平；默认末端才编码为 `ResultSetUpdate`，Filter 可短路，错误归入原 Statement | `ResultSetFilter`、`ResultSetFilterChain`、`FilterChain` 与 `DruidPooledResultSet` 28 条精确入口；`ResultSetUpdate` 仅作为物理 SPI 描述 | PASS（基础标量/时间 setter 子族；object、资源对象、LOB stream/Reader、通用 stream 与 NString 子族分别由 `SEM-FLT-034..038` 关闭） | Java 四层 28 签名静态对照；Rust 28 条精确物理参数日志与全量短路；DriverError/exceptionCount；真实 Toasty SQLite 28 条 index/label UnsupportedOperation |
| SEM-FLT-034 | ResultSet `updateObject` 的 plain 与 `scaleOrLength` 版本必须各自保留 index/label 四个重载；SQL NULL、整数/字节等强类型、vendor custom 对象运行时身份及负 `scaleOrLength` 不得在通用 update 中丢失；默认末端才编码为对应 `ResultSetUpdate` variant，Filter 可短路，错误归入原 Statement | `ResultSetFilter`、`ResultSetFilterChain`、`FilterChain` 与 `DruidPooledResultSet` 四条精确入口；`JdbcOpaqueObject` 保留 vendor 对象并支持具体类型下转 | PASS（object setter 四重载；资源对象及 LOB stream/Reader setter 分别由 `SEM-FLT-035..036` 关闭） | Java `Filter`/`FilterChain`/`FilterChainImpl`/`ResultSetProxyImpl` 四重载静态对照；Rust 默认末端 descriptor、四重载短路、vendor 对象下转、SQL NULL/负 scale 身份、DriverError/exceptionCount；真实 Toasty SQLite index/label capability error |
| SEM-FLT-035 | ResultSet `updateRef/updateBlob/updateClob/updateArray/updateRowId/updateNClob/updateSQLXML` 必须各自保留 index/label 14 个资源对象重载；Java null、资源运行时类型/共享句柄和 RowId 字节值不可被通用 object/update 描述抹平；Filter 可短路，默认末端调用物理同名重载，错误归入原 Statement | 四层 14 条资源 setter 精确入口；Filter 边界以拥有型 `Option<JdbcX>` 保持链内生命周期，物理末端才转为借用 | PASS（资源对象 setter 子族；stream/Reader LOB setter 由 `SEM-FLT-036` 关闭） | Java 四层 14 签名静态对照；Rust 默认末端 14 个精确物理日志、全重载短路、nullable 与 RowId 字节身份、DriverError/exceptionCount；真实 Toasty SQLite 14 个独立 UnsupportedOperation |
| SEM-FLT-036 | ResultSet `updateBlob(InputStream)`、`updateClob(Reader)`、`updateNClob(Reader)` 必须分别保留 index/label、无长度/long 长度共 12 个 Filter 重载；Java null、InputStream/Reader 共享游标和关闭状态、负数及极大 long 均不得被预读、校正或合并；Filter 可短路，默认末端才编码 `Unspecified/Long` 并调用物理同名 SPI，错误归入原 Statement | 四层 12 条 LOB stream/Reader 精确入口；Filter 边界以拥有型 `Option<RdbcInputStream/RdbcReader>` 保持链内生命周期，末端映射 `JdbcStreamLength/JdbcCharacterLength` | PASS（LOB stream/Reader setter 子族；ASCII/Binary/Character/NCharacter stream setter 由 `SEM-FLT-037` 关闭） | Java `Filter`/`FilterChain`/`FilterChainImpl`/`ResultSetProxyImpl` 12 签名与 raw 末端 `V0_STATIC`；Rust 默认末端 12 日志、nullable、`-17/i64::MAX`、短路共享游标、DriverError/exceptionCount；真实 Toasty SQLite 12 次精确 SPI capability error |
| SEM-FLT-037 | ResultSet `updateAsciiStream/updateBinaryStream/updateCharacterStream` 必须分别保留 index/label 与无长度/int/long 共 18 个 Filter 重载，`updateNCharacterStream` 保留 index/label 与无长度/long 四个重载；Java null、InputStream/Reader 共享游标、负数、`i32::MIN/MAX` 与 `i64::MAX` 不得被预读、校正或合并；Filter 可短路，默认末端才编码对应 `ResultSetUpdate` 的 `Unspecified/Int/Long`，错误归入原 Statement | 四层 22 条 stream setter 精确入口；Filter 边界使用拥有型 `Option<RdbcInputStream/RdbcReader>`，物理末端保持现有通用 `update_value/update_value_by_label` SPI 描述符 | PASS（stream setter 子族；NString setter 子族由 `SEM-FLT-038` 关闭） | Java `Filter`/`FilterChain`/`FilterChainImpl`/`ResultSetProxyImpl` 22 签名与 raw 末端 `V0_STATIC`；Rust 默认末端 22 个 descriptor 日志、nullable、int/long 边界、短路共享游标、DriverError/exceptionCount；真实 Toasty SQLite 22 次 generic update capability error |
| SEM-FLT-038 | ResultSet `updateNString(int/String, String)` 虽与普通 String setter 共用 Rust `Option<String>`，仍必须保留 index/label 两个独立 Filter 调用；nullable、Unicode、标签身份和 NString operation 名称不得折叠为 `updateString`；Filter 可短路，默认末端才编码 `ResultSetUpdate::NString`，错误归入原 Statement | 四层 `result_set_update_n_string*` 两条精确入口；物理末端继续复用最小 `update_value/update_value_by_label` SPI，但描述符固定为 `NString` | PASS（NString setter Filter 子族；ResultSet update setter Filter 子域已闭合） | Java `Filter`/`FilterChain`/`FilterChainImpl`/`ResultSetProxyImpl` 两签名与 raw `updateNString` 末端 `V0_STATIC`；Rust 默认末端两 descriptor、nullable Unicode、index/label 短路、DriverError/exceptionCount；真实 Toasty SQLite 两次 generic update capability error |
| SEM-FLT-039 | Java `FilterAdapter` 的默认适配语义是：`init/destroy/configFromProperties` 无副作用，Wrapper 只匹配运行时自身类型，其余 hook 必须继续调用同名 `FilterChain`，不得返回伪造常量或吞掉错误；Rust 无继承，必须以可组合独立对象承载已迁移 hook | 独立 `filter_adapter.rs`；实现 `BeforeFilter`/`AfterFilter` 默认放行、`ExtendedFilter` 配置与类型令牌、`ResultSetFilter` 185 个逐方法默认继续链及 `Wrapper` 精确自身下转 | PARTIAL（canonical 对象与已迁移 hook 默认语义闭合；Java 491 个公开方法的未迁移调用族仍开放） | Java `FilterAdapter.java` 491 个公开方法及链委托 `V0_STATIC`；Rust 生命周期/配置/None/foreign/self Wrapper 精确断言、同实例三视图注册；真实 Toasty SQLite query/NString getter 与 capability error `V5_HOST` |
| SEM-FLT-040 | Java `FilterEventAdapter` 对 physical connect 与 Statement create/prepare/call 成功调用 after hook；对 execute/query/update/batch 执行 before→downstream→success-after，异常则调用 error-after 后重新抛出，且 error-after 自身异常替换原异常；query 成功还必须保持 query-after→resultSetOpen-after 顺序 | 独立 `filter_event_adapter.rs`；组合式 `FilterEventListener` 承载 Java protected hook，physical connect 已由有位置链执行 before→raw factory→带 connection ID 的 after，dataSource get 也使用独立有位置链；`after_statement_event` 反向展开创建事件，`BeforeFilter/AfterFilter/ResultSetFilter` 分派执行、错误替换与 ResultSet open | PARTIAL（物理 connect 与 dataSource get 均进入生产路径；StatementProxy/具体 DataSource 实例身份、later/downstream before 失败时已进入 adapter 的局部 error-after 展开及完整 Java 45 分支错误矩阵仍开放） | Java `FilterEventAdapter.java` 541 行、32 个公开入口、13 个模板 hook及相关测试 `V0_STATIC`；Rust 默认模板矩阵、双 adapter 反向创建事件、成功回调失败、error-after 替换原错 `V1_RUST_LOCAL`；真实 Toasty SQLite execute/query/update/batch/prepared/error 与 query-after→open `V5_HOST`，不是 live differential |
| SEM-FLT-041 | Java `FilterManager` 从三个 ClassLoader 顺序合并 `META-INF/druid-filter.properties`，后者覆盖；alias 查找大小写敏感，未知名称仅在 Java UTF-16 长度 `<128` 时按类名回退；逗号展开不 trim，已有 Filter 按类名 `equalsIgnoreCase` 去重；类缺失只记录并继续，构造异常包装为 SQLException | `filter_manager.rs` + bundled properties；显式 property source 顺序替代 ClassLoader，`register_filter` 工厂替代反射，canonical `FilterChainImpl` 保存 Java 类名并去重；Factory `setFilters` 和 inventory AutoLoad 已装配 | PARTIAL（本对象当前可表达语义已闭合；JVM 自动 ClassLoader 资源扫描、跨 crate provider 验证和未迁移 alias 行为仍开放） | Java `FilterManagerTest` + `DruidLoaderUtilsTest` 源侧 4/4，源码/调用链 `V0_STATIC`；Rust alias/failure/dedupe `V2_MIRRORED`，UTF-16、properties escape/continuation、显式工厂 `V1_RUST_LOCAL`；内置 `stat/default` 去重并进入真实 Toasty SQLite Statement/ResultSet `V5_HOST`；AutoLoad 当前仅 V1，统一验证待做 |
| SEM-FLT-042 | `Filter#connection_close` 必须只包围真实 raw driver close，按 position 进入；StatFilter 在下游前记录且失败不回滚，LogFilter 在下游成功后记录；池化连接逻辑 close 只能走 `dataSource_releaseConnection`，不得冒充 physical close | `PhysicalConnectionCloseFilterChain` + `PhysicalConnectionCloseContext` 支撑协议；同步销毁和受监管异步 close worker 均从 position 0 进入同一链，末端调用 `PhysicalConnectionFactory#close`；废弃的扁平 `after_connection_close` 广播已移除 | PARTIAL/IMPLEMENTED_UNVERIFIED | Java Filter/FilterChainImpl/StatFilter/LogFilter 源码顺序 `V0_STATIC`；Rust workspace 编译门禁通过；多 Filter 短路/错误、worker FIFO、close/restart 排空及真实 SQLite 在最终统一阶段验证 |
| SEM-FLT-043 | `Filter#connection_connect` 必须按 position 包围 raw driver connect；同一可变 Properties 贯穿各 Filter 与 driver；ID 只在 raw connect 成功后分配；StatFilter 自行负责 before/error/after/Entry，LogFilter/FilterEventAdapter 在下游成功后接收真实 ID；未启用 stat 时不得生成 Java StatFilter 副作用 | `PhysicalConnectionConnectFilterChain` + `PhysicalConnectionConnectResult`；`PhysicalConnectionFactory#create_info_with_properties` 默认适配预配置 factory，扩展 Adapter 可消费最终属性；holder 保存 Filter 改写后的属性；初始化/校验失败重新进入 physical-close 链 | PARTIAL/IMPLEMENTED_UNVERIFIED | Java FilterChainImpl/StatFilter/FilterEventAdapter 源码顺序 `V0_STATIC`；workspace 编译门禁通过；Properties 改写的真实 driver 消费、多 Filter 短路/after-error、loginTimeout 与 Java live/SQLite 统一验证 |

### `SEM-SQL-*`

保留原全局 ID，便于历史引用和自动审计。

| ID | 语义 | Rust 迁移策略 | 当前 | 验收 |
| :--- | :--- | :--- | :--- | :--- |
| SEM-SQL-001 | `DbType.of` 名称/别名、mask/hash/equals/style；URL 推断属于 JdbcUtils | canonical `DbType` + 分层 `JdbcUtils` | PARTIAL/IMPLEMENTED_UNVERIFIED：`of` 已恢复 Java case-sensitive/no-trim 规则，仅 aliyun_ads/maxcompute 忽略大小写；AntSpark、mask 合并、hashCode64、equals 与 PostgreSQL-style 已落。JVM log4jdbc variant 不进入 Rust DbType，旧 URL 只归一化真实 vendor | Java 全枚举、非法大小写/空白、hash/mask live/golden fixture；JdbcUtils URL/driver 独立闭合 |
| SEM-SQL-001A | `JdbcUtils.getDbTypeRaw/getDbType/getTypeName` 与 db-family 判断 | 严格 JDBC 兼容入口 + 独立 Rust `infer_db_type` | PARTIAL/IMPLEMENTED_UNVERIFIED：URL 大小写/顺序和历史 vendor 返回、未使用 driverClassName、Java Types 名称、MySQL/Oracle/PostgreSQL/SQL Server family 已落；native DSN 不污染 Java API | Java URL/type/family 全矩阵 live/golden；真实 Toasty/SQLx/RBDC driver identity |
| SEM-SQL-002 | Lexer token、offset、line/column | `LayoutCharacters`、`CharTypes`、335 项 `Token`、`Keywords` 与 canonical `Lexer` 已独立落位；Lexer 直接扫描 RdbcString UTF-16，保留 `pos/mark/bufPos/ch/token/hash/hashLCase`、SQLite/DM keyword、SavePoint、line/column，并覆盖通用 identifier/string/number/variable/comment/operator token stream | PARTIAL/IMPLEMENTED_UNVERIFIED | Token/keyword/hash、完整 token stream、offset/line/column Java live diff；补齐方言专用快速扫描入口和 feature 分支 |
| SEM-SQL-002A | parser 异常类型、消息、位置与 cause | canonical `ParserException` + EOF/NotAllowComment/SQLParseException 四对象；Rust Error/Deref 映射 Java 继承，保留 nullable message/cause、line/column 与精确 source-SQL 换行模板 | IMPLEMENTED_UNVERIFIED | 构造器/class/message/source/line/column Java live diff；Lexer/SQLUtils 错误生产链 |
| SEM-SQL-002B | `SymbolTable` intern 与 hash 冲突 | canonical `SymbolTable`；32768 global、tableSize-1 mask、low-i32 bucket、单槽首项缓存；同 hash 返回同 Arc 且不比较文本，不同 hash 冲突返回新值且不覆盖；String 按 UTF-16，bytes 按 UTF-8 replacement | IMPLEMENTED_UNVERIFIED | identity、collision、invalid UTF-8、offset/len 与零/非二次幂容量 Java live diff |
| SEM-SQL-002C | `SQLInsertValueHandler` 流式 VALUES 回调 | 关联 Row 保留自定义行身份；14 回调逐项保留，SqlInsertNumber 区分 Integer/Long/BigInteger，FunctionValue 保留 UTF-16/String/Number/Decimal/Boolean/Date/null/vendor Object，Date 使用 Java epoch millis | IMPLEMENTED_UNVERIFIED | 回调次序、index、类型分支、错误短路、row/complete 与 StatementParser Java live diff |
| SEM-SQL-003 | 注释、hint、变量和字符串转义 | canonical Lexer 已迁移 `--`/`//` 单行与嵌套块注释，skip/keep/allow、CommentHandler、通用 `#` variable、N 字符串、双单引号及反斜线基础转义；MySQL `#` comment、hint、safe-comment 白名单及全部 DialectFeature 转义分支继续补齐 | PARTIAL/IMPLEMENTED_UNVERIFIED | 保留/跳过/拒绝、handler、嵌套/未闭合、各方言转义 Java live diff |
| SEM-SQL-004 | `SQLParserFeature` 开关 | canonical `SqlParserFeature` 独立对象；29 项按 Java ordinal 固定，values/valueOf/name、mask 有符号 i32、isEnabled/config/of/null varargs 均可观察 | IMPLEMENTED_UNVERIFIED | 29 项 ordinal/mask/name 与组合/清除 Java live diff |
| SEM-SQL-004B | `DialectFeature` lexer/parser 双 mask 与构造时序 | canonical `DialectFeature`；19 lexer + 57 parser 的 values/ordinal/valueOf，deprecated aliases 共享 bit；默认七项、List 默认基、Feature varargs 零基及 boolean 默认基四路径独立 | IMPLEMENTED_UNVERIFIED | 全 mask/name、alias、四构造器与 config/unconfig Java live diff |
| SEM-SQL-004A | `SQLType` 完整分类返回域 | canonical `SqlType` 独立对象；128 项按 Java ordinal 固定，保留 INSERT/SHOW/LIST/ALTER TABLE 子分类、java_name 与严格 valueOf | IMPLEMENTED_UNVERIFIED | 128 项 ordinal/name Java live diff；getSQLType/V2 parser 分类链 |
| SEM-SQL-005 | `parseStatements` 全输入消费与 EOF | `SqlUtils::parse_statements/parse_single_statement` + sqlparser 方言 Adapter | PARTIAL/IMPLEMENTED_UNVERIFIED：完整输入、多语句与单语句数量门禁已接；Java parser feature、错误位置/类型与 live diff 待补 | trailing token、空输入、多语句、方言 error diff |
| SEM-SQL-006 | 多语句、分隔符与 keepComments | `SqlUtils::parse_statements/to_sql_string` | PARTIAL/IMPLEMENTED_UNVERIFIED：statement list 和 `;\n` 输出已接入 Wall 与 Stat service；keepComments/Java visitor 格式待补 | statement list/output diff |
| SEM-SQL-007 | 方言 parser 工厂/SPI | registry + sqlparser 扩展 | TODO | DbType→parser |
| SEM-SQL-008 | AST 节点类型、字段与 parent | compatibility AST | TODO | 逐对象 snapshot |
| SEM-SQL-009 | attributes、before/after comments | metadata sidecar | TODO | clone/serialize |
| SEM-SQL-010 | Visitor visit/endVisit 和中止 | visitor trait | TODO | 事件序列 |
| SEM-SQL-011 | output visitor 格式化 | formatter adapter | TODO | byte/golden diff |
| SEM-SQL-012 | parameterize、参数列表和 merge | `SqlUtils::parameterize` + `SqlMerger` 词法状态机 | PARTIAL/IMPLEMENTED_UNVERIFIED：字符串/数值、前缀 literal、quoted identifier、注释、科学计数与 placeholder 边界已接，非正则替换；Java AST 参数列表与全方言仍缺 | SQL+params diff |
| SEM-SQL-013 | fingerprint 稳定性 | canonical hash policy | PARTIAL | Java key 对照 |
| SEM-SQL-014 | schema repository/resolve | repository module | TODO | DDL→lineage |
| SEM-SQL-015 | builder/transform | AST rewrite | TODO | rewrite diff |
| SEM-SQL-016 | `PagerUtils` count/limit | dialect pager | TODO | 方言 SQL diff |
| SEM-SQL-017 | parser exception 类型、位置、消息 | `DruidSqlError` | TODO | malformed corpus |
| SEM-SQL-018 | MySQL 方言对象族 | extension/fork | TODO | Java MySQL corpus |
| SEM-SQL-019 | Oracle 方言对象族 | extension/fork | TODO | Java Oracle corpus |
| SEM-SQL-020 | PostgreSQL 方言对象族 | extension/fork | TODO | Java PG corpus |
| SEM-SQL-021 | SQL Server/DB2/ClickHouse 等方言 | extension/fork | TODO | 各自 corpus |

### `SEM-WALL-*`

保留原全局 ID，便于历史引用和自动审计。

| ID | 语义 | 原总账状态 | 验收条件 |
| :--- | :--- | :--- | :--- |
| SEM-WALL-001 | provider 根据 DbType 选择，未知方言走 SPI | TODO | provider 矩阵 |
| SEM-WALL-002 | select/insert/update/delete allow | PARTIAL | 每项开/关 |
| SEM-WALL-003 | DDL、transaction、set/show/use 等 allow | PARTIAL | 每项开/关 |
| SEM-WALL-004 | update/delete 必须有 where | PARTIAL | AST 语义，不可字符串猜测 |
| SEM-WALL-005 | 恒真/恒假/double const/like/case 条件 | TODO | Java Wall corpus |
| SEM-WALL-006 | deny/permit table/function/schema/variant | PARTIAL/IMPLEMENTED_UNVERIFIED：Java 20 个内置规则资源已原样进入 crate；WallConfig 按固定顺序追加、小写、去重。deny table/function/schema 已进入通用 AST，方言 Visitor 追加 variant、readonly 与 v$；permit/object 的完整优先级仍缺 | 大小写、schema、嵌套、permit 优先级 |
| SEM-WALL-007 | white list 与 cache | PARTIAL：`WallProvider` 已有 white/black LRU、命中计数、size、invalidate/reset；逐方言 key 与并发语义未统一验证 | hit/miss/reset |
| SEM-WALL-008 | multiStatement/comment/hint/metadata | PARTIAL/IMPLEMENTED_UNVERIFIED：multiStatement 与 comment 拒绝已有 AST/词法入口；允许 comment 时按 Java 前一 token 白名单生产 WallContext comment warning；hint、metadata 和逐方言 lexer 差分仍缺 | parser feature 组合与 Java comment handler golden |
| SEM-WALL-009 | mustParameterized | TODO | 参数化规则 |
| SEM-WALL-010 | tenant column/table pattern 注入、隐藏与检查 | PARTIAL/IMPLEMENTED_UNVERIFIED：`TenantCallBack` 四方法与语句类型已建模；独立 `WallVisitorUtils` 按 Java active path 为顶层 SELECT/UNION 及 join 表追加 tenant 投影、为 UPDATE 追加赋值、为 INSERT values/select/union 追加列和值；普通 Statement 使用改写后 SQL 调驱动并回写 lastSql，addBatch 在物理 Statement 保存前逐条改写，Prepared/Callable 在物理 prepare 前改写且 cache key 同步。同步 `set_tenant_value` 保留 Java API，Tokio `scope_tenant_value` 提供跨 worker 安全作用域。ResultSet open 继续生成双向映射/隐藏列/tenant 列并在成功 next 后回调。Java 当前注释掉的 DELETE/WHERE tenant condition 不擅自启用；完整方言边界仍缺 | Java TenantSelect/Update/Insert 全 corpus、callback/pattern/metadata/resultSet differential；普通/Prepared/Callable/batch 真实 SQLite 与 schema/alias 方言边界 |
| SEM-WALL-011 | WallContext 和 check 统计 | PARTIAL/IMPLEMENTED_UNVERIFIED：同步 thread-local 生命周期、DbType、SQL/表/函数统计、comment/update-none/delete-none/like-number warning、update 检查项与 provider check 绑定已接；union/其余 visitor warning 及线程交错差分待补 | Java ThreadLocal 生命周期、首次 warning、表/函数大小写与 101 项历史边界、嵌套 check 和清理 |
| SEM-WALL-012 | violation 类型、errorCode、message | PARTIAL | 逐规则对照 |
| SEM-WALL-013 | logViolation/throwException | PARTIAL：两个原子开关与 tracing/typed error 分支已接；四组合及错误优先级待统一验证 | 四种组合 |
| SEM-WALL-014 | provider cache clear/white list API | PARTIAL/IMPLEMENTED_UNVERIFIED：canonical `WallProviderStatValue`、white/black list value/size/hit、`getStatValue(reset)` 原子取值与 DruidDataSource 管理面已接；`WallProvider::clear_cache` 与 `WallFilter::clear_provider_cache` 只清判定缓存，white/black set 快照已接；多数据源 facade 已迁移递归 map、name list、blackList 按 SQL 去重/1000 上限、数值/字符串及 Java long[] 历史聚合；admin action 和 Java live diff 待补 | `WallStatTest_statMap` + clear 前后计数/cache + admin action |
| SEM-WALL-015 | 各方言 WallVisitor | PARTIAL/IMPLEMENTED_UNVERIFIED：七个 Provider/Visitor 各自独立文件并进入 hard-check；MySQL outfile/variant、Oracle/SQLServer variant、DB2/PG/Oracle v$、通用 deny/readonly 已接；SQLite Visitor 保留 Java 历史 dbType 返回值 | 七方言 Java 安全语料、parser/export parameter 格式和错误码差分 |
| SEM-WALL-016 | updateCheckColumns、WallUpdateCheckHandler 与 Prepared 参数检查 | PARTIAL/IMPLEMENTED_UNVERIFIED：table.column 规范化、LinkedHashSet 首列、运行期 handler 替换、handler 存在时绕过 white/black cache、字面量即时检查、相等/IN 过滤值去重、匿名/显式 1-based 占位符和执行前参数求值已接；复杂表达式、方言占位符及真实 SQLite/Java live diff 待补 | `WallUpdateCheckTest` 全 case、Prepared 参数缺失/NULL/重复、缓存 hit=0 与错误时机 |

### `SEM-STAT-*`

保留原全局 ID，便于历史引用和自动审计。

| ID | 语义 | 原总账状态 | 验收条件 |
| :--- | :--- | :--- | :--- |
| SEM-STAT-001 | SQL 参数化后合并 | PARTIAL | 基于 P4 AST 与 Java key 对照 |
| SEM-STAT-002 | execute/error/running/concurrentMax | PARTIAL/IMPLEMENTED_UNVERIFIED：`ExecuteCount` 已纠正为仅成功执行，错误独立累计；before/after、物理错误、listener 错误、后续 before 短路及 after 清理失败均接入 running 归零与 concurrentMax，Filter 前置错误按完成栈逆序清理且保留主错误；最近错误 message/class/time 写入 Java 管理字段，Rust 默认不伪造 JVM stack trace | 固定时钟并发、错误字段 golden 与 Java live diff |
| SEM-STAT-003 | execute time total/max/histogram | PARTIAL/IMPLEMENTED_UNVERIFIED：成功和错误均记录 total/max 及 `<1ms` 至 `>=1000s` 八档 histogram，管理快照导出 Java `Histogram`；边界与最大值发生时间仍待差分 | 固定时钟边界桶 |
| SEM-STAT-004 | update/fetch rows total/max/六档直方图 | PARTIAL/IMPLEMENTED_UNVERIFIED：`JdbcSqlStat` 已按 Java `<1/<10/<100/<1000/<10000/其余` 边界维护 update/fetch total、max 和 histogram；executeUpdate、generic execute update、batch、eager query 与 ResultSet close 已接真实生产链，ResultSet 绑定创建 SQL 并按实际读取峰值在首次 close 时回写；尚待统一 Java 差分、真实 SQLite 与并发关闭验证 | Java `JdbcSqlStat#addUpdateCount/addFetchRowCount` + SQLite 执行/提前关闭差分 |
| SEM-STAT-005 | transaction/batch/resultSet hold time | PARTIAL/IMPLEMENTED_UNVERIFIED（SQL 级事务内执行数、普通 Statement batch count/size total/max、PreparedStatement Java `batchSqlList` 固定零长度语义、事务开始/提交/回滚七档时长、连接租约八档 hold histogram 已接；Statement/PreparedStatement 执行耗时随 ResultSet 传递，首次 close 累加 ResultSet hold、execute+hold total 和八档 histogram，并保留 Java 零 update 桶历史行为；统一差分待补） | 状态序列 |
| SEM-STAT-006 | slow SQL/slow parameters | PARTIAL/IMPLEMENTED_UNVERIFIED：动态有符号毫秒阈值、慢 SQL 计数与 tracing level 已接；成功慢执行把普通或 Prepared 参数保存为 `LastSlowParameters` JSON，覆盖 NULL/布尔/整数/浮点/Decimal、Java UTF-16 97+`...` 字符串截断、日期时间，以及 InputStream/Reader/Blob/Clob/NClob/Array/Ref/RowId/SQLXML/URL/vendor object 占位符；错误执行不覆盖上次成功快照，reset 清空。Rust 日期/时间不携带 JVM epoch、部分平台资源不保留驱动具体类名，仍待 Java golden 明确差异 | `StatFilterBuildSlowParameterTest` cases + Java/Rust JSON golden |
| SEM-STAT-007 | datasource/connection/statement/resultset 分层统计 | PARTIAL/IMPLEMENTED_UNVERIFIED：`JdbcConnectionStat`/Entry、`JdbcStatementStat` 已接入生产；SQL cache 按插入顺序容量淘汰并产生 skipSqlCount；ResultSet/Callable 的直接 Blob/Clob getter及 Java 对应 `getObject` 返回 Blob/Clob/NClob 时同时增加数据源与当前 SQL 的 LOB open；ResultSet 按 Java hook 统计读取量/流打开数并在首次 close 汇入 SQL；`JdbcSqlStatValue` 已覆盖 Java 41 键，执行开始/最大耗时发生时点进入生产，JVM-only stack/file 保持 null；管理 ID 使用进程级递增对象身份，HASH 使用 Java UTF-16 FNV-1a 有符号 long，不再复用内部 xxh3 cache key；statement trace 在显式关闭与连接 reset/recycle/destroy 路径计入幂等 close；统一并发/差分仍缺 | 快照 41 键、ID/FNV/time 逐字段 Java golden/live diff |
| SEM-STAT-008 | reset 的原子语义 | PARTIAL/IMPLEMENTED_UNVERIFIED：数据源区间快照通过 atomic swap 取走峰值、等待、逻辑/物理连接、执行/事务、PS 与直方图；SQL reset 已纠正为移除“成功数为零且未运行”条目，保留其他 SQL 身份和 runningCount 并清零全部区间原子字段/直方图，不再错误清空 cache；ResultSet/pool/Wall reset 已接，跨对象单锁一致性及并发 record 竞争未验证 | 与并发 record 竞争 |
| SEM-STAT-009 | StatFilter 上下文/listener | PARTIAL/IMPLEMENTED_UNVERIFIED：同名 context/listener/adapter 和 execute/update/batch/transaction/ResultSet 生产事件已接；Filter before 短路增加逆序错误展开，after 即使某项失败也继续清理并返回首错；嵌套调用与全事件族差分未闭合 | 嵌套调用、短路与首错 |
| SEM-STAT-010 | JMX 属性映射 OpenMetrics | TODO | 指标名、label、类型 |
| SEM-STAT-011 | JdbcStatContext 执行上下文 | PARTIAL/IMPLEMENTED_UNVERIFIED：nullable 五字段、create/get/set、Tokio task/OS thread fallback、context SQL override、name/file 写入 JdbcSqlStat、statement close 清 name/file/sql 已接；requestId/traceEnable 保留且不伪造未被 Java StatFilter 消费的行为 | task 终结清理、嵌套 task 继承策略、Java ThreadLocal live diff |
| SEM-STAT-012 | TableStat 与 inner 值对象 | IMPLEMENTED_UNVERIFIED：14 counter wrapping、固定 Display 顺序、Name/Column 仅按 hash 相等、Condition equality 忽略 values、JSON string 格式、Column 标志/属性和 Mode mark 已接 | Java FNV/normalize、Unicode/quoted compound owner、Display golden diff |
| SEM-STAT-011 | DruidStatService JSON schema | PARTIAL/IMPLEMENTED_UNVERIFIED：basic/datasource/sql/wall/connectionInfo/active trace/reset 路由和 ResultCode 包装已接；reset-all 依次重置 Jdbc/dataSource 并增加 facade count，log-and-reset 独立调用每个 datasource stat sink；URL 分派保留 exact 与 startsWith 差异，query 不 URL-decode、重复键后者覆盖；空排序列表返回 null，MapComparator 的 null/number/string、数组下标与 asc/desc 已映射，SQL detail 补 formattedSql/本地时间；basic 保留 Java 兼容键为 null，真实输出 StartTime/RustMSRV/target，并枚举已注册数据源的去重 driver label；SQL 快照 41 键已闭合；未实例化 Adapter Drivers、locale Collator、parsed schema 字段、非法分页异常、web/spring 分支与逐字段 Java/Axum golden 未闭合 | golden response、URL/排序/分页矩阵、三层 resetCount/gate、sink 错误隔离 |
| SEM-STAT-012 | tracing/logging 字段和脱敏 | PARTIAL：Druid 事件输出已统一到 tracing；Java logger 实现对象明确不迁移，结构化字段/敏感值脱敏和 capture 验证待补 | event snapshot |

### `SEM-HA-*`

保留原全局 ID，便于历史引用和自动审计。

| ID | 语义 | 原总账状态 | 验收条件 |
| :--- | :--- | :--- | :--- |
| SEM-HA-001 | named/random/sticky selector | PARTIAL/IMPLEMENTED_UNVERIFIED：canonical trait/enum/factory 与三个独立对象已建立；named 单节点/target/default、random 双 blacklist/全拉黑 fallback/busy 非全忙剔除、sticky task holder/5 秒过期/容量检查已接；LoadBalancer 保持 Rust 扩展不冒充 selector | Java selector 全矩阵、确定性随机源、task 终结清理与并发 selector 替换 |
| SEM-HA-002 | 节点校验、摘除、恢复 | PARTIAL/IMPLEMENTED_UNVERIFIED：Random selector 在 testOnBorrow/testOnReturn 均 false 且存在 Tokio runtime 时启动唯一 validate/recover 任务；validate 五并发、success/lastCheck skip 窗、wrapping 连败、threshold blacklist、动态 1 秒下限休眠及 name cleanup 已接；recover 对 blacklist 顺序 acquire/sleep/ping 并恢复摘除；destroy 发送 shutdown 并取消任务；haRandomValidator 成功执行更新时间 | Java raw-driver 与 pooled recover 差异、properties 参数、连续失败/恢复/取消/重启完整时间线及真实 SQLite |
| SEM-HA-003 | Registry/Watcher 事件 | PARTIAL/IMPLEMENTED_UNVERIFIED：NodeEvent ADD/DELETE 差分、typed NodeListener observer、FileNodeListener Java-properties 读取/空 URL 过滤/非空快照替换/同步首次刷新/定时 try-lock/shutdown 已接；ZooKeeper 以真实 async client 实现 persistent parent、ephemeral member、直接子节点快照、ADD/DELETE、忽略 UPDATE、断连重连全量 rebuild、三种 URL placeholder 和 client 所有权 | 文件变化 Java live diff、真实 ZooKeeper 集群的注册/断连/重连/删除时间线 |
| SEM-HA-004 | 热切换与旧池排空 | PARTIAL/IMPLEMENTED_UNVERIFIED：PoolUpdater 串行消费事件；ADD 动态创建 native Druid 子池或取消待删除，DELETE 先进入 HA blacklist；禁止默认删除最后节点；周期扫描在 active=0 后关闭再移除，active>0 延迟，关闭异常仍摘除；父池容量、超时、校验、驱逐、泄漏、PS cache、Filter 与连接属性复制已接 | 活跃事务/连接归还时间线、关闭竞态、非 Toasty vendor factory 注入和最终 SQLite 差分 |
| SEM-HA-005 | 全节点不可用错误 | PARTIAL/IMPLEMENTED_UNVERIFIED：Java-compatible `get_connection` 保留 `Ok(None)`；实现 Rust `Pool` 时映射为带 cause 槽的 `DataSourceNotAvailable`，未与 `DataSourceNotFound` 混同 | 空 map、HA blacklist、selector blacklist 全覆盖与调用方 fallback |

## 配置、Wall 与统计字段级语义

#### 13. PoolConfig 字段级对照（恢复原表）

原文列出的配置项全部保留。表中的 Rust 字段是迁移目标，不代表当前字段存在或已经接线。

| Java 配置 | Java 默认值 | Rust 目标字段 | 当前 | 关键验收 |
| :--- | :--- | :--- | :--- | :--- |
| `maxActive` | 8 | `max_open` | IMPLEMENTED_UNVERIFIED，CAS 预留与 init 边界校验已接 | 并发容量不越界 |
| `minIdle` | 0 | `min_idle` | IMPLEMENTED_UNVERIFIED，后台/keepAlive 回填已接 | 驱逐/keepAlive 后仍满足 |
| `maxWait` | -1 | `acquire_timeout`，`Duration::MAX` 表示无限 | IMPLEMENTED_UNVERIFIED | 无限、0、正数 |
| `maxWaitThreadCount` | -1 | `max_wait_thread_count: Option<usize>` | IMPLEMENTED_UNVERIFIED，取消时 Drop 减 waitCount | 拒绝边界与计数守恒 |
| `initialSize` | 0 | `initial_size` | IMPLEMENTED_UNVERIFIED，init 预建及 `initialSize <= maxActive` 校验已接 | init 创建数量 |
| `asyncInit` | false | `async_init` | IMPLEMENTED_UNVERIFIED，受监管 creator 异步补足 initialSize，init 不等待 | 返回时机、失败恢复、close/restart |
| `initExceptionThrow` | true | `init_exception_throw` | IMPLEMENTED_UNVERIFIED，true 在 initialized/worker 就绪后返回首错；false 固定 3s 重试 | Java 失败代次与中断 |
| `timeBetweenEvictionRunsMillis` | 60000 | `time_between_eviction_runs` | IMPLEMENTED_UNVERIFIED，Tokio maintenance 已接 | 调度周期 |
| `minEvictableIdleTimeMillis` | 1800000 | `idle_timeout` | IMPLEMENTED_UNVERIFIED | 边界时刻 |
| `maxEvictableIdleTimeMillis` | 25200000 | `max_evictable_idle_time` | IMPLEMENTED_UNVERIFIED，大小关系校验已接 | 强制淘汰 |
| `testOnBorrow` | false | `test_on_borrow` | PARTIAL，配置与 borrow validator 已接线 | borrow 校验与真实驱动错误 |
| `testOnReturn` | false | `test_on_return` | PARTIAL，显式 close 已接线 | return 校验、Drop 处置、真实驱动 |
| `testWhileIdle` | false | `test_while_idle` | IMPLEMENTED_UNVERIFIED，borrow 空闲阈值校验已接 | idle 校验 |
| `validationQuery` | null | `validation_query` | IMPLEMENTED_UNVERIFIED，传入 checker | query/driver ping |
| `validationQueryTimeout` | -1 | `validation_query_timeout` | IMPLEMENTED_UNVERIFIED，传入 checker | timeout |
| `keepAlive` | false | `keep_alive` | IMPLEMENTED_UNVERIFIED，到期探测、计数、后台调度和失败补池已接 | 差分与故障注入 |
| `poolPreparedStatements` | false | `pool_prepared_statements` | PARTIAL，native pool/holder/pooled wrapper 已接线 | cache enable、external bridge |
| `maxPoolPreparedStatementPerConnectionSize` | 10 | `max_pool_prepared_statements_per_connection` | PARTIAL，access-order LRU 容量已接线 | 0/边界、管理面/属性别名 |
| `removeAbandoned` | false | `remove_abandoned` | IMPLEMENTED_UNVERIFIED，weak active lease 追踪与 running 跳过已接 | 活跃追踪 |
| `removeAbandonedTimeoutMillis` | 300000 | `remove_abandoned_timeout` | IMPLEMENTED_UNVERIFIED | 超时失效/Drop 销毁 |
| `logAbandoned` | false | `log_abandoned` | IMPLEMENTED_UNVERIFIED，连接 ID/阈值日志与独立 backtrace 管理面已接 | 日志脱敏 |
| `breakAfterAcquireFailure` | false | `break_after_acquire_failure` | IMPLEMENTED_UNVERIFIED，达到重试阈值后终止本次获取 | 重试终止 |
| `connectionErrorRetryAttempts` | 1 | `connection_error_retry_attempts` | IMPLEMENTED_UNVERIFIED，立即重试次数与 500ms 默认退避已接 | 次数和退避 |
| `timeBetweenConnectErrorMillis` | 500 | `time_between_connect_error` | IMPLEMENTED_UNVERIFIED | 截止时间与退避 |
| `failFast` | false | `fail_fast` + `CreateFailureState` | PARTIAL/IMPLEMENTED_UNVERIFIED：不是首次失败立即返回；超过 retry 且正退避后置 failContinuous、唤醒池为空的等待者并以 current createError 为 cause；成功恢复清状态 | Java 错误身份、多等待者、恢复与 timeout 诊断 |
| `asyncCloseConnectionEnable` | false | Rust ownership/异步物理 close | PARTIAL，Rust 无 owner thread 等价物 | 异步关闭与同线程告警处置 |
| `useUnfairLock` | true | parking_lot/Notify 等待策略 | PARTIAL，默认非公平；运行时切换未接 | 等待公平性 |
| `phyTimeoutMillis` | -1 | `physical_connection_timeout: Option<Duration>`，`None` 表示禁用 | PARTIAL，归还与 shrink 淘汰边界已验证 | 属性绑定、管理面、后台维护 |
| `phyMaxUseCount` | -1 | `max_use_count`（Rust `0` 表示禁用） | PARTIAL，边界契约通过 | 管理面/属性别名 |
| `defaultAutoCommit` | true | `default_auto_commit` | PARTIAL，创建初始化与 reset 已接线 | 真实 adapter 与属性绑定 |
| `defaultReadOnly` | null | `default_read_only` | PARTIAL，`Option<bool>` 已接线 | unsupported/真实 adapter |
| `defaultTransactionIsolation` | null | `default_transaction_isolation` | PARTIAL，`Option<u8>` 已接线 | JDBC 常量映射/真实 adapter |
| `defaultCatalog` | null | `default_catalog` | PARTIAL，非空时初始化 | adapter 能力/属性绑定 |
| `dbTypeName=odps` 特例 | 推断值 | `db_type_name` | PARTIAL，跳过 default autoCommit 已验证 | URL 推断/属性绑定 |
| `name` | null | `name` | PARTIAL | Filter/stat context |
| `url/jdbcUrl` | null | `url` | PARTIAL | 属性别名 |
| `driverClassName` | null | `driver_name` | PARTIAL | driver registry |
| `username` | null | `username` | PARTIAL | 配置优先级 |
| `password` | null | `Secret<String>` | PARTIAL | 不泄漏、版本更新 |

字段完成需要同时满足：存在、默认值一致、属性绑定一致、运行路径读取、管理面可见、差分测试通过。

#### 14. WallConfig 字段级对照（恢复原表）

| Java WallConfig | Rust 目标字段 | 当前 | 行为验收 |
| :--- | :--- | :--- | :--- |
| `selectAllow` | `select_allow` | PARTIAL | false 拒绝所有 SELECT |
| `selectAllColumnAllow` | `select_all_column_allow` | PARTIAL，规则未完整接线 | `SELECT *` |
| `selectIntoAllow` | `select_into_allow` | PARTIAL，未接线 | SELECT INTO |
| `selectLimit` | `select_limit` | 字段/默认值已接，行为 TODO | 方言 LIMIT 注入/限制 |
| `insertAllow` | `insert_allow` | PARTIAL | INSERT 开关 |
| `updateAllow` | `update_allow` | PARTIAL | UPDATE 开关 |
| `deleteAllow` | `delete_allow` | PARTIAL | DELETE 开关 |
| `dropTableAllow` | `drop_table_allow` | PARTIAL | DROP TABLE |
| `truncateAllow` | `truncate_allow` | PARTIAL | TRUNCATE |
| `alterTableAllow` | `alter_table_allow` | PARTIAL，未接线 | ALTER |
| `createTableAllow` | `create_table_allow` | PARTIAL，未接线 | CREATE |
| `commitAllow` | `commit_allow` | PARTIAL，未接线 | COMMIT |
| `rollbackAllow` | `rollback_allow` | PARTIAL，未接线 | ROLLBACK |
| `useAllow/showAllow/describeAllow` | 对应同名字段 | PARTIAL，未接线 | 方言管理语句 |
| `startTransactionAllow` | `start_transaction_allow` | PARTIAL，未接线 | START TRANSACTION |
| `setAllow` | `set_allow` | PARTIAL，未接线 | SET |
| `multiStatementAllow` | `multi_statement_allow` | PARTIAL | 多语句 |
| `hintAllow` | `hint_allow` | PARTIAL，未接线 | hint/token |
| `commentAllow` | `comment_allow` | PARTIAL，未接线 | 注释 |
| `noneBaseStatementAllow` | `none_base_statement_allow` | PARTIAL，未接线 | 非基础语句 |
| `limitZeroAllow` | `limit_zero_allow` | PARTIAL，未接线 | LIMIT 0 |
| `selectWhereAlwayTrueCheck` | `select_where_alway_true_check` | PARTIAL，未接线 | 恒真条件 |
| `selectHavingAlwayTrueCheck` | `select_having_alway_true_check` | PARTIAL，未接线 | HAVING 恒真 |
| `updateWhereAlwayTrueCheck` | `update_where_alway_true_check` | PARTIAL，未接线 | UPDATE 恒真 |
| `deleteWhereAlwayTrueCheck` | `delete_where_alway_true_check` | PARTIAL，未接线 | DELETE 恒真 |
| `conditionAndAlwayTrueAllow` | `condition_and_alway_true_allow` | PARTIAL，未接线 | AND 恒真 |
| `conditionAndAlwayFalseAllow` | `condition_and_alway_false_allow` | PARTIAL，未接线 | AND 恒假 |
| `conditionDoubleConstAllow` | `condition_double_const_allow` | PARTIAL，未接线 | 双常量比较 |
| `conditionLikeTrueAllow` | `condition_like_true_allow` | PARTIAL，未接线 | LIKE 恒真 |
| `caseConditionConstAllow` | `case_condition_const_allow` | PARTIAL，未接线 | CASE 常量 |
| `variantCheck` | `variant_check` | PARTIAL，未接线 | 变量检查 |
| `mustParameterized` | `must_parameterized` | PARTIAL，未接线 | 参数化要求 |
| `metadataAllow/wrapAllow` | 对应同名字段 | PARTIAL，未接线 | metadata/wrapper |
| `denyTables` | `deny_tables` | PARTIAL | schema/大小写/嵌套 |
| `denyFunctions` | `deny_functions` | PARTIAL，测试明确未接线 | 函数遍历 |
| `denySchemas` | `deny_schemas` | PARTIAL | schema 解析 |
| `denyVariants` | `deny_variants` | PARTIAL/IMPLEMENTED_UNVERIFIED：MySQL/Oracle/SQLServer Visitor 已接，精确父节点条件仍缺 | 变量 |
| `readOnlyTables` | `read_only_tables` | PARTIAL/IMPLEMENTED_UNVERIFIED：INSERT/UPDATE/DELETE 目标表已接 | DML 拒绝 |
| white/black SQL cache | cache/sets | PARTIAL/IMPLEMENTED_UNVERIFIED：有界 cache、hit/clear/stat 已接 | add/remove/clear 与并发 |
| `tenantColumn` | `tenant_column` | PARTIAL，未接线 | tenant 条件 |
| `tenantTablePattern` | `tenant_table_pattern` | PARTIAL，未接线 | 表匹配 |

#### 15. 统计方法级对照（恢复原表）

| Java 统计对象/方法 | Rust 目标 | 当前 | 验收 |
| :--- | :--- | :--- | :--- |
| `JdbcSqlStat` | `stats/jdbc_sql_stat.rs::JdbcSqlStat` | MATCH/PARTIAL/IMPLEMENTED_UNVERIFIED | canonical 快照 |
| `getExecuteCount()` | `execute_count` | PARTIAL/IMPLEMENTED_UNVERIFIED | 已按 Java 仅计成功；差分待做 |
| `getTotalTime()` | `total_time_ns` | PARTIAL/IMPLEMENTED_UNVERIFIED | 成功/错误均接；固定时钟待做 |
| `getMaxTimespan()` | `max_time_ns` | PARTIAL/IMPLEMENTED_UNVERIFIED | 原子 max 已接；发生时间待补 |
| `getRunningCount()` | `running_count` | PARTIAL/IMPLEMENTED_UNVERIFIED | before/after/短路/错误清理已接，并发差分待做 |
| `getConcurrentMax()` | `concurrent_max` | PARTIAL/IMPLEMENTED_UNVERIFIED | 原子并发峰值已接，交错验证待做 |
| `getFetchRowCount()` | `fetch_row_count` | PARTIAL/IMPLEMENTED_UNVERIFIED | eager query 与 ResultSet 首次 close 已接 |
| `getUpdateCount()` | `update_count` | PARTIAL/IMPLEMENTED_UNVERIFIED | update/generic execute/batch 已接 |
| `getErrorCount()` | `error_count` | PARTIAL | driver/Wall/filter 错误 |
| batch size total/max | `execute_batch_size_total/max` | PARTIAL/IMPLEMENTED_UNVERIFIED | Statement/PreparedStatement 口径已接，差分待做 |
| result-set hold time | `result_set_hold_time_ns` / combined total+histogram | PARTIAL/IMPLEMENTED_UNVERIFIED | 首次 close 与提前终止生产链已接，固定时钟待做 |
| in-transaction count | `in_transaction_count` | PARTIAL/IMPLEMENTED_UNVERIFIED | before 读取真实连接事务状态，差分待做 |
| `JdbcDataSourceStat` | `JdbcDataSourceStat` | RENAME/PARTIAL | datasource 快照 |
| `JdbcConnectionStat` | `JdbcConnectionStat` | PARTIAL/IMPLEMENTED_UNVERIFIED | connect/close/error/entry/histogram 已接，差分待做 |
| `JdbcStatementStat` | `JdbcStatementStat` | PARTIAL/IMPLEMENTED_UNVERIFIED | create/prepare/call/execute/error/close/histogram 已接，隐式清理与差分待做 |
| `JdbcResultSetStat` | `JdbcResultSetStat` | PARTIAL | 对象字段/CAS/reset/min 及 StatFilter→ResultSet open/close/fetch/hold 已闭合；Java 未调用的 error 不擅自接线，SQL 级与管理面待补 |
| `DruidDataSourceStatManager` | canonical 显式 Arc registry + 独立 manager resetCount | PARTIAL/IMPLEMENTED_UNVERIFIED：register/unregister/get/instances、逐 datasource reset、空/非空一轮完成后 manager resetCount 增加已接；不与 datasource 自身 resetCount 合并 | Java identity map/系统属性/JMX 注册与并发 reset 统一验证 |
| `TableStat` | `TableStat` | TODO | select/update/delete/insert |
| `slowSqlMillis` | `slow_sql_threshold` | PARTIAL | 阈值边界 |
| `mergeSql` | `merge_sql` | PARTIAL | P4 AST 参数化 |
| `connectionStackTrace` | tracing span/optional backtrace | TODO | 开关和脱敏 |

#### 17. Rust 侧新增对象的语义边界

| Rust 对象 | 来源语义 | 允许新增的原因 | 禁止事项 |
| :--- | :--- | :--- | :--- |
| `PhysicalConnection` | JDBC Connection 平台能力 | Rust 内部 object-safe async SPI | 不公开池化所有权，不承载 Filter/统计 |
| `PhysicalConnectionFactory` | JDBC Driver/driver manager | native direct adapter 显式依赖注入 | 不得获取 bb8/deadpool lease |
| `ToastyConnectionFactory` / `ToastyConnectionAdapter` | 内置标准数据源 | Toasty `Driver::connect` raw connection | 不得创建 Toasty `Db` pool；SQLite 真实合同必须通过 |
| `ConnectionProvider` | DataSource 获取路径 + 外部池 wrapper | native/bridge 统一入口 | 同一租约不得嵌套两层 pool |
| `ConnectionLease`、`LeaseReturner` | pooled close/recycle | Rust 所有权与异步 Drop | 不得双重归还或静默丢弃 |
| `DruidConnectionHolder` | Java 同名 holder | Rust 所有权下由 native idle queue 与 pooled facade 移动同一个对象 | 不得再拆出第二份 idle metadata，也不得暴露为第二个公开连接 |
| `ConnectionDefaults` | holder 默认状态与 reset | Rust 值对象，避免驱动类型泄漏 | 只能作为 canonical holder 的组成，不得替代 PS cache 等未迁移职责 |
| `ConnectionRecycleDisposition` | recycle 最终分支 | 将处置结果传给 native/external Provider | 不得跳过 rollback/reset/validate 直接伪造 reusable |
| `PoolBuilder` | properties/setter | typed builder | 不得丢属性别名 |
| `BeforeFilter/AfterFilter` | Filter hook | async 拆分 | 不得丢事件 |
| `ParameterizedSql`、fingerprint | parameterized visitor/stat key | Rust 值对象 | 结果必须与 Java 对齐 |
| `SqlHint`、`DataSourceGroup` | HA selector/node group | async context/ArcSwap | 不得丢粘性与恢复 |
| OpenMetrics exporter | MBean/StatService | 协议替换 | 不得丢属性和管理动作 |
<!-- GLOBAL_SEMANTIC_LEDGER_MERGE_END -->
