# Druid 1.2.28 → druid-rust 语义迁移对照表

> 对象表回答“代码落在哪里”，本表回答“行为是否一致”。每条 `SEM-*` 是独立验收单元；Rust 实现形态可以不同，但输入、输出、错误、状态、副作用、并发和可观察结果必须与 Druid 1.2.28 一致。连接对象和 Provider 的硬约束见[连接抽象与驱动适配架构](./5、连接抽象与驱动适配架构.md)。

## 1. 状态与验收规则

| 状态 | 定义 |
| :--- | :--- |
| `PASS` | Java oracle 与 Rust 差分通过，真实依赖路径也通过 |
| `PARTIAL` | 一部分场景通过，但配置、分支或调用入口不完整 |
| `FAIL` | 已存在实现，但证据证明语义错误 |
| `TODO` | 尚未实现或尚无差分证据 |

单元测试通过不自动得到 `PASS`。每条契约至少记录：

```text
contract_id
java_source + java_fixture
rust_source + rust_test
input_matrix
expected_result / expected_error
state_before / state_after
side_effects (metrics, logs, callbacks)
concurrency_and_cancellation
mapping_decision
```

## 2. 差分评测流水线

```mermaid
flowchart LR
    Corpus["版本化输入语料<br/>配置/SQL/时序/故障"] --> Java["Java Druid 1.2.28 Oracle"]
    Corpus --> Rust["druid-rust Candidate"]
    Java --> Normalize["规范化器<br/>时间、ID、路径、错误链"]
    Rust --> Normalize
    Normalize --> Diff["字段级/事件序列/状态机差分"]
    Diff --> Pass["PASS + 证据归档"]
    Diff --> Fail["FAIL + 最小反例"]
    Fail --> Ledger["更新对象账本/语义账本"]
```

规范化只允许消除随机 ID、绝对时间和平台路径等非语义噪声，不得抹平错误类型、SQL 输出、统计字段或事件顺序。

## 3. 初始化与配置

| ID | Java 行为 | Rust 目标 | 当前 | 验收 |
| :--- | :--- | :--- | :--- | :--- |
| SEM-CFG-001 | `DruidDataSource.init()` 幂等初始化 | `DruidDataSource::init()` | TODO | 并发 init 只创建一套资源 |
| SEM-CFG-002 | `configFromProperties` 属性名、别名、优先级 | serde/config facade | TODO | 全量属性 golden test |
| SEM-CFG-003 | 默认值与 Druid 1.2.28 一致 | config types | PARTIAL | 字段逐项 snapshot |
| SEM-CFG-004 | 非法 max/min/wait/validation 配置立即失败 | builder/init validation | PARTIAL | 错误类型和时机一致 |
| SEM-CFG-005 | SPI Filter/Checker/Sorter/Driver 自动发现和排序 | inventory registry | TODO | provider 顺序与 fallback |
| SEM-CFG-006 | init 创建 initialSize 连接并填充 holder | pool initialization | TODO | 成功/部分失败/全失败 |
| SEM-CFG-007 | init 启动 creator/destroy/log tasks | Tokio supervised tasks | TODO | 启停、panic、重启 |
| SEM-CFG-008 | enable/disable/close/restart 状态门禁 | datasource state machine | TODO | 每状态公开 API 行为 |
| SEM-CFG-009 | 密码/配置版本变化使旧连接失效 | versioned secret/config | TODO | 归还时 discard |
| SEM-CFG-010 | MBean 配置读写结果 | admin config API | TODO | 字段、权限、错误对照 |
| SEM-CFG-011 | `initPhysicalConnection` 按 autoCommit → readOnly → isolation → catalog 初始化，ODPS 跳过 autoCommit | `PoolInner::initialize_physical_connection` | PARTIAL（事件序列契约通过；真实 adapter 能力矩阵待补） | 不同初始状态、空 catalog、unsupported |
| SEM-CFG-012 | 物理连接初始化失败时关闭连接、释放容量并记 connect error | native create failure path | PARTIAL（故障注入契约通过） | 每个 setter 失败、close 失败、并发关闭 |

## 4. 获取连接

CodeGraph 验证 Java 主链为 `getConnection` → FilterChain `dataSource_connect` → `getConnectionDirect` → `getConnectionInternal` → idle/create/wait → validate → `DruidPooledConnection`。

| ID | 语义 | 当前 | 关键差分场景 |
| :--- | :--- | :--- | :--- |
| SEM-POOL-001 | 优先复用 idle holder | PARTIAL | FIFO/LIFO 与时间字段 |
| SEM-POOL-002 | 空闲为空且容量可用时原子预留并创建 | PASS（CAS 预留 + gated concurrency contract） | 高并发不得越过 maxActive |
| SEM-POOL-003 | 达上限后按公平性等待 | PARTIAL | fair/unfair 唤醒顺序 |
| SEM-POOL-004 | maxWait 超时及错误字段 | PARTIAL | 0、正数、无限等待 |
| SEM-POOL-005 | 创建失败退避、重试和 failFast | TODO | 连续失败/恢复 |
| SEM-POOL-006 | testOnBorrow | PARTIAL | valid/invalid/validator error |
| SEM-POOL-007 | testWhileIdle | TODO | idle 时间阈值 |
| SEM-POOL-008 | keepAlive 探测与补最小空闲 | PARTIAL（到期探测、队列顺序、成功/失败计数已验证；失败后的 minIdle 回填与后台调度待补） | minIdle 守恒 |
| SEM-POOL-009 | 连接被 disable/closed datasource 拒绝 | TODO | 错误类型 |
| SEM-POOL-010 | getConnection Filter before/after | PARTIAL | 顺序、短路、异常 |
| SEM-POOL-011 | active/pooling/connect 计数原子变化 | PARTIAL | 取消、超时、创建失败 |
| SEM-POOL-012 | async 取消不遗失容量 permit | TODO | 在 await 点取消 |
| SEM-POOL-013 | 所有获取入口只返回 `DruidPooledConnection` | PASS（native、`dyn Pool`、dynamic、bb8、deadpool） | native、dynamic、bridge 类型一致 |
| SEM-POOL-014 | 单次 acquire 只选择 native 或 external bridge 一个 Provider | PARTIAL（类型边界已互斥，`DruidDataSource` 配置门禁待建） | 配置互斥；禁止 pool-in-pool |
| SEM-POOL-015 | Provider 返回带唯一回收权的连接租约 | PARTIAL（`DruidPooledConnection` FnOnce + external `PhysicalConnectionLease`） | 成功、超时、取消、Provider 关闭 |

## 5. 归还、丢弃与维护

Java `recycle` 的顺序是迁移契约：移除 abandoned trace → 必要时 rollback → holder reset → discard/寿命/使用次数/密码版本判断 → closed/testOnReturn → schema 恢复 → datasource enable → phyTimeout → active/close/recycle 计数 → `putLast` 或物理关闭。

| ID | 语义 | 当前 | 验收 |
| :--- | :--- | :--- | :--- |
| SEM-REC-001 | close/Drop 只归还一次 | PASS（显式 close、重复 close、Drop 契约） | 重复 close、move、panic |
| SEM-REC-002 | `dyn Pool` 路径同样归还 | PASS（无 `into_core` 转换；Dynamic 计数守恒测试） | trait-object route 与 concrete pool 一致 |
| SEM-REC-003 | 非 autoCommit 且非 readOnly 时 rollback；失败由 close 吞掉、连接淘汰并增加 recycleError | PARTIAL（Java/Rust 成功、readOnly、失败事件契约通过；真实驱动待补） | rollback 成功/失败 |
| SEM-REC-004 | holder reset 顺序：readOnly、holdability、isolation、autoCommit、listener/statement trace、warnings；验证后恢复 initSchema | PARTIAL（属性与 warnings 已实现；MySQL-family initSchema 已在 testOnReturn 后恢复且 schema 变化清 PS cache；listener/statement trace 未实现） | 状态逐项恢复、任一步失败 |
| SEM-REC-005 | testOnReturn；invalid 时 destroy 且不计 recycleError | PARTIAL（validator 成功/失败与计数契约通过；真实驱动待补） | invalid、validator error、timeout |
| SEM-REC-006 | fatal/discard/closed 连接不回池 | PARTIAL（显式 discard、closed、adapter marker 已接线；sorter 矩阵待补） | sorter 与 driver error |
| SEM-REC-007 | phyMaxUseCount、phyTimeout；Rust 额外 maxLifetime 默认禁用 | PARTIAL（phyMaxUseCount 与 Java MaxPhyTime/shrink/归还 phyTimeout 边界已验证；管理面绑定和后台维护待补） | 边界时刻 |
| SEM-REC-008 | password/config version 淘汰 | TODO | 版本切换 |
| SEM-REC-009 | 满池返还物理关闭且计数正确 | PARTIAL | 与并发 create 竞争 |
| SEM-REC-010 | removeAbandoned 追踪与关闭 | TODO | owner、timeout、stack trace |
| SEM-REC-011 | shrink/idle eviction/minEvictableIdleTime/maxEvictableIdleTime | PARTIAL（显式 shrink 的 minIdle、phyTimeout、min/max idle 与 keepAlive 主分支已实现；DestroyTask、异常后补池待补） | minIdle/keepAlive 组合 |
| SEM-REC-012 | fill/empty/purge/restart | TODO | 维护操作和统计 |
| SEM-REC-013 | creator/destroy task 关闭可等待 | TODO | graceful shutdown |
| SEM-REC-014 | statement cache 清理 | PARTIAL（holder take/discard、pool destroy、显式 clear 与 MySQL schema 变化已清理；recycle 全异常矩阵、listener/result-set trace 待补） | recycle error/discard/schema change/in-use |
| SEM-REC-015 | 一个 lease 只有一个归还策略且至多执行一次 | PASS（native/bb8/deadpool exactly-once contract） | close、Drop、panic、重复 close |
| SEM-REC-016 | async recycle 失败进入受监管回收，不在 Drop 中阻塞 | PARTIAL（脏连接/要求 testOnReturn 的 Drop 直接 discard；受监管销毁与 shutdown 排空待补） | runtime 内外 Drop、队列满、shutdown |
| SEM-REC-017 | native 归还 Druid idle queue；bridge 归还原外部池 | PASS（native active/idle 与两种 external reuse 契约） | 两种模式计数和所有权分别守恒 |

2026-07-28 证据批次：

- Java 基线提交 `33824c3dec1612711f9bb4e409319bcab2e4cd0e`：创建/回收目标用例通过；PreparedStatement oracle 另执行 `DruidConnectionHolderTest4`、两组 `PreparedStatementKeyTest`、`PSCacheTest3`、`DruidDataSourceTest_clearCache`，21/21 测试方法通过；Callable oracle 执行 `PoolableCallableStatementTest` 与 `ConnectionTest4`，101/101 通过；
- Rust：`crates/druid/tests/recycle_semantics_test.rs` 12/12，通过事件序列、状态快照、canonical holder 往返和计数断言；
- Rust：`crates/druid/tests/druid_connection_holder_test.rs` 6/6，验证 holder 所有权、时间/useCount/版本、默认状态与 schema 恢复；`crates/druid/tests/maintenance_semantics_test.rs` 5/5，验证 shrink/phyTimeout/keepAlive 主分支；
- external bridge：bb8/deadpool 各 2 个真实 SQLite 回收用例，验证显式事务 close 复用原物理连接、脏事务 Drop 标记 broken/discarded 后创建替代连接；
- PreparedStatement：Rust core 6/6、pool 6/6、SQLx SQLite 5/5（其中 2 个 prepared 新契约）、RBDC 3/3；覆盖完整 key、access-order LRU、share/inUse、异常移除、schema clear、跨租约拒绝、真实 SQLx statement 执行和 RBDC token 委托；
- CallableStatement：Java 116 个公开声明冻结为构造器 + 115 个方法；Rust 115 个
  同语义入口已闭合，另有 5 个生命周期/扩展方法。core typed/资源与 pool
  contract 覆盖 `Precall1/2/3`、缓存、IN/OUT、LOB/流、`wasNull`、异常淘汰和
  非 callable 句柄拒绝；支持存储过程的真实 Adapter 与 vendor 矩阵仍开放；
- 由于真实 SQLx/RBDC 数据库矩阵尚未覆盖 readOnly/isolation/holdability 等可选能力，相关契约保持 `PARTIAL`，不提前标 `PASS`。

## 6. Connection、Statement 与事务

| ID | 语义 | 当前 | 验收 |
| :--- | :--- | :--- | :--- |
| SEM-CONN-001 | autoCommit/readOnly/isolation/catalog/schema/holdability | PARTIAL | getter/setter/unsupported |
| SEM-CONN-002 | commit、rollback、savepoint | PARTIAL | 成功、错误、状态 |
| SEM-CONN-003 | network/query timeout 与 cancel | TODO | driver cancellation |
| SEM-CONN-004 | statement execute/query/update/largeUpdate/batch | PARTIAL | 返回类型和行数 |
| SEM-CONN-005 | prepared 参数类型、null、LOB、清理和 batch | PARTIAL（`Vec<Value>` 单次参数、clear hook 与真实 SQLx prepared 执行已接通；JDBC setter 状态、LOB、batch 待补） | 参数序列 |
| SEM-CONN-006 | callable IN/OUT 参数 | PARTIAL（三种 prepareCall key、六种 OUT 注册、标量/Decimal/日期时间/Blob/Clob/NClob/Reader 命名 IN、索引/命名 OUT、LOB 资源与精确长度重载、wasNull、缓存与异常删除已迁移；URL、Array/Ref/RowId/SQLXML、其余流、typed getObject、ResultSet/unwrap 和真实 Adapter 待补） | Java 101 个 oracle + Rust typed 2/2、Blob 2/2、Clob 2/2、pool 2/2 + SQLite unsupported 能力检测 |
| SEM-CONN-007 | result set next/close/fetch row count/hold time | TODO | stream 提前退出 |
| SEM-CONN-008 | metadata 与 unwrap | TODO | driver 类型和值 |
| SEM-CONN-009 | fatal exception 使 holder discard | PARTIAL | vendor SQLState/code |
| SEM-CONN-010 | XA prepare/commit/rollback/recover | TODO | 2PC 故障注入 |
| SEM-CONN-011 | `PhysicalConnection` 只表达物理数据库能力，不含池化/Filter/统计 | PASS（独立 object-safe SPI；旧 `Connection` 仅 alias） | API surface 与依赖检查 |
| SEM-CONN-012 | `SqlxConnectionAdapter`、`RbdcConnectionAdapter` 通过同一 direct contract | PARTIAL（两者已接通 create/exec/fetch/transaction/savepoint/close；Callable 当前都显式 unsupported） | reset、cancel、metadata、真实数据库及 callable 能力矩阵 |
| SEM-CONN-013 | bb8/deadpool 通过 Provider contract，而非 `PhysicalConnectionFactory` | PASS（两者直接实现 core `Pool`） | acquire/timeout/return/broken lease |
| SEM-CONN-014 | adapter 统一上报 capability 与结构化 `PhysicalError` | TODO | unsupported、transient、fatal、timeout、cancel |

## 7. FilterChain

| ID | Java 语义 | Rust 当前 | 状态 | 验收 |
| :--- | :--- | :--- | :--- | :--- |
| SEM-FLT-001 | Filter 按配置顺序执行 before | `FilterChain::before_execute` | PARTIAL | 顺序/短路 |
| SEM-FLT-002 | after 在成功和失败均执行，顺序正确 | `after_execute(...).await` | PASS（completion cancellation 另见 SEM-FLT-016） | exactly-once |
| SEM-FLT-003 | `exec` 全上下文透传 | before/after 复用同一 `ExecContext` | PASS | 参数、name、start、fingerprint |
| SEM-FLT-004 | `fetch` 同样经过 chain | 统一 before/after，after 含 row_count | PASS | before/after + row stat |
| SEM-FLT-005 | connection_* hooks | 事件 enum，仅部分调用 | PARTIAL | 每个 Java hook |
| SEM-FLT-006 | statement/prepared/callable hooks | 事件 enum | PARTIAL | 全执行入口 |
| SEM-FLT-007 | resultSet/LOB/metadata hooks | enum 不完整接线 | TODO | 访问与 close |
| SEM-FLT-008 | init/destroy/configFromProperties | trait 默认方法 | PARTIAL | manager 生命周期 |
| SEM-FLT-009 | dynamic-dispatch chain 游标 | 无 `FilterChainImpl` 等价 | TODO | Filter 调 chain 下一节点 |
| SEM-FLT-010 | Filter alias/SPI/order | 无 manager | TODO | 配置加载 |
| SEM-FLT-011 | WallFilter 接入 | `Wall` 未实现 BeforeFilter | TODO | 所有 SQL 入口 |
| SEM-FLT-012 | StatFilter 接入 | AfterFilter 调用链已恢复 | PARTIAL | 真实池执行后统计逐字段增加 |
| SEM-FLT-013 | LogFilter 输出配置 | 无 | TODO | 字段、级别、脱敏 |
| SEM-FLT-014 | ConfigFilter 解密/远程配置 | 无 | TODO | key/URL/error |
| SEM-FLT-015 | Encoding/DateTime filter 结果 | 无 | TODO | driver fixture |
| SEM-FLT-016 | execute/query future 被取消后仍产生唯一 completion outcome | 无 completion guard | TODO | 每个 await 点、行流提前 drop、队列关闭 |

## 8. SQL parser、AST、Visitor 与输出

| ID | 语义 | Rust 迁移策略 | 当前 | 验收 |
| :--- | :--- | :--- | :--- | :--- |
| SEM-SQL-001 | `DbType.of` 名称/别名/URL 推断 | canonical `DbType` | TODO | 全枚举 fixture |
| SEM-SQL-002 | Lexer token、offset、line/column | compatibility lexer | TODO | token stream diff |
| SEM-SQL-003 | 注释、hint、变量和字符串转义 | lexer/parser adapter | TODO | 保留与拒绝 |
| SEM-SQL-004 | `SQLParserFeature` 开关 | feature layer | TODO | 逐开关 |
| SEM-SQL-005 | `parseStatements` 全输入消费与 EOF | parser facade | TODO | trailing token error |
| SEM-SQL-006 | 多语句、分隔符与 keepComments | parser facade | PARTIAL，仅 Wall 简单 parse | statement list diff |
| SEM-SQL-007 | 方言 parser 工厂/SPI | registry + sqlparser 扩展 | TODO | DbType→parser |
| SEM-SQL-008 | AST 节点类型、字段与 parent | compatibility AST | TODO | 逐对象 snapshot |
| SEM-SQL-009 | attributes、before/after comments | metadata sidecar | TODO | clone/serialize |
| SEM-SQL-010 | Visitor visit/endVisit 和中止 | visitor trait | TODO | 事件序列 |
| SEM-SQL-011 | output visitor 格式化 | formatter adapter | TODO | byte/golden diff |
| SEM-SQL-012 | parameterize、参数列表和 merge | AST parameterizer | PARTIAL，当前正则式近似 | SQL+params diff |
| SEM-SQL-013 | fingerprint 稳定性 | canonical hash policy | PARTIAL | Java key 对照 |
| SEM-SQL-014 | schema repository/resolve | repository module | TODO | DDL→lineage |
| SEM-SQL-015 | builder/transform | AST rewrite | TODO | rewrite diff |
| SEM-SQL-016 | `PagerUtils` count/limit | dialect pager | TODO | 方言 SQL diff |
| SEM-SQL-017 | parser exception 类型、位置、消息 | `DruidSqlError` | TODO | malformed corpus |
| SEM-SQL-018 | MySQL 方言对象族 | extension/fork | TODO | Java MySQL corpus |
| SEM-SQL-019 | Oracle 方言对象族 | extension/fork | TODO | Java Oracle corpus |
| SEM-SQL-020 | PostgreSQL 方言对象族 | extension/fork | TODO | Java PG corpus |
| SEM-SQL-021 | SQL Server/DB2/ClickHouse 等方言 | extension/fork | TODO | 各自 corpus |

只有上游 `sqlparser-rs` 的输出恰好一致时才可直接标 `PASS`；“能解析”不等于 Druid 语义一致。

## 9. Wall

| ID | 语义 | 当前 | 验收 |
| :--- | :--- | :--- | :--- |
| SEM-WALL-001 | provider 根据 DbType 选择，未知方言走 SPI | TODO | provider 矩阵 |
| SEM-WALL-002 | select/insert/update/delete allow | PARTIAL | 每项开/关 |
| SEM-WALL-003 | DDL、transaction、set/show/use 等 allow | PARTIAL | 每项开/关 |
| SEM-WALL-004 | update/delete 必须有 where | PARTIAL | AST 语义，不可字符串猜测 |
| SEM-WALL-005 | 恒真/恒假/double const/like/case 条件 | TODO | Java Wall corpus |
| SEM-WALL-006 | deny/permit table/function/schema/variant | PARTIAL | 大小写、schema、嵌套 |
| SEM-WALL-007 | white list 与 cache | TODO | hit/miss/reset |
| SEM-WALL-008 | multiStatement/comment/hint/metadata | PARTIAL | parser feature 组合 |
| SEM-WALL-009 | mustParameterized | TODO | 参数化规则 |
| SEM-WALL-010 | tenant column/table pattern 注入/检查 | TODO | join/subquery/update |
| SEM-WALL-011 | WallContext 和 check 统计 | TODO | task-local 隔离 |
| SEM-WALL-012 | violation 类型、errorCode、message | PARTIAL | 逐规则对照 |
| SEM-WALL-013 | logViolation/throwException | TODO | 四种组合 |
| SEM-WALL-014 | provider cache clear/white list API | TODO | admin action |
| SEM-WALL-015 | 各方言 WallVisitor | TODO | 方言安全语料 |

当前 Wall 的测试能证明一小组规则工作，不能证明 46 个配置字段均生效。

## 10. 统计、日志和监控

| ID | 语义 | 当前 | 验收 |
| :--- | :--- | :--- | :--- |
| SEM-STAT-001 | SQL 参数化后合并 | PARTIAL | 基于 P4 AST 与 Java key 对照 |
| SEM-STAT-002 | execute/error/running/concurrentMax | PARTIAL | 固定时钟并发 |
| SEM-STAT-003 | execute time total/max/histogram | PARTIAL | 边界桶 |
| SEM-STAT-004 | update/fetch rows total/max | TODO | exec/fetch 真实接线 |
| SEM-STAT-005 | transaction/batch/resultSet hold time | TODO | 状态序列 |
| SEM-STAT-006 | slow SQL/slow parameters | PARTIAL | 阈值、错误 |
| SEM-STAT-007 | datasource/connection/statement/resultset 分层统计 | TODO | 快照逐字段 |
| SEM-STAT-008 | reset 的原子语义 | TODO | 与并发 record 竞争 |
| SEM-STAT-009 | StatFilter 上下文/listener | TODO | 嵌套调用 |
| SEM-STAT-010 | JMX 属性映射 OpenMetrics | TODO | 指标名、label、类型 |
| SEM-STAT-011 | DruidStatService JSON schema | TODO | golden response |
| SEM-STAT-012 | tracing/logging 字段和脱敏 | TODO | event snapshot |

## 11. HA、管理与集成

| ID | 语义 | 当前 | 验收 |
| :--- | :--- | :--- | :--- |
| SEM-HA-001 | named/random/sticky selector | PARTIAL | 确定性种子与上下文 |
| SEM-HA-002 | 节点校验、摘除、恢复 | TODO | 故障时间线 |
| SEM-HA-003 | Registry/Watcher 事件 | TODO | ZooKeeper/File adapter |
| SEM-HA-004 | 热切换与旧池排空 | PARTIAL | 活跃事务和连接 |
| SEM-HA-005 | 全节点不可用错误 | TODO | fallback 策略 |
| SEM-ADM-001 | datasource/sql/wall/web/session 端点 | TODO | JSON schema/status |
| SEM-ADM-002 | 登录、IP allow/deny、session | TODO | 安全矩阵 |
| SEM-ADM-003 | reset/clear/enable/disable 管理动作 | TODO | 权限和副作用 |
| SEM-ADM-004 | 静态资源与页面 | TODO | route/content/security |
| SEM-INT-001 | starter 配置绑定和条件装配结果 | TODO | config fixtures |
| SEM-INT-002 | WebStatFilter 请求/URI/session 统计 | TODO | Tower middleware |
| SEM-INT-003 | ORM/iBatis SQL 统计集成 | TODO | ORM fixture |

## 12. 错误、并发和生产属性

| ID | 语义 | 当前 | 验收 |
| :--- | :--- | :--- | :--- |
| SEM-NFR-001 | 错误类别、cause、上下文字段 | PARTIAL | error snapshot |
| SEM-NFR-002 | 不 panic；错误可恢复 | PARTIAL | fuzz/fault injection |
| SEM-NFR-003 | maxActive、active/pooling/total 守恒 | FAIL | loom + stress |
| SEM-NFR-004 | async cancellation 安全 | TODO | 每 await 点取消 |
| SEM-NFR-005 | shutdown 不丢连接/任务 | TODO | graceful timeout |
| SEM-NFR-006 | 配置/规则热更新一致性 | TODO | versioned snapshot |
| SEM-NFR-007 | 密钥和 SQL 参数不泄漏 | TODO | log/metric scan |
| SEM-NFR-008 | 长稳无内存/连接/任务泄漏 | TODO | 24h soak |
| SEM-NFR-009 | 性能不低于发布预算 | TODO | Java/Rust 同环境 benchmark |

## 13. PoolConfig 字段级对照（恢复原表）

原文列出的配置项全部保留。表中的 Rust 字段是迁移目标，不代表当前字段存在或已经接线。

| Java 配置 | Java 默认值 | Rust 目标字段 | 当前 | 关键验收 |
| :--- | :--- | :--- | :--- | :--- |
| `maxActive` | 8 | `max_active` | PARTIAL，现名 `max_open` | 并发容量不越界 |
| `minIdle` | 0 | `min_idle` | PARTIAL，未维护 | 驱逐/keepAlive 后仍满足 |
| `maxWait` | -1 | `max_wait` | PARTIAL，现为 timeout | 无限、0、正数 |
| `initialSize` | 0 | `initial_size` | PARTIAL | init 创建数量 |
| `timeBetweenEvictionRunsMillis` | 60000 | `time_between_eviction_runs` | TODO | 调度周期 |
| `minEvictableIdleTimeMillis` | 1800000 | `min_evictable_idle_time` | TODO | 边界时刻 |
| `maxEvictableIdleTimeMillis` | 25200000 | `max_evictable_idle_time` | TODO | 强制淘汰 |
| `testOnBorrow` | false | `test_on_borrow` | PARTIAL，配置与 borrow validator 已接线 | borrow 校验与真实驱动错误 |
| `testOnReturn` | false | `test_on_return` | PARTIAL，显式 close 已接线 | return 校验、Drop 处置、真实驱动 |
| `testWhileIdle` | false | `test_while_idle` | TODO | idle 校验 |
| `validationQuery` | null | `validation_query` | TODO | query/driver ping |
| `validationQueryTimeout` | -1 | `validation_query_timeout` | TODO | timeout |
| `keepAlive` | false | `keep_alive` | PARTIAL，到期探测与计数已接线 | 后台调度、失败后补池 |
| `poolPreparedStatements` | false | `pool_prepared_statements` | PARTIAL，native pool/holder/pooled wrapper 已接线 | cache enable、external bridge |
| `maxPoolPreparedStatementPerConnectionSize` | 10 | `max_pool_prepared_statements_per_connection` | PARTIAL，access-order LRU 容量已接线 | 0/边界、管理面/属性别名 |
| `removeAbandoned` | false | `remove_abandoned` | TODO | 活跃追踪 |
| `removeAbandonedTimeoutMillis` | 300000 | `remove_abandoned_timeout` | TODO | 超时关闭 |
| `logAbandoned` | false | `log_abandoned` | TODO | 日志脱敏 |
| `breakAfterAcquireFailure` | false | `break_after_acquire_failure` | TODO | 重试终止 |
| `connectionErrorRetryAttempts` | 1 | `connection_error_retry_attempts` | TODO | 次数和退避 |
| `asyncCloseConnectionEnable` | false | `async_close_connection_enable` | TODO | 异步关闭 |
| `useUnfairLock` | true | `use_unfair_lock` | TODO | 等待公平性 |
| `phyTimeoutMillis` | -1 | `physical_connection_timeout: Option<Duration>`，`None` 表示禁用 | PARTIAL，归还与 shrink 淘汰边界已验证 | 属性绑定、管理面、后台维护 |
| `phyMaxUseCount` | -1 | `max_use_count`（Rust `0` 表示禁用） | PARTIAL，边界契约通过 | 管理面/属性别名 |
| `defaultAutoCommit` | true | `default_auto_commit` | PARTIAL，创建初始化与 reset 已接线 | 真实 adapter 与属性绑定 |
| `defaultReadOnly` | null | `default_read_only` | PARTIAL，`Option<bool>` 已接线 | unsupported/真实 adapter |
| `defaultTransactionIsolation` | null | `default_transaction_isolation` | PARTIAL，`Option<u8>` 已接线 | JDBC 常量映射/真实 adapter |
| `defaultCatalog` | null | `default_catalog` | PARTIAL，非空时初始化 | adapter 能力/属性绑定 |
| `dbTypeName=odps` 特例 | 推断值 | `db_type_name` | PARTIAL，跳过 default autoCommit 已验证 | URL 推断/属性绑定 |
| `name` | null | `name` | PARTIAL | Filter/stat context |
| `url/jdbcUrl` | null | `url` | PARTIAL | 属性别名 |
| `driverClassName` | null | `driver_name` | PARTIAL | driver registry |
| `username` | null | `username` | PARTIAL | 配置优先级 |
| `password` | null | `Secret<String>` | PARTIAL | 不泄漏、版本更新 |

字段完成需要同时满足：存在、默认值一致、属性绑定一致、运行路径读取、管理面可见、差分测试通过。

## 14. WallConfig 字段级对照（恢复原表）

| Java WallConfig | Rust 目标字段 | 当前 | 行为验收 |
| :--- | :--- | :--- | :--- |
| `selectAllow` | `select_allow` | PARTIAL | false 拒绝所有 SELECT |
| `selectAllColumnAllow` | `select_all_column_allow` | PARTIAL，规则未完整接线 | `SELECT *` |
| `selectIntoAllow` | `select_into_allow` | PARTIAL，未接线 | SELECT INTO |
| `selectLimit` | `select_limit` | TODO | 方言 LIMIT 注入/限制 |
| `insertAllow` | `insert_allow` | PARTIAL | INSERT 开关 |
| `updateAllow` | `update_allow` | PARTIAL | UPDATE 开关 |
| `deleteAllow` | `delete_allow` | PARTIAL | DELETE 开关 |
| `dropTableAllow` | `drop_table_allow` | PARTIAL | DROP TABLE |
| `truncateAllow` | `truncate_allow` | PARTIAL | TRUNCATE |
| `alterTableAllow` | `alter_table_allow` | PARTIAL，未接线 | ALTER |
| `createTableAllow` | `create_table_allow` | PARTIAL，未接线 | CREATE |
| `commitAllow` | `commit_allow` | PARTIAL，未接线 | COMMIT |
| `rollbackAllow` | `rollback_allow` | PARTIAL，未接线 | ROLLBACK |
| `useAllow/showAllow/describeAllow` | 对应同名字段 | PARTIAL，未接线 | 方言管理语句 |
| `startTransactionAllow` | `start_transaction_allow` | PARTIAL，未接线 | START TRANSACTION |
| `setAllow` | `set_allow` | PARTIAL，未接线 | SET |
| `multiStatementAllow` | `multi_statement_allow` | PARTIAL | 多语句 |
| `hintAllow` | `hint_allow` | PARTIAL，未接线 | hint/token |
| `commentAllow` | `comment_allow` | PARTIAL，未接线 | 注释 |
| `noneBaseStatementAllow` | `none_base_statement_allow` | PARTIAL，未接线 | 非基础语句 |
| `limitZeroAllow` | `limit_zero_allow` | PARTIAL，未接线 | LIMIT 0 |
| `selectWhereAlwayTrueCheck` | `select_where_alway_true_check` | PARTIAL，未接线 | 恒真条件 |
| `selectHavingAlwayTrueCheck` | `select_having_alway_true_check` | PARTIAL，未接线 | HAVING 恒真 |
| `updateWhereAlwayTrueCheck` | `update_where_alway_true_check` | PARTIAL，未接线 | UPDATE 恒真 |
| `deleteWhereAlwayTrueCheck` | `delete_where_alway_true_check` | PARTIAL，未接线 | DELETE 恒真 |
| `conditionAndAlwayTrueAllow` | `condition_and_alway_true_allow` | PARTIAL，未接线 | AND 恒真 |
| `conditionAndAlwayFalseAllow` | `condition_and_alway_false_allow` | PARTIAL，未接线 | AND 恒假 |
| `conditionDoubleConstAllow` | `condition_double_const_allow` | PARTIAL，未接线 | 双常量比较 |
| `conditionLikeTrueAllow` | `condition_like_true_allow` | PARTIAL，未接线 | LIKE 恒真 |
| `caseConditionConstAllow` | `case_condition_const_allow` | PARTIAL，未接线 | CASE 常量 |
| `variantCheck` | `variant_check` | PARTIAL，未接线 | 变量检查 |
| `mustParameterized` | `must_parameterized` | PARTIAL，未接线 | 参数化要求 |
| `metadataAllow/wrapAllow` | 对应同名字段 | PARTIAL，未接线 | metadata/wrapper |
| `denyTables` | `deny_tables` | PARTIAL | schema/大小写/嵌套 |
| `denyFunctions` | `deny_functions` | PARTIAL，测试明确未接线 | 函数遍历 |
| `denySchemas` | `deny_schemas` | PARTIAL | schema 解析 |
| `denyVariants` | `deny_variants` | PARTIAL，未接线 | 变量 |
| `readOnlyTables` | `read_only_tables` | TODO | DML 拒绝 |
| white/black SQL cache | cache/sets | TODO | add/remove/clear |
| `tenantColumn` | `tenant_column` | PARTIAL，未接线 | tenant 条件 |
| `tenantTablePattern` | `tenant_table_pattern` | PARTIAL，未接线 | 表匹配 |

## 15. 统计方法级对照（恢复原表）

| Java 统计对象/方法 | Rust 目标 | 当前 | 验收 |
| :--- | :--- | :--- | :--- |
| `JdbcSqlStat` | `JdbcSqlStat` | RENAME/PARTIAL | canonical 快照 |
| `getExecuteCount()` | `execute_count` | PARTIAL | 成功+失败口径 |
| `getTotalTime()` | `total_time_ns` | PARTIAL | 固定时钟 |
| `getMaxTimespan()` | `max_time_ns` | PARTIAL | CAS 并发 |
| `getRunningCount()` | `running_count` | TODO | before/after 守恒 |
| `getConcurrentMax()` | `concurrent_max` | TODO | 并发峰值 |
| `getFetchRowCount()` | `fetch_row_count` | TODO | fetch/stream 关闭 |
| `getUpdateCount()` | `update_count` | TODO | execute update |
| `getErrorCount()` | `error_count` | PARTIAL | driver/Wall/filter 错误 |
| batch size total/max | 对应字段 | TODO | batch |
| result-set hold time | 对应字段/histogram | TODO | close/提前终止 |
| in-transaction count | 对应字段 | TODO | transaction context |
| `JdbcDataSourceStat` | `JdbcDataSourceStat` | RENAME/PARTIAL | datasource 快照 |
| `JdbcConnectionStat` | `JdbcConnectionStat` | TODO | connect/close/error |
| `JdbcStatementStat` | `JdbcStatementStat` | TODO | prepare/execute/close |
| `JdbcResultSetStat` | `JdbcResultSetStat` | TODO | open/fetch/close |
| `DruidDataSourceStatManager` | registry + facade | TODO | register/remove/reset |
| `TableStat` | `TableStat` | TODO | select/update/delete/insert |
| `slowSqlMillis` | `slow_sql_threshold` | PARTIAL | 阈值边界 |
| `mergeSql` | `merge_sql` | PARTIAL | P4 AST 参数化 |
| `connectionStackTrace` | tracing span/optional backtrace | TODO | 开关和脱敏 |

## 16. 管理端点、Wrapper 与 Starter 对照（恢复原表）

### 16.1 管理端点

| Java 端点/对象 | Rust 目标 | 当前 | 完成条件 |
| :--- | :--- | :--- | :--- |
| `StatViewServlet` | Axum `StatViewRouter` | TODO | route、认证、IP |
| `/druid/index.html` | `/druid/index.html` | TODO | 静态资源与安全头 |
| `/druid/datasource.json` | `/druid/api/datasources` + compatibility route | TODO | JSON 字段 |
| `/druid/sql.json` | `/druid/api/sql/top` + compatibility route | TODO | 排序、筛选、分页 |
| `/druid/wall.json` | `/druid/api/wall` | TODO | Wall stat/config |
| `/druid/connectionInfo-*.json` | `/druid/api/active` | TODO | 脱敏 |
| `/druid/weburi.json` | URI stat API | TODO | Tower middleware |
| `/druid/webapp.json` | app stat API | TODO | application snapshot |
| `/druid/websession.json` | session stat API | TODO | session adapter |
| `/druid/spring.json` | integration stat API | TODO | Rust integration 对应结果 |
| `WebStatFilter` | `WebStatLayer` | TODO | request/URI/session |
| `DruidStatServiceMBean` | metrics + admin operations | TODO | 属性和操作 |

### 16.2 外部池 Wrapper

| Java Wrapper | Rust 迁移方式 | 当前 |
| :--- | :--- | :--- |
| DBCP `BasicDataSource` wrapper | `DbcpConnectionProvider` + compatibility config | TODO |
| C3P0 `ComboPooledDataSource` wrapper | `C3p0ConnectionProvider` + capability map | TODO |
| Proxool `ProxoolDataSource` wrapper | `ProxoolConnectionProvider` + capability map | TODO |
| `DruidDataSourceC3P0Adapter` | `DruidDataSourceC3p0Adapter` compatibility facade | TODO |

这里迁移的是配置、生命周期、unwrap 和统计结果；不要求在 Rust 中重建 Java 第三方类。

### 16.3 Spring Boot Starter 结果迁移

| Java Starter 对象 | Rust 目标 | 当前 |
| :--- | :--- | :--- |
| `DruidDataSourceAutoConfigure` | feature + config loader + builder | TODO |
| `DruidDataSourceWrapper` | `DruidDataSource` facade | TODO |
| `DruidStatProperties` | typed config | TODO |
| `DruidFilterConfiguration` | filter registry/config | TODO |
| `DruidStatViewServletConfiguration` | admin router config | TODO |
| `DruidWebStatFilterConfiguration` | Tower layer config | TODO |
| `DruidSpringAopConfiguration` | tracing/instrumentation layer | TODO |
| `spring.datasource.druid.*` | compatible alias profile | TODO |

## 17. Rust 侧新增对象的语义边界

| Rust 对象 | 来源语义 | 允许新增的原因 | 禁止事项 |
| :--- | :--- | :--- | :--- |
| `PhysicalConnection` | JDBC Connection 平台能力 | Rust 内部 object-safe async SPI | 不公开池化所有权，不承载 Filter/统计 |
| `PhysicalConnectionFactory` | JDBC Driver/driver manager | native direct adapter 显式依赖注入 | 不得获取 bb8/deadpool lease |
| `ToastyConnectionFactory` / `ToastyConnectionAdapter` | 内置标准数据源 | Toasty `Driver::connect` raw connection | 不得创建 Toasty `Db` pool；SQLite 真实合同必须通过 |
| `ConnectionProvider` | DataSource 获取路径 + 外部池 wrapper | native/bridge 统一入口 | 同一租约不得嵌套两层 pool |
| `ConnectionLease`、`LeaseReturner` | pooled close/recycle | Rust 所有权与异步 Drop | 不得双重归还或静默丢弃 |
| `DruidConnectionHolder` | Java 同名 holder | Rust 所有权下由 native idle queue 与 pooled facade 移动同一个对象 | 不得再拆出第二份 idle metadata，也不得暴露为第二个公开连接 |
| `ConnectionDefaults` | holder 默认状态与 reset | Rust 值对象，避免驱动类型泄漏 | 只能作为 canonical holder 的组成，不得替代 PS cache 等未迁移职责 |
| `ConnectionRecycleDisposition` | recycle 最终分支 | 将处置结果传给 native/external Provider | 不得跳过 rollback/reset/validate 直接伪造 reusable |
| `PoolBuilder` | properties/setter | typed builder | 不得丢属性别名 |
| `BeforeFilter/AfterFilter` | Filter hook | async 拆分 | 不得丢事件 |
| `ParameterizedSql`、fingerprint | parameterized visitor/stat key | Rust 值对象 | 结果必须与 Java 对齐 |
| `SqlHint`、`DataSourceGroup` | HA selector/node group | async context/ArcSwap | 不得丢粘性与恢复 |
| OpenMetrics exporter | MBean/StatService | 协议替换 | 不得丢属性和管理动作 |

## 18. 测试基线（保留并升级原验收）

原 `druid-core` 等测试已并入 `druid`；adapter 合同已并入 `druid-wrapper`。

| 当前模块/对象域 | 归并来源（历史） | 原验收 | 修订验收 |
| :--- | :--- | :--- | :--- |
| `druid/core` | `druid-core` | 每 trait 一个 mock | 方法/错误差分 + 真实 adapter |
| `druid/pool` | `druid-pool` | 10,000 acquire/release | 加 loom、取消、泄漏、状态快照 |
| `druid/sql` | `druid-sql` | DROP/DELETE/UPDATE 三类 | 全方言 corpus + AST/output/parameterize |
| `druid/stats` | `druid-stats` | SQL 合并率 ≥90% | Java key 和快照逐字段一致 |
| `druid/dynamic` | `druid-dynamic` | 1,000 RPS 错误率 <1% | 加事务粘性、恢复、排空 |
| `druid-admin` | `druid-admin` | datasource JSON 可访问 | 全端点 schema、认证、IP、reset |
| `druid-wrapper` | 原四个 adapter/bridge crate | 任一 Postgres 冒烟 | 所有 adapter 真实数据库矩阵 |

## 19. 迁移门禁

每个阶段关闭前运行：

1. Java fixture 生成或执行 oracle。
2. Rust 同输入执行候选实现。
3. 结果、错误、状态快照、事件序列、指标逐字段比较。
4. 并发契约使用 loom/压力测试，驱动契约使用真实数据库。
5. 未绑定对象、未绑定契约、stub、仅 mock adapter、失效配置字段任一存在即失败。

---

文档版本：v2.0

基线日期：2026-07-28

状态：语义契约基线
